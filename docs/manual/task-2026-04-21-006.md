# Manual Task, Diagnose "Metabase dashboard isn't loading" (UAT r1 item 15)

**Created:** 2026-04-21, Phase 9F UAT r1 response
**Phase:** 9F (UAT item 15)
**Status:** pending
**Priority:** high
**Blocks:** Joint 9E+9F release cut (dashboard load is the Tier 3 payoff; if it doesn't load, the demo's final beat is broken).

## Context

UAT r1 item 15 read "The dashboard isn't loading" on the `/demo/ecommerce/confirmation` page. The confirmation page embeds one full Metabase dashboard via a server-signed JWT. There are three paths where "isn't loading" can happen; which one you see determines the fix.

The signing path is at `src/app/demo/ecommerce/confirmation/page.tsx`:

- If `METABASE_EMBED_CONFIG` or `MB_EMBEDDING_SECRET_KEY` env vars are missing, `mintConfirmationDashboardUrl` returns `null` and `DashboardPayoff` renders the "dashboard embeds disabled" fallback with a deep-link to `bi.iampatterson.com/dashboard/2` behind Google SSO.
- If both env vars are set and valid, the page renders an `<iframe src={signedUrl}>`. The iframe's load outcome depends on Metabase reachability + warm state.

The dual-evaluator fix pack (commit `7c79129`) already confirmed the signer code path is correct; the behaviour depends entirely on deploy-time env + Metabase state.

## What you're seeing, figure out which path

Visit `/demo/ecommerce/confirmation` on the environment where you hit this (likely a local `npm run dev` or a Vercel preview) and open the browser devtools.

### Path A, "dashboard embeds disabled in this environment"

If the page shows an amber `TIER 3 · DASHBOARDS` panel with the text "the signing env vars aren't wired in this environment," **the env vars are missing**. This is the code's honest fallback.

**Fix:** set both in the relevant environment:
- `MB_EMBEDDING_SECRET_KEY`, mirror of Secret Manager `metabase-embedding-secret-key`
- `METABASE_EMBED_CONFIG`, mirror of Secret Manager `metabase-embed-config`

For Vercel: `vercel env add <name>` or set via the dashboard. For local `npm run dev`: create `.env.local` (gitignored) with both values.

After setting, restart `npm run dev` or trigger a redeploy.

### Path B, iframe renders but shows "This question couldn't be embedded"

The JWT is either malformed, signed with the wrong secret, or the dashboard id in `METABASE_EMBED_CONFIG.dashboardId` doesn't match what's published-embeddable on the Metabase instance.

**Check:**
1. `gcloud secrets versions access latest --secret=metabase-embed-config --project=iampatterson`, note the `dashboardId` field.
2. On Metabase at `bi.iampatterson.com`, open that dashboard and confirm "Embedding" is ON and "Enable embedding" is saved.
3. Confirm `MB_EMBEDDING_SECRET_KEY` matches Metabase's "Embedding secret key" (Admin → Settings → Embedding).

### Path C, iframe spins indefinitely

Metabase cold-start. Cloud Run JVM warmup after idle. The session handoff `docs/sessions/session-2026-04-21-002.md` notes `--no-cpu-throttling` was applied earlier, but verify:

```
gcloud run services describe metabase --region=us-central1 --project=iampatterson --format='value(spec.template.spec.template.metadata.annotations."run.googleapis.com/cpu-throttling")'
```

Expected: `false` (or unset if `--no-cpu-throttling` is the default). If it returns `true`, re-run:

```
gcloud run services update metabase --region=us-central1 --project=iampatterson --no-cpu-throttling
```

Also check `minScale`:

```
gcloud run services describe metabase --region=us-central1 --project=iampatterson --format='value(spec.template.metadata.annotations."autoscaling.knative.dev/minScale")'
```

Expected: `1` (keeps at least one warm instance). If `0`, bump to `1`:

```
gcloud run services update metabase --region=us-central1 --project=iampatterson --min-instances=1
```

### Path D, iframe renders but body is blank white

The iframe loads the embed HTML shell (200 OK) but shows nothing. In devtools Network, all requests to `/app/*` on `bi.iampatterson.com` show `302 → accounts.google.com/o/oauth2/v2/auth` with header `x-goog-iap-generated-response: true`.

**Root cause:** the LB URL-map pathMatcher routes `/api/*` and `/embed/*` to the non-IAP backend, but not `/app/*` (Metabase's JS/CSS bundle path). Asset requests fall through to the IAP-gated backend and IAP rewrites them to its login page, which cannot render inside an iframe (CSP `frame-ancestors`).

**Fix (applied 2026-04-22):** add `/app/*` to the pathMatcher:

```
gcloud compute url-maps export metabase-url-map --destination=/tmp/metabase-url-map.yaml --project=iampatterson
# add "- /app/*" to pathMatchers[0].pathRules[0].paths (same indent as /api/*, /embed/*)
gcloud compute url-maps import metabase-url-map --source=/tmp/metabase-url-map.yaml --project=iampatterson
```

Defensive future additions if new asset families surface in a HAR: `/public/*`, `/fonts/*`, `/favicon.ico`.

### Path E, assets load but cold-start exceeds ship-gate thresholds

Incognito load, all assets return 200, dashboard renders, but first paint >10s (30–45s observed 2026-04-22).

**Root cause:** not JVM cold-start (`minScale=1` prevents that); this is Metabase application warmup, per-card BigQuery queries cold-miss Metabase's card cache and BigQuery's 24h query cache, serialising ~3 card queries at 5–15s each.

**Fix:** the three-layer stack in Phase 9F deliverable 9, Cloud Run config (already applied), Metabase dashboard caching (TTL 24h on dashboard id 2, set once in admin), and runtime organic warmup (`src/lib/metabase/keep-warm.ts` fired from `/` and `/demo/ecommerce`).

After the full stack ships, re-run the ship-gate probe below from a fresh-deploy state (resets the warmup module's 30-min debounce so the next request *is* a cold-path probe).

## Ship-gate probe (D9 binary release blocker)

Once dashboard loads are working, record the cold-start probe for the release handoff:

1. Wait ≥15 minutes after last touch (let Metabase idle).
2. Open an incognito window to `/demo/ecommerce/confirmation?order_id=TEST&total=26.00&items=1`.
3. Record:
   - time to first iframe paint (when anything appears inside the iframe)
   - time to interactive (when dashboard visualisations finish rendering)
4. **Ship-gate invariants:**
   - First paint < 10s
   - Interactive < 15s

If either exceeds the threshold, `--no-cpu-throttling` isn't enough and the release is blocked until the cold-start path is addressed (likely needs `--min-instances=1` firmly in place + `--cpu-boost` if available).

## Notes (fill in when complete)

Once diagnosed, add a Notes block below with:
- Which path (A / B / C) applied.
- What configuration change resolved it.
- Probe timings.

Then flip **Status** to `done` and the 9F release cut is unblocked on this axis.

That changes the whole diagnosis. This isn't a cold-start issue — it's a URL map / IAP misconfiguration, and it's a harder ship-gate failure than the probe.
                                                                                        
What you just proved                                                                                                                                           
                                                                                                                                                              
A first-time anonymous visitor to /demo/ecommerce/confirmation cannot see the dashboard at all. The iframe's request to                                        
https://bi.iampatterson.com/embed/dashboard/<jwt> is being intercepted by IAP, which returns its Google login page. Since the iframe has no session with IAP,  
it either shows a blocked/blank body or the IAP auth flow can't render inside the frame. Only after you went to bi.iampatterson.com in a separate tab and      
completed the Google SSO did your browser get an GCP_IAP_* cookie — and then the iframe request carried that cookie and IAP let it through.
                                                                                                                                                              
The intended design (from session 021 / task-2026-04-17-005)                                                                                                   

▎ URL-map path split /api/* + /embed/* → non-IAP backend, applied to bi.iampatterson.com.                                                                      
                                                              
There should be two backend services behind the LB:                                                                                                            
1. IAP-gated backend — default, for the admin UI (/, /dashboard/2, etc.)
2. Public (non-IAP) backend — attached to path matchers /embed/* and /api/embed/* so static embeds work for anonymous users                                    
                                                                                                                          
That path split either never got applied, or it regressed. The symptom — "embed works only after authenticating to IAP directly" — is definitionally this rule 
missing.                                                                                                                                                       
                                                                                                                                                              
Cold-start is a secondary concern                                                                                                                              
                                                              
With minScale=1 holding a warm instance, a 1-minute wait after auth is probably IAP handshake + Metabase session init + dashboard render against the BigQuery  
data source, not a JVM cold-start. Once the URL map is fixed, anonymous embed requests won't hit IAP, so the "IAP dance" latency disappears and the real probe
numbers will almost certainly fit the ship-gate thresholds.                                                                                                    
                                                              
Priority order: fix the embed path, then measure cold-start. A failing probe on a broken embed path tells you nothing.                                         

Commands to confirm + fix (your shell)                                                                                                                         
                                                              
1. Confirm the diagnosis in incognito (30s, no auth cookie present):                                                                                           
                                                              
Open an incognito window to /demo/ecommerce/confirmation?order_id=TEST&total=26.00&items=1. If the dashboard stays blank/broken, that's the anonymous-visitor  
reality. (Your authenticated browser sees it work because you have the IAP cookie.) **CONFIRMED**
                                                                                                                                                              
2. Inspect the URL map:                                         

# list LB URL maps
gcloud compute url-maps list --project=iampatterson --format='table(name,defaultService.basename())'
```
gcloud compute url-maps list --project=iampatterson --format='table(name,defaultService.basename())'
NAME              DEFAULT_SERVICE
metabase-url-map  metabase-backend-direct
```

# then describe the one for bi.iampatterson.com (name from above)                                                                                              
gcloud compute url-maps describe metabase-url-map --project=iampatterson
```
gcloud compute url-maps describe metabase-url-map --project=iampatterson
creationTimestamp: '2026-04-17T10:56:59.607-07:00'
defaultService: https://www.googleapis.com/compute/v1/projects/iampatterson/global/backendServices/metabase-backend-direct
fingerprint: 7I1iGXvNqfk=
hostRules:
- hosts:
  - '*'
  pathMatcher: direct-paths
id: '4317386865151822469'
kind: compute#urlMap
name: metabase-url-map
pathMatchers:
- defaultService: https://www.googleapis.com/compute/v1/projects/iampatterson/global/backendServices/metabase-backend
  name: direct-paths
  pathRules:
  - paths:
    - /api/*
    - /embed/*
    service: https://www.googleapis.com/compute/v1/projects/iampatterson/global/backendServices/metabase-backend-direct
selfLink: https://www.googleapis.com/compute/v1/projects/iampatterson/global/urlMaps/metabase-url-map
```

What you're looking for in the describe output:                                                                                                                
- defaultService — likely metabase-backend (the IAP-gated one)
- pathMatchers — this is the critical part. Need a path matcher that routes /embed/* and /api/embed/* to a different backend service that does NOT have IAP    
attached.                                                                                                                                                  
                                                                                                                                                              
If pathMatchers is missing or only contains the default, that's the bug.
                                                                                                                                                              
3. Check backend services + IAP state:                          
                                                                                                                                                              
gcloud compute backend-services list --project=iampatterson --format='table(name,iap.enabled,backends.group.basename())'

You should see two backend services. If only one exists, the non-IAP backend was never created (or got deleted). If two exist and both have iap.enabled: True, 
that's the bug.
```
gcloud compute backend-services list --project=iampatterson --format='table(name,iap.enabled,backends.group.basename())'
NAME                     ENABLED  GROUP
metabase-backend         True     ['metabase-neg']
metabase-backend-direct           ['metabase-neg']
```
                                                                                                                                                              
4. Rerun the IAP setup script (idempotent):                                                                                                                    

The infrastructure/metabase/setup-iap.sh script from session 019 handles this, and infrastructure/metabase/ should also have the domain setup script. Session  
handoffs at docs/sessions/session-2026-04-17-02*.md have the full context.
                                                                                                                                                              
Post back                                                                                                                                                      

Paste the output of steps 2 + 3 and I can tell you exactly what's missing. If the non-IAP backend exists but the URL map path matcher is gone, it's a          
one-command fix. If the non-IAP backend never got created, it's 2-3 commands. If it was somehow set up right originally and regressed, we'll want to know
when/why (handoff candidate for the Phase 11 GTM-CI pipeline's operational sibling — a terraform-ified LB + URL map so this can't silently drift).

Option 1 (recommended) — export, edit in your normal editor, import                                                                                            
                                                                                                                                                              
# 1. Export the URL map to a local file                                                                                                                        
gcloud compute url-maps export metabase-url-map --destination=/tmp/metabase-url-map.yaml --project=iampatterson

# 2. Open it in whatever editor you like (VS Code, TextEdit, whatever)                                                                                         
open /tmp/metabase-url-map.yaml          # macOS default app                                                                                                   
# or: code /tmp/metabase-url-map.yaml    # if VS Code's `code` shim is installed                                                                               
                                          
# 3. Add the `- /app/*` line to the pathRules.paths array, save the file.                                                                                      
                                                                                                                                                              
# 4. Re-import (applies your edit to the live LB)                                                                                                              
gcloud compute url-maps import metabase-url-map --source=/tmp/metabase-url-map.yaml --project=iampatterson

# 5. Verify the change is live                                                                                                                                 
gcloud compute url-maps describe metabase-url-map --project=iampatterson
                                                                                                                                                              
The section you're editing looks like this and should end up like this:
                                                                                                                                                              
pathMatchers:                                                                                                                                                  
- defaultService: https://.../metabase-backend                                                                                                                 
name: direct-paths                                                                                                                                           
pathRules:                                                                                                                                                   
- paths:                                                      
  - /api/*
  - /embed/*                                                                                                                                                 
  - /app/*            # ← add this line (same indentation as the other two)
  service: https://.../metabase-backend-direct                                                                                                               
                                                              
YAML is whitespace-sensitive — the - /app/* line must have the exact same leading spaces as - /api/* and - /embed/*.
                                                                                                                                                              
On re-import, gcloud validates the YAML and rejects malformed input cleanly, so if the indent is off you'll get a readable error rather than a half-applied
change. 