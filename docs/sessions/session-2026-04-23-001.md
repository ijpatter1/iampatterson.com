# Session Handoff, 2026-04-23-001

**Phase:** 9F, Ecommerce Demo Native-Reveal Rebuild (+ Phase 10a scoped, Phase 11 D9 extended)
**Date:** 2026-04-23 (session spanned 2026-04-22 → 2026-04-23)
**Branch:** `phase/9f-ecommerce-native-reveal` (11 commits ahead of origin, clean working tree)

## Completed This Session

Session started with 3 Pass-3 evaluator findings from the prior handoff (1 Tech Important + 2 Tech Minors), then grew to encompass the ship-gate probe, two live production infra fixes, the Phase 9F D9 keep-warm feature, two evaluator fix-packs, and a new Phase 10a scoping. 11 commits total.

### Pass-3 evaluator closure (prior handoff's findings)

| Commit | What |
|---|---|
| `12b2461` | `remove_from_cart` GTM trigger + GA4 tag pair shipped. Schema added in `c72e248` last session was never followed by allow-list wiring; cart walkthrough's `routing: ['GA4', 'BigQuery']` claim now honored end-to-end. New `tests/integration/web-container-spec.test.ts` (+10 tests) pins both web-container.json + deploy-phase6.js. Live-deployed this session (web container version 8, trigger id 114 + tag id 115). |
| `113262d` | Stale `"24 post-F2"` / `"14 of 20"` / `"22 pre-F2"` counts in `ride-along.ts` + `session-state-provider.test.tsx` swept for `.length` references. No behavior change. |
| `d94d85f` | `EcomDemoBanner` `className` now composes with palette classes instead of replacing them. Regression pin ensures caller-provided className survives alongside `#0D0B09` / `#F3C769`. |

### Production infrastructure fixes (out-of-code)

| What | How |
|---|---|
| Live GTM web container deploy | `remove_from_cart` trigger (id 114) + GA4 tag (id 115) POSTed via targeted inline node script, published as container version 8. Verified ship via integration test + live describe. |
| Live `metabase-url-map` URL-map edit | Added `/app/*` to non-IAP pathMatcher alongside `/api/*` + `/embed/*`. Fixed a silently-broken Metabase embed (anonymous visitors got blank iframe because IAP was 302'ing `/app/dist/*.js` bundle requests to Google login). Diagnosed from user-provided HAR. |
| Metabase dashboard caching configured | 24h TTL on dashboard id 2 via Metabase admin (`Admin settings → Performance → Caching`). Layer (b) of the Phase 9F D9 three-layer warmup stack. |
| `deploy-phase6.js` hardened | `266b32c` — workspace ID now resolved at runtime (previously hardcoded workspace 7 / 13 that GTM had consumed during prior publish), and added `tagmanager.edit.containerversions` scope (was missing, caused `ACCESS_TOKEN_SCOPE_INSUFFICIENT` on create_version). |

### Phase 9F D9 three-layer keep-warm stack

Triggered by 2026-04-22 cold-start probe returning 30–45s first-paint (vs <10s ship-gate). Diagnosis: not JVM cold-start (`minScale=1` holds the container warm), but Metabase application warmup + BigQuery per-card query warmup.

| Commit | Layer |
|---|---|
| `5f80eb0` | Docs — Phase 9F D9 rewritten to describe three-layer stack; Phase 11 D9 extended to cover Metabase LB topology alongside GTM; task card Paths D + E added. |
| `bd94376` | Layer (c) runtime warmup — new `src/lib/metabase/keep-warm.ts` (fire-and-forget + async variants, module-scope 30-min debounce initialised to `-Infinity`, fetches `/embed/dashboard/:jwt` + metadata API + per-card endpoints). Invoked from `src/app/page.tsx` + `src/app/demo/ecommerce/page.tsx` Server Components. 14 tests covering debounce, no-op on null mintUrl, silent on failure, tolerates malformed metadata, unparseable JWT. 4 source-sniff pins that both pages wire the hook without awaiting. |
| (admin config) | Layer (b) Metabase dashboard caching TTL=24h on dashboard id 2. |
| (pre-session) | Layer (a) Cloud Run `--no-cpu-throttling` + `--min-instances=1`. |

**Ship-gate probe re-run 2026-04-23:** first paint <10s, interactive <10s. Status flipped `pending → done` on `docs/manual/task-2026-04-21-006.md`.

### Two evaluator fix-packs on the warmup feature

| Commit | What |
|---|---|
| `ca4c4f9` | Pass-1 findings. Critical 1: both server components were statically prerendered (`○`), warmup fired at build time not per-visitor → added `export const dynamic = 'force-dynamic'` + source-sniff pins. Critical 2: `parseOrderedCards` → `parseDashcards` (Metabase v0.59.6 emits `dashcards`; `ordered_cards` is pre-0.47 legacy, kept as defensive fallback). Major: task card lines 121-253 of pasted chat transcript truncated; replaced with Notes block + Status flip. Minors: `console.warn` for observability; new Task 7 step 9 in `infrastructure/metabase/README.md` for layer (b) caching config. |
| `f19e74d` | Pass-2 Tech Minor 3. `console.warn` also fires when every card-fan-out fetch fails (the partial-IAP-regression canary), silent on partial failure. +2 test pins. Also: disclosed in commit body that `ca4c4f9` silently bundled a `docs/voice-and-style-guide.md` working-tree change from `git add -A`. |

### Scoping commits (no implementation)

| Commit | What |
|---|---|
| `de71535` | Phase 11 D9 (new) — declarative GTM deploy pipeline deliverable: generic reconciler on `web-container.json` + GitHub Actions workflow (dry-run diff on PR, live apply on merge via WIF, manual approval). Retires `deploy-phase6.js`. |
| `5f80eb0` | Phase 11 D9 extended to cover Metabase LB topology (forwarding rule, URL map, backend services, IAP state, SSL cert) alongside GTM. |
| `1f88398` | Phase 10a (new sub-phase) — framework currency Next.js 14 → 16. 7 deliverables. Triggered by Pass-2 Tech Important (Vercel fire-and-forget durability); `after()` from `next/server` stable in Next 15 is the clean primitive to migrate the keep-warm invocation to. Runs after 9E+9F joint release cut on its own `phase/10a-framework-currency` branch. |

### Trivial Minors sweep at handoff time

| Commit | What |
|---|---|
| `b980a35` | Handoff-eval Pass-3 Minors sweep: `deploy-phase6.js:289` Step 1 log now derives from `.length`; container-version notes string derives from `.length` (was hardcoded "8 triggers, 8 GA4" which is now 9/9); 1 stray em-dash at `keep-warm.test.ts:147` → comma; `src/app/demo/ecommerce/page.tsx:29` comment updated post-`force-dynamic`; task card line 115 `(A / B / C)` → `(A / B / C / D / E)`. |

## In Progress

Nothing. Working tree clean; every shipped commit is complete.

## Blocked

Nothing on the session's work. Two forward-dated items that should NOT be worked on this phase:

- **Phase 10a implementation** is gated on the 9E+9F joint release cut landing on `main`. Doing the Next 14 → 16 upgrade on the in-flight 9F branch would add churn to the release PR.
- **Phase 11 body work** (GTM + Metabase LB reconciler, rest of operational readiness) is Phase 11 scope; out of current phase.

## Issues & Technical Debt

Carried from handoff evaluators, ranked by severity:

### Important (live gap, scheduled for closure)

1. **Next.js 14 Vercel fire-and-forget durability gap in the keep-warm module.** `src/lib/metabase/keep-warm.ts:129` fires `void warmMetabaseDashboard(deps).catch(() => {})` from a Server Component. On Vercel's Lambda runtime, the execution context can freeze immediately after response flush, truncating the 5–15s per-card BigQuery fan-out mid-flight. Layer (b) Metabase dashboard caching provides independent cold-start protection (24h TTL), so the feature is self-healing — but between now and Phase 10a landing, the expensive warmup layer's durability is not guaranteed on cold Lambdas. **Closed by Phase 10a deliverable 2** (`after()` from `next/server`, stable in Next 15).

### Minor (defer to next session or phase)

2. **Phase 10a deliverable 1's enumerated "7 file" list will drift.** REQUIREMENTS.md D1 pins seven specific file paths using async Request APIs. Between now and 10a implementation, renames or new `cookies()` / `headers()` call sites could drift. Consider softening to "approximately seven files identified at scoping time; codemod is the authoritative pass" when starting 10a work.
3. **Anonymous deep-link caveat is traffic-profile-dependent.** D9 paragraph's "at most one visitor per 24h quiet period sees a cold dashboard" holds under portfolio traffic volumes; a PR-campaign spike driving `/demo/ecommerce/confirmation?...` deep-links directly would bypass layer (c) entirely. Worth revisiting if traffic profile changes; potential Phase 10 or 11 addition of scheduled keep-warm layer (d).
4. **Voice guide entry label is off-voice.** `docs/voice-and-style-guide.md:17` "Industry-generalization-as-filler" is a nominal compound while sibling principles lead with imperatives ("Lead with the thing", "Name the specific", "Cut the explanation…"). Rewrite to imperative form (e.g. "**Don't pad with industry generalizations.**") would harmonize.
5. **Voice guide bundling disclosure pattern (process note).** `ca4c4f9` silently bundled the "Industry-generalization-as-filler" working-tree edit into its scope list; disclosed retroactively in `f19e74d`'s body. No corrective action needed; process note for future sessions to co-edit disclose in the original commit when adjacent docs get bundled via `git add -A`.
6. **FetchMock type errors in `tests/unit/lib/metabase/keep-warm.test.ts` under `tsc --noEmit`** (lines 108, 126, 309). `jest.fn(async () => { throw ... })` infers as `Mock<Promise<never>, []>` which doesn't assign to `FetchMock`. Per `MEMORY.md` `tsc --noEmit` is not CI-enforced, so no runtime/build impact. Triage in Phase 10a D5 (test-suite stabilization under React 19 is the natural place).

## Evaluator Results

### Technical evaluator

- **Weighted score:** 4.00/5.0
- **Verdict:** PASS WITH ISSUES
- **Critical:** 0
- **Important unresolved:** 1 (Next.js 14 fire-and-forget durability, captured as Phase 10a D2)
- **Minors addressed inline (trivial sweep):** 5 (closed in `b980a35`)
- **Minors carried:** 3 (item 2, 5, 6 above; items 3 + 4 came from product review)

### Product reviewer

- **Weighted score:** 4.58/5.0
- **Verdict:** PASS
- **Vision alignment:** 5/5
- **User experience:** 4.5/5
- **Content quality:** 4/5
- **Feature depth:** 4.5/5
- **Minors:** 4 (items 2, 3, 4, 5 above)

## Test State

- **Total:** 1164 (was 1132 at session start — **+32 net**)
- **Passing:** 1164
- **Failing:** 0
- **Skipped:** 0
- **Suites:** 105 (was 102 — +3: `web-container-spec.test.ts`, `keep-warm.test.ts`, `keep-warm-wiring.test.ts`)

## Build State

- **Build:** Clean. `/` and `/demo/ecommerce` render `ƒ` (Dynamic) as intended; `/demo/ecommerce/confirmation` `ƒ`. One pre-existing Recharts cosmetic warning on `/demo/ecommerce/analytics`, unrelated.
- **Lint:** Clean (`next lint` — 0 warnings, 0 errors).
- **TypeScript:** Strict mode holds in CI (Jest + Next build both run). `tsc --noEmit` has 26 errors, 24 pre-existing, 2 new this session (FetchMock alias mismatch, see Minor #6). Per `MEMORY.md`, `tsc --noEmit` is not CI-gated.

## Next Steps

Both 9F ship-gate closure and Phase 10a scoping landed this session. Phase 9F is **dev-complete** — all 12 deliverables ✅ in `docs/PHASE_STATUS.md`, ship-gate passed. The logical next steps, in priority order:

1. **Push branch to origin.** `git push origin phase/9f-ecommerce-native-reveal`. 11 commits ahead. Small.
2. **Optional: formal Phase 9F UAT r3.** UAT rounds 1 + 2 closed across sessions -003 and -004. With the this-session work (ship-gate closure, `remove_from_cart` honesty fix, evaluator fix-packs), a structured UAT r3 pinning each item pass/fail is the natural next artifact if a third review is wanted before release cut. Pattern: `docs/uat/phase-9e-uat.sh` shape. Medium.
3. **Joint 9E+9F release cut to `main`.** Merge `phase/9f-ecommerce-native-reveal` → `main`. All ship-gate obligations closed. Medium.
4. **Start Phase 10a** (framework currency, Next 14 → 16). Gated on step 3. Cut branch `phase/10a-framework-currency` from the post-merge main. Follow Phase 10a's 7 deliverables. Small-to-medium per deliverable; overall 1–2 focused sessions.

## Session Notes

### Pattern: evaluator-driven iterative closure produced 3 dual passes this session

Prior handoff had Pass-3 evaluator findings open (1 Tech Important + 2 Tech Minors). This session's work on the keep-warm feature triggered Pass-1 (FAIL, 2 Criticals on the new module), Pass-2 (PASS WITH ISSUES, 1 Tech Important + minors), and handoff Pass-3 (PASS WITH ISSUES, 1 Important + 8 Minors; Important was already captured as Phase 10a D2, 5 Minors swept in `b980a35`). The pattern that worked: fix Critical + Major inline, schedule Important if it needs a framework primitive we don't have, sweep cheap Minors, carry durable items to handoff Issues & Technical Debt. Both `/evaluate` iterations landed fixes that exposed real production bugs (statically-prerendered warmup + `ordered_cards` field drift).

### Pattern: schema-to-allow-list drift (now caught twice)

- First instance: `remove_from_cart` added to schema + dataLayer helper in `c72e248` last session; GTM trigger+tag pair missing. Caught by Pass-3 tech evaluator, closed in `12b2461`.
- Second instance (same session): `/app/*` path in Metabase URL map. The original LB setup (session-2026-04-17-00X) split `/api/*` + `/embed/*` → non-IAP but didn't capture Metabase's JS bundle path family. Caught by user HAR, fixed via `gcloud compute url-maps export/import`. Documented as task card Path D.

Both drifts motivated the Phase 11 D9 scope extension (`5f80eb0`). The declarative reconciler covers both GTM containers + Metabase LB topology against canonical specs; the existing integration pin (`tests/integration/web-container-spec.test.ts`) is the red/green gate for schema-to-GTM; a new `tests/integration/metabase-lb-spec.test.ts` is scoped for the URL-map path matchers. The LB regression pin for `/app/*` deferred to Phase 11 D9 implementation rather than added ad hoc here.

### Pattern: `git add -A` can bundle unintended working-tree changes

`ca4c4f9` silently swept a `docs/voice-and-style-guide.md` edit (a new "industry-generalization-as-filler" paragraph, on-voice content but not in the stated commit scope). Disclosed retroactively in `f19e74d`'s body. Future sessions should either (a) stage files explicitly rather than `git add -A` when the session's worked on many surfaces, or (b) `git status` before committing and disclose any surprise additions in the commit message.

### Pattern: production-deploys-as-side-effects reveal script fragility

Had to deploy the `remove_from_cart` trigger+tag pair to GTM live this session. `deploy-phase6.js` hardcoded workspace ID 7 (consumed by prior publish; current live workspace is 8 for web / 14 for sGTM). Also missing `tagmanager.edit.containerversions` scope. Both traps documented + fixed in `266b32c`. A future live deploy via the canonical script now resolves workspaces at runtime.

### The anonymous deep-link caveat framing

Phase 9F D9 paragraph explicitly acknowledges that a visitor who deep-links straight to `/demo/ecommerce/confirmation?...` without going through `/` or `/demo/ecommerce` in the prior 24h will see a cold first-paint, bounded by the 24h dashboard cache TTL. This is the honest framing: defense-in-depth via layers, not a single-mechanism guarantee. Worth keeping in mind if a real campaign-driven deep-link spike happens; noted under Issues & Technical Debt as item 3.

### Branch state

- **Branch:** `phase/9f-ecommerce-native-reveal` at `b980a35`
- **Commits this session:** 11 (`12b2461` → `b980a35`)
- **Unpushed:** 11 commits ahead of `origin/phase/9f-ecommerce-native-reveal`. Next session should push early.
- **Working tree:** clean.

### CLAUDE.md freshness

Current Phase header is deeply stale (predates UAT rounds 1 + 2 + this session's work). Propose updates in a followup commit.

---

**Next session starting point:** `/start-phase 9F` (or directly to `/start-phase 10a` if 9E+9F has been pushed + merged in the interim). First step: push the branch, then either run UAT r3 or proceed straight to the joint 9E+9F release cut.
