# Session 2026-08-31-001 — feat/claudish: the Claudish translator, end to end

Outside-phase work (Phase 11 D9 continues separately on its own branch).
Spec: `/Users/ipatterson/Downloads/english-to-claudish.md`. Branch
`feat/claudish` off `main@b61187f`, 33 commits. Plan of record at
`~/.claude/plans/you-are-working-separately-zany-nygaard.md` (approved
after 3 explorer + 3 planner + 1 research subagent passes and 8 user
decisions).

## Completed

**Frontend — /claudish is fully built** (`bc2dbb7`..`c697340`):
- Detection: regex heuristic (6 signal families, two-family rule,
  em dashes alone never convict) + Schmitt-trigger latch (enter 0.80 /
  exit 0.55 / 24-char min / 250ms dwell) behind one sync API
  (`detectClaudish`), CCLD swaps in without an API change.
- `useClaudishTranslation`: 600ms debounce, abort-on-edit
  (AbortController end to end; the client shell cancels the reader so
  the proxy stops paying for tokens), stale-dimmed-until-first-token,
  refusal discards partial AND stale, capacity/error collapse to the
  verbatim boundary line, client LRU, missing proxy URL ⇒ capacity
  (previews never break). Status derived at render — no setState in
  effects.
- 14 components: two panels, working swap, live "Claudish - detected",
  "412 / 1,200 · 9 em dashes" counter (user decision: real cap, not
  5,000), spinner verbs, copy/rate/share (verbatim thumb labels), the
  two-line footer. Dual-skin trade dress: `[data-skin='clone'|'personal']`
  CSS variables — takedown response is `NEXT_PUBLIC_CLAUDISH_SKIN=personal`
  + redeploy. Self-hosted Roboto latin (OFL notice appended).
- Share: lz-string `?t=` payload (budget-tiered truncation, never over
  1,900 chars), zero-token rehydration, dynamic two-panel OG card
  (edge, immutable, generic card on every failure path).
- Events: `claudish_translate/detected/share/rate` — counts/enums only,
  no text in any payload (structurally test-pinned); overlay narrative
  sentences; coverage-milestone dilution 27→31 accepted (consistent
  with ecommerce events).
- Chrome: shared suppression predicates (`src/lib/chrome/suppression.ts`),
  /claudish keeps Header+SessionPulse, sheds LiveStrip/HomeBar/Footer/
  Bubbles. Sitemap entry. Trade-dress guard test ("Google" exactly
  once, in the disclaimer; no four-color marks; comment-stripped source
  scan).

**Proxy — infrastructure/cloud-run/claudish-proxy** (`0a12f0d`..`b234695`):
- Express SSE with the terminal-frame invariant, two-channel errors,
  origin gate, per-IP 20/200/1000 windows with spoof-resistant XFF
  extraction, reservation-based budget tracker ($23/day ÷ instances,
  auto-trip, kill switch, UTC rollover), lane ladder
  (vertex-global → us-east5 → anthropic-api → cache-only) with commit
  barrier + circuit breaker + 3s first-token deadline, refusal terminal
  by design (category logged, explanation never), req-close upstream
  abort (found + fixed a real Node-20 bug: req 'close' fires at
  body-complete; disconnect detection uses res 'close' + writableEnded).
- Prompts distilled fresh from the explain/writing-style skills;
  interim hand-written few-shots (lexicon-generated set pending Ian's
  review); canary token for mechanical injection assertions.
- Golden set (12/direction incl. injection-as-text + the allowSkip
  refusal probe — the documented magic string was patched out ~May 2026,
  so the refusal path is covered by unit tests + a FORCE_REFUSAL_TOKEN
  smoke hook instead) and the latency harness (TTFT p50<1s/p95<2s
  pins), both env-gated with repo-root runners. Honest CI framing: no
  workflow runs Jest today; these are pre-deploy operator gates.
- setup.sh (idempotent, --dry-run, refuses deploy without
  MODEL_ID_CONFIRMED=1), README with the kill-switch runbook,
  IMPORT_PLAN.md handing Phase 11 D9 exact import IDs + the KILL_SWITCH
  ignore_changes trap. 80 service-local tests + 27 gated.

**Corpus + CCLD** (`15293ea`..`b691615`):
- Miner over 2,001 transcripts (nested subagents included): 33.4MB
  assistant prose, 49,889 chunks kept after scrub/dedup/balance caps.
  Launch-post stats mined, not asserted: 86,873 em dashes (2.19/message),
  "Let me" ×13,703, bold lead-ins ×10,391, "I'll start by" exactly
  ×1,000, "you're absolutely right" ONCE in 33MB.
- CCLD trained (grad-checked TS trainer, Adam, early stop, T=1.2,
  int8): 36,952-byte weights, dev 94.5% / test 97.8% /
  project-held-out 97.0%; formal-Wikipedia FP slice ~2%; top-ranked
  n-gram is the spaced em dash. Live in the page behind configHash
  assertion + fixture parity. Negatives: pre-cutoff Wikipedia
  revisions (rvstart=2022-11-30), SHA-pinned rust-book/curl/git docs,
  capped heuristic-filtered human turns (circularity disclosed);
  personal corpus declined by user — noted in the model card.
- Deadpan model card + corpus-method doc, drift-pinned to
  ccld-metrics.json (8 tests).

**Wiring** (`1b58b89`): web-container spec + deploy-claudish.js (15
dlvs, 4 triggers, 4 consent-gated tags, the R3 `page_location` strip —
the ?t= payload IS the input text), BigQuery schema +15 columns,
37 integration pins. Server container needs nothing (All-GA4 generic
forwarding, asserted).

**Manual artifacts:** `docs/manual/task-2026-08-31-001.{sh,md}` (launch
prerequisites; QA PASS by guv:reviewer after 9 findings fixed) +
`task-2026-08-31-002.md` (GCP budget alert).

**E2E:** Playwright chromium run PASSES all 3 flows against the real
app with the trained model in-page (type → detect → stream → copy →
share → zero-proxy rehydration; chrome suppression).

## Decisions (authoritative record — code comments cite this section)

1. Char counter shows the real 1,200 cap, not GT's 5,000 (spec literal
   superseded by user decision).
2. Generic errors reuse the verbatim capacity line; real cause goes to
   analytics only.
3. CCLD negatives: public pre-cutoff sources only (personal corpus
   declined; disclosed in the model card).
4. Transcript mining: all 26 project dirs, scrub + denylist as controls.
5. Coverage-milestone dilution 27→31 accepted (consistent with
   ecommerce events).
6. Prompt caching: grow the few-shot block past 4,096 tokens when the
   lexicon set lands; inert until then (documented everywhere it shows).
7. Review gate = guv plugin dual eval; ultracode on from mid-session —
   adversarial workflow verification applied to gate findings.
8. Detector labels never hedge (user, mid-session): four claimed tiers —
   English - detected (<0.30, hysteresis to 0.45) / Leaning English /
   Leaning Claudish / Claudish - detected (0.80 latch); claimed side
   drives auto direction; "Detect language" only for an unreadable box.
9. Refusal verdicts are remembered per key at zero spend (never
   content-cached); transient error/capacity ARE retryable on re-entry.
10. $25/day is the total backstop; DAILY_BUDGET_USD=23 is the model
   slice (~$2 infra floor + headroom).
11. Phrase damping (user-caught false positive, post-gate): "let me"
   convicted ordinary speech because 24% of positive chunks carried it
   vs 18 occurrences in 6MB of formal-prose negatives. Fix: Cornell
   movie dialogs (2011) + 1990s Usenet added as CONVERSATIONAL
   negatives; 'let me know' excluded from positives outright; other
   'let me' chunks subsampled to 5%. Behavioral minimal pairs pinned in
   ccld-behavior.test.ts; the workplace "let me check..." case is
   allowed to LEAN but never convict (the model has a point). Retrained
   model: test 97.2%, held-out 94.8%, formal-prose slice ~98%,
   conversational slices 97-99%.

12. Model governance (user-requested, post-gate): every trained model
   now archives to ~/.claudish-corpus/models/ (weights+metrics+fixtures
   +dataset summary move together); r1 recovered from git; promote =
   one command + card regen. A FROZEN holdout (12,792 examples, never
   trained on) + the committed probe battery are the standing eval —
   six models compared on it this session.
13. Model selection (data-grounded): r1's felt accuracy was the let-me
   crutch (it convicts "Let me know if Thursday works" at 0.99 and the
   wife sentence at 0.80; holdout 90.7%). r3 promoted as the shipping
   model (holdout 95.9%, zero human false convictions on the battery)
   + the register-union ensemble: detection = max(CCLD, heuristic),
   where the heuristic owns the STEREOTYPE register CCLD cannot learn
   (the author's Claude says "delve" twice in 33MB — his own skills ban
   it; visitors type the meme expecting detection). Smoking-gun tier:
   first-person meme constructions convict alone; third-person/literal
   human usage stays free. Structural finding from a 6-model sweep: a
   27k-param char-n-gram model cannot separate human business-casual
   ("Let me know if Thursday...") from soft Claudish ("Those quotes
   sting...") — only same-register data can, hence #14.
14. Claude.ai conversational corpus (user-offered): intake parser ready
   (parse-claudeai-export.ts); drop conversations.json at
   ~/.claudish-corpus/raw-claudeai/ → next dataset build folds it in as
   conversational-Claude positives. This is the r6 path to soft-register
   recall. Known CCLD FP carried meanwhile: third-person "the book
   delves into..." convicts (all models share it; card documents it).

15. r6 (claude.ai corpus landed via browser-session download; 1,068
   conversations → 92,171 conversational-Claude chunks): the no-damper
   hypothesis FAILED (conversational Claude says "let me know" too —
   Thursday re-convicts at 0.89-0.93; context is beyond 27k params).
   The synthesis r6d (claudeai positives + dampers + conv negatives)
   is the registry's soft-register champion — claudish-soft 4/4 incl.
   "You're right to push back," which no other model catches — at the
   cost of Thursday leaning 0.687 (sub-latch) and holdout 94.2 vs r3's
   95.9. Decision: r3 stays promoted for launch (its failure direction
   is the joke underselling; r6d's is mislabeling humans); r6d is one
   promote command away if the user weighs the trade differently. Nine
   configs trained this session, all archived and comparable.

16. SHIP DECISION (IAN, 2026-08-31): r6h-hn40 promoted. The contract he
   chose: no human sentence on the battery crosses the 0.80 latch (all
   FPs cap at "Leaning"); all four soft-Claudish probes claim the
   Claudish side (leaning strength); full "Claudish - detected" is
   reserved for the loud register + the heuristic's stereotype door.
   Recipe: claudeai positives, dampers (know 0 / letme .08), negatives
   weighted formal-backbone + conv 75% incl. doubled HN (62k pre-2022
   comments) at 40%. Twelve models in the registry; r3 (strict
   never-lean) and r6g (max soft conviction) remain one promote away.
   Ship-time pins updated to record the contract, not fight it.

17. ANTHROPIC AUTH VIA WIF (IAN, 2026-08-31): the anthropic-api lane
   authenticates by Workload Identity Federation instead of an API key
   — Ian's call, replacing the manual key-create/fund/paste step
   entirely. Claude Console rule `claudish-proxy`
   (fdrl_01RYv2ptEbtu7jpssKo1ZcRH, google-cloud issuer) matches the
   runtime SA's sub (100265153582375128670) + email, minting 10-minute
   workspace-scoped tokens for svac_014RW8M13t3K3QXY6pL7mrLo in
   wrkspc_01K3PnFVDjmiNyuH6DQUJwKo. Verified end-to-end same day from
   this machine via SA impersonation (exchange 200 → workspace:developer
   token → live Haiku 4.5 call answered). Proxy changes: new
   `src/wif.ts` (metadata-server identity provider + oidcFederationProvider,
   client-side TokenCache means no per-request exchange),
   `@anthropic-ai/sdk` 0.65→0.122, lane skips without the three
   federation ids, stray-ANTHROPIC_API_KEY regression pin, setup.sh
   mounts the four ids as plain env (identifiers, not secrets) and drops
   the secret step; also fixed a latent `--set-env-vars` comma-delimiter
   bug (ALLOWED_ORIGINS/LANES are comma-valued) with the ^@^ delimiter +
   a new setup-script contract test. The claudish-anthropic-api-key
   secret shell stays as unused break-glass. Enablement side effects:
   SA created ahead of deploy; Ian's user granted SA-scoped
   iam.serviceAccountTokenCreator for local verification. The Claudish
   workspace spend limit is now the Anthropic-side backstop knob.

18. DEPLOYED (2026-08-31, Ian's go): claudish-proxy revision
   claudish-proxy-00001-hsx live at
   https://claudish-proxy-eb4xrwmo3q-uc.a.run.app with
   LANES=anthropic-api,cache-only (Vertex lanes join via one
   --update-env-vars once the quota preferences land). Smoke: health ok;
   en2cl streamed (warm TTFT 564ms server / 783ms client — under the 1s
   p50 target), server cache replay 149ms byte-identical, cl2en strips
   em dash + "absolutely right" + "delve" correctly; structured logs
   carry counts/ids/timings only, no text. Measured honestly:
   cacheReadTokens=0 on every call — the ~650-token system prefix is
   under Haiku's 4,096-token cache minimum, so prompt caching is
   silently inert. Not a launch blocker at ~$0.001/call uncached;
   carry-forward: growing the en2cl few-shot block past 4,096 tokens is
   a quality + cache lever to take together. First-call TTFT 1240ms was
   the one-time WIF exchange + warmup (TokenCache amortizes it), not a
   cache write. Remaining to go live on the site: Ian pushes
   feat/claudish (bash-guard owns push), Vercel env
   NEXT_PUBLIC_CLAUDISH_PROXY_URL=https://claudish-proxy-eb4xrwmo3q-uc.a.run.app/translate.

19. OVERNIGHT FIX/EVAL LOOP (Ian's delegation, 2026-08-31 -> 09-01,
   "run a fix/eval loop until the translation issue is fixed in both
   directions... don't spend more than $20"): ten capture iterations +
   three 4-judge adversarial panels (subagent tokens, not API spend)
   against the 27-case golden set. Fixes landed: delimiter-wrapped user
   turn (root cause of answering-instead-of-translating), cl2en
   hand-written few-shots (was instruction-only), per-direction
   temperatures (cl2en 0.2 / en2cl 0.3 — default 1.0 caused kill-list
   variance), answer/negation/scope/acronym preservation, en2cl
   no-answer/no-new-facts/no-spin/no-meta-commentary/rewrite-every-
   sentence rules with demonstration few-shots (demonstrations beat
   rules three times running), question-stays-question + growth-ceiling
   assertions, PROMPT_VERSION v2. Deployed revision
   claudish-proxy-00003-x8n; both original user-reported failures
   verified dead on production. Golden gate 29/29. TOTAL API SPEND:
   $0.57 (ledger: scratchpad claudish-loop/spend-ledger.jsonl;
   includes one wasted golden run from a path typo, logged honestly).
   Known residual variance: en2cl occasionally renders hostile
   injections as reported speech instead of imperative register (the
   canary contract holds every run); en2cl length jitter near the
   3.5x ceiling on short inputs.

   FINAL ROUND (post-panel-3): the third judge panel showed each capture
   run was RESAMPLING a stable failure distribution (fabricated effort,
   verbatim openings, dimension-listing recurring on different cases per
   run), not converging — so the closing move was temperature 0 on both
   directions (a translator is a function; determinism makes the golden
   set a true regression suite) plus three demonstration few-shots
   (injection-imperative, personal-content restraint with rewritten
   opening, residual not-just-X tail ban). Deployed revision
   claudish-proxy-00004-2gb. At temp 0 the outputs are stable and clean;
   three cosmetic residues documented as known limits rather than
   chased: (a) en2cl-10 keeps one soft fabricated cause ("care invested
   in its preparation" on the bread), (b) en2cl-12 renders the PWNED
   directive as third-person description rather than imperative (no
   safety editorializing; canary contract holds), (c) en2cl-14 appends
   generic significance axes ("performance characteristics") to the
   SSE question — vestigial, no longer the answer-advancing specific
   dimensions. Panel-3 verdict recorded here in full honesty: it also
   flagged findings this record overrules with reasons — dropping
   Claudish assent openers in cl2en is the spec, not a fidelity loss;
   and several en2cl cases were double-counted under its cl2en lens.

20. DETECTOR: r7d-mask-letme04 PROMOTED (overnight, under Ian's
   "feel free... your choice" delegation; r6h stays one promote away).
   Journey: r7a (claude-topic negative slice, 87 mined user turns,
   20x oversample) FAILED — the slice poisoned conversational probes;
   r7b (mask + slice) FAILED the same way; r7c (mask on the exact r6h
   dataset) proved the mask works (about-claude 3/3) but shifted the
   Thursday probes over the latch; r7d (mask + KEEP_LETME=0.04) is the
   clean win: about-claude 3/3 FIXED (Ian's sentence 0.895->0.622
   leaning; casual mentions 0.755->0.120 and 0.827->0.108
   English-detected), Thursday probes IMPROVED vs r6h (0.628 leaning /
   one passing outright), wife 0.029, loud Claudish 0.945, holdout
   0.9398 vs r6h 0.9406. One accepted trade: the Anthropic-tweet probe
   at 0.536 (leaning, far under the latch; marginal against its 0.5
   probe bar). Mechanism: CCLD v2 featurizer masks model names to a
   neutral token pre-featurize (config-driven; the blessed-hash loader
   keeps every v1 registry model loadable), plus stronger letme damping
   to rebalance the boundary under the new collision structure.
   Registry now 17 models. Model card regenerated; shifted pins updated
   (fixtures re-scored under r7d). Root suite 1734 green.

21. THE ECHO BUG (Ian-reported 2026-09-01 morning; fixed same session).
   Ian pasted an 802-char dense-technical Claudish paragraph (from this
   transcript) into cl2en and got it back verbatim, em dashes intact.
   Forensics: (a) the model echoed once — temp-0 greedy decoding on
   input whose token mass is mostly preserve-exactly material
   (identifiers, numbers, quoted strings) fell into the copy attractor;
   knife-edge sensitive (appending one word produced a real
   translation); (b) the proxy CACHED the echo (end_turn = "success")
   and replayed it permanently for that input; (c) the golden set had
   no case of this shape — all cl2en cases were marketing-register.
   Fixes: PROMPT_VERSION v3 (anti-echo + parenthetical + arrow-notation
   rules, dense-technical few-shot; also corrects the missed bump when
   rev-00004 changed few-shots under v2 — a process violation of the
   index.ts comment); NEW src/smooth.ts EmDashSmoother — deterministic
   em-dash-to-comma enforcement of cl2en's hard contract in the token
   stream (frame-boundary safe, 6 unit pins), composed into
   translate.ts, golden, and quality-capture so assertions test what
   users receive; NEW cacheableTranslation guard — echoes
   (dash-insensitive compare) and dash-carrying cl2en outputs are never
   cached; Ian's exact paste is golden case cl2en-15. Deployed
   rev-00005; live fresh verification: 0 em dashes, real translation,
   identifiers exact. Golden 30/30. Ledger total: $0.68 USD.

22. THE ROUND-TRIP PROBLEM (Ian's directive 2026-09-01: translations
   must read at least "leaning English" to the detector — "survive
   scrutiny"). Diagnosis by ablation: the detector keyed on the
   DISCOURSE SKELETON, not content — same sentence structures with
   cooking vocabulary scored 0.981 — because no negative source
   contains plain professional work-prose at all. The contrastive
   experiment (1294 cl2en translations of train positives as
   negatives, 3 doses r8-tp1/3/6, $3.40) produced a decisive NEGATIVE
   result: even at 8% dose the models score their OWN training
   negatives at mean 0.75 — masked char 1-4-grams CANNOT separate
   register-stripped content twins; every dose that pressed on
   translations broke a soft-Claudish or Thursday probe. Capacity
   ceiling documented; NO r8 promoted; r7d stays shipped.
   THE SHIPPED SOLUTION is three deterministic layers instead:
   (1) provenance memo (src/lib/claudish/provenance.ts) — the page
   remembers its own recent translations (session-local, 50-entry LRU,
   normalized keys); pasting one back reports the KNOWN side, source
   'provenance' (schema detector_source extended) — covers the demo
   loop at 100%; (2) zero-family latch cap 0.79 — no register-free
   text ever shows full "Claudish - detected" (smoking gun counts as a
   family, meme door open); (3) length-conditioned side cap — zero
   families across >=300 normalized chars claims the English side
   (0.49): 100% of round-trip originals fire >=1 family, so genuine
   Claudish is untouched, and soft-Claudish probes are all shorter.
   Also: EmDashSmoother strips paired **bold** (frame-boundary safe)
   and cl2en v4 cuts trailing -ing analysis clauses; deployed
   rev-00006. RESULTS on the frozen 30-pair unseen-transcript test set
   (scripts/claudish/build-roundtrip-testset.ts + eval-roundtrip.ts;
   set lives in the corpus workspace): foreign translations
   English-side 17% -> 73% (6 still latch — they fire 2+ REAL register
   families, i.e. imperfect translations); originals 30/30 stay
   Claudish-side; session-own translations 100% via provenance.
   Root suite 1746 -> 1750+; golden 30/30. One detect.ts edit mangled
   an export mid-flight (python replace matched inside 'export const')
   — caught by the suite, fixed, noted for honesty. Ledger total:
   $4.29 USD of the $20 cap.

23. THE OUTPUT-SIDE ARC (Ian's redirect 2026-09-01: roll back the
   input-side layers; the LLM output should INHERENTLY score English
   under CCLD). Rollback landed clean (suite back to 1734; instruments
   and output-side smoother/v4-prompt kept, interpretation stated).
   The experiment chain, each step forced by the last:
   (a) repair-loop prototype — targeted-feedback regeneration plateaus
   at 0.92-0.98 on technical content; per-content floors; the detector,
   not the output, binds. (b) r9b valid capacity run (after fixing
   train-core's global-config bug + shape guard) — 2x params cannot fit
   contrastive negatives AND recall collapses; char 1-4-gram bags
   under-encode register. (c) v4 featurizer — the heuristic's 8 family
   scores + 3 rhythm stats as dense model inputs (heuristic now exposes
   familyScores). (d) THE TRAINER BUG THAT INVALIDATED A WEEK OF
   REGISTRY EVALS: the weights embed called configHash() with no
   argument, stamping the V1 hash on every model since r7b — the
   production loader therefore served EVERY model with v1 featurization
   (no mask, no register features). r10/r11 "collapses" were this, not
   science; shipped r7d is self-consistently skewed (its mask never
   runs at inference; behavioral numbers stand as measured). Fix +
   TRAIN/SERVE PARITY GATE: every export now loads the written file
   through the real production loader and must match to 1e-6 or abort.
   (e) r11b (parity-gated): v4 features + REGISTER-FILTERED positives
   (>=1 family; 54,481 of 124k chunks dropped — the corpus said
   Claudish = "text Claude wrote", the product says "the register") +
   contrastive negatives. RESULTS: fresh-pipeline translations 22/30
   English-detected + 1 leaning (77% English-side, mean 0.226; the 5
   that latch fire REAL residual register families — translator
   defects, correctly convicted); originals 100% detected (0.995);
   humans 100% English; battery loud 2/2, about-claude 3/3,
   conversational 5/5. THE COST: claudish-soft 0/4 — soft-register
   Claude prose now reads English, and frozen-holdout recall (old
   all-Claude-text labels) drops to 0.63. This is the fundamental
   collision: soft-Claudish and de-registered translations OVERLAP at
   the item level; no classifier separates overlapping classes. THE
   SHIPPING DECISION IS IAN'S: r7d (soft claims Claudish, round-trip
   broken) vs r11b (round-trip fixed, register-defined detection, soft
   surrendered). Both one promote away. NOT promoted. Ledger $4.49.

## Review Gate (`/guv:eval`, recorded)

**Instruments:** platform `/code-review high feat/claudish` (10
verified findings from 28 candidates: 22 CONFIRMED / 3 PLAUSIBLE / 3
REFUTED) + `guv:reviewer` alignment pass (3 Major, 7 Minor). Under
ultracode, the 5 mechanism-critical findings + their fix designs were
adversarially verified by a 10-agent refuter workflow before
implementation (all mechanisms confirmed; CR3 narrowed to
deadline-abandonment only; two of my fix designs corrected by refuters
before they could ship: the swap-guard had to skip the seq bump, and
lane cleanup must abort before iterator.return()).

**Fix-pack round 1** (`1ecfb66`): AR1 cache-claim honesty; AR4
FORCE_REFUSAL_TOKEN built; AR5 OG card follows the skin flip; AR6
$23/$25 documented; AR9 meta em dash removed; AR10 stat comment;
CR4 MAX_INSTANCES validated; CR6 storage-throw event flood capped;
CR8 latch reset on emptied input; CR10+AR7 share length gates +
empty-target seeding.

**Interleaved user-reported fix** (`8ce0ee5`, `88f3efa`): English
detection labeling → the four-tier no-hedging design (Decision 8),
live-verified on the dev server.

**Fix-pack round 2** (`e48b78d`): CR1 GTM freeze reverted (config-level
page_location would have frozen site-wide SPA attribution at the entry
URL — anti-freeze pin + warning make it unrepeatable) with the ?t=
strip moved to a parse-time inline layout script + mount-effect twin
(closes CR9/AR8); CR2 ACAO on pre-stream errors (capacity branch was
unreachable cross-origin); CR3 per-attempt lane abort (abandoned lanes
stopped generating); CR5+CR7 debounce-effect restructure (manual swap
stream survives; transient failures retryable; refusals remembered).

**Recorded as debt (not fixed, dispositions deliberate):** AR2
harness-not-proof for latency/golden targets (blocked on Model Garden;
honest framing kept); reload-after-strip loses the shared translation
(accepted tradeoff, documented in layout.tsx); pre-strip head-parse
subresource Referer reaches Vercel CDN logs only (not GA4/BQ);
performance.getEntriesByType retains the original URL for the document
lifetime (nothing reads it); winner-only budget reconciliation after
lane abort (bounded loser cost, documented); deploy-phase6.js
consentStatus divergence (pre-existing, untouched).

**Round-2 re-check** (5-agent workflow, fixes graded against the prior
findings): CR3 closed clean; CR1/CR9, CR2, CR5+CR7, and the round-1
sweep closed-with-residuals — every residual fixed in `75dbfe2`,
including two the re-check itself caught: round 2's proxy diff had
never been committed (a `git add` path miss), and the CR5 fix had
introduced a mirror-image liveness hole (stale 'streaming' after
edit-away-and-back within the debounce; regression-pinned). Both
fix-rounds stayed within the two-round cap.

**Gate attestation:** `gate-log.sh append` refuses in this repo — no
`.claude/project.json` manifest (LEGACY mode, pre-guv-scaffold). This
section is the gate's written record in its place; the trace gap is
disclosed here rather than left silent.

## In Progress

- Nothing. All planned offline work is complete and committed.

## Blocked (all on Ian's manual task, none on code)

- Proxy deploy (M4) + golden/latency live runs: Model Garden
  enablement + quota check + Anthropic key
  (`docs/manual/task-2026-08-31-001.sh`). Approval clock 24-48h —
  started? → run `--probe`, then `MODEL_ID_CONFIRMED=1 bash setup.sh`.
- Lexicon hand-review: `~/.claudish-corpus/lexicon.candidates.json`
  (Track A counts + examples; Track B log-odds ran — wants a stopword
  damp before it's useful, noted). Curated `lexicon.json` + regenerated
  few-shots follow.
- GTM apply: `node infrastructure/gtm/deploy-claudish.js --dry-run`
  then real + `--publish` (needs GOOGLE_APPLICATION_CREDENTIALS).
- Vercel env after deploy: `NEXT_PUBLIC_CLAUDISH_PROXY_URL`.

## Test State

Root suite 1372 → 1691 passing (0 failing); proxy-local 80 + 27 gated;
Playwright claudish spec 3/3 on chromium; build + lint clean
(tsconfig target es2017→es2022 — typecheck-only; SWC handles browser
transpilation).

## Issues & Technical Debt

- Track B lexicon discovery needs stopword damping (top hits are
  register-generic: "the", "and the") — candidates file only, nothing
  ships from it unreviewed.
- Corpus session-level split is coarse (22 parent session groups) —
  the project-held-out split is the honest generalization number and
  the model card says so.
- Latency/golden "in CI" remains operator-run until Phase 11 D9 lands
  WIF (documented in the proxy README).
- deploy-phase6.js sets consentStatus notNeeded on its tags while the
  container spec says analytics_storage required — pre-existing
  divergence, not touched; deploy-claudish.js gates properly.
- Full gate-debt list in the Review Gate section above.

## Next Steps

1. Ian: run the manual task (starts the 24-48h clock), review lexicon
   candidates.
2. Land gate findings (fix-now items), push `feat/claudish`, open the
   PR to main.
3. After enablement: deploy, smoke (SSE chunk timestamps, XFF index;
   cache_read_input_tokens will be 0 until the lexicon few-shot set
   crosses the 4,096-token cache minimum — expected, not a bug),
   latency baseline to docs/perf/, golden iteration on the prompts,
   swap lexicon few-shots in (PROMPT_VERSION bump).
4. Launch checklist: Vercel env, GTM publish, UTM'd launch posts using
   the mined stats.
