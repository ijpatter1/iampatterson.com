# Session Handoff — 2026-09-09-001

**Phase:** 14 — Declarative infrastructure (complete; closes initiative 002)
**Date:** 2026-09-09
**Predecessor:** `session-2026-09-08-002` — this session continued past its own handoff, so that artifact covers the build and this one covers the landing.

---

## Completed This Session

Phase 14's six deliverables were already built when this session began. What it did was **land them and then find out what the landing had not proved** — two merges, four review instrument runs, and a chain of defects in the checks themselves.

**The merges, and what they demonstrated.** PR #75 merged as `80a2ff1`. Both `infra-reconcile` and `infra-terraform` triggered and both `apply (main)` jobs held at `status=waiting` on `env=infra-production` — nothing applied unattended, which is the half of [14.3]'s acceptance that could be verified without a person. Ian approved both:

| Run | Result |
|---|---|
| `infra-reconcile` (34358564145) | `infra-production carries 1 protection rule(s).` → `no drift` → published GTM **container version 11** (`ci: 80a2ff1 on 2026-09-09`), now live: 24 tags, 23 triggers, 63 variables |
| `infra-terraform` (34358564379) | `Apply complete! Resources: 0 added, 0 changed, 0 destroyed.` |

PR #76 merged as `55cfd12`, and its run (`34362729377`) exercised the preflight added to `infra-terraform.yml`: step 3 of 7, before auth, `init` and `apply`.

**Commits** (`c14e24f..HEAD`, 23 files, +897/−75):

- `183bf19` — the local gate's findings: stale referents swept, the accounting recomputed from the audit, pipefail pinned
- `e67916a` — four manual cards closed that advertised finished work as `pending`
- `a7915e3` — named this round's own commits as uncovered
- `d3dd7c5` — the merge held at the approval gate, and the role list had a hole
- `3f71538` — permission grants moved into `settings.json` so worktree agents can see them
- `d4275e1` — the preflight was on one apply job, not each
- `382cd7f` — the terraform preflight ran, step 3 of 7
- `cbdb824` — the platform review found two of my own tests passing on their own comments
- `67f12bb` — Phase 14 UAT plan, and a defect it found on its first run
- `d500a8e` — the guard counted protection rules of any type, so it could not fail on the case it exists for

**The one functional defect in production code:** `infra-reconcile.yml` piped the reconciler through `tee`, so the step exited with `tee`'s status — always 0. A crashed reconciler would have reported success, dropping the warning banner from the PR comment *and* skipping the failure gate, the two mechanisms that exist to catch it. Fixed with `shell: bash`; later refined so both containers are always diffed rather than aborting after the first.

**A production IAM gap the merge exposed.** The first CI `terraform plan` failed 403 on `secretmanager.versions.get`. The deployer held `roles/secretmanager.secretAccessor`, which carries `versions.access` — the payload — but not `versions.get`, the metadata, which `google_secret_manager_secret_version` reads first. Confirmed by diffing the two role definitions rather than guessing. Ian chose the project-level `roles/secretmanager.viewer`; the plan then read *"No changes."* Twelve roles now, not eleven.

**Four manual cards closed against evidence.** Cards 002 and 003 sat at `Status: pending` while the infrastructure they describe had been live for months (`metabase-cert` ACTIVE, forwarding rule serving 34.102.206.180, IAP returning 302). Sweeping the directory found 004 in the same state, which neither reviewer named — `metabase-api-key` and `metabase-embed-config` both exist and are produced only *after* first-run setup. This matters because `/guv:phase` Step 3 treats `pending` as blocking: four cards were gating work that had shipped.

**Subagent permission prompts, diagnosed and fixed.** `.claude/settings.local.json` is gitignored, and `git worktree add` checks out tracked files only, so worktree-isolated subagents ran with `settings.json` alone and lost all twenty grants — including `Bash(bash:*)`, which guv needs constantly because its scripts are invoked as `bash …/script.sh`. Comparing against `adzeta-guv` isolated it: that project has no hooks, unscoped `Write(**)`/`Edit(**)`, and a `settings.local.json` holding one env var and no permissions, so its agents lose nothing. The deny lists are byte-identical in both projects, so the denials were never the cause.

---

## In Progress

Nothing. The tree is clean and every deliverable is terminal.

---

## Blocked

Nothing is dependency-blocked or human-gated. Three manual cards remain genuinely `pending` and were **not** audited this session — they are unrelated to Phase 14:

- `task-2026-04-25-007` — real-device verification of the mobile matrix
- `task-2026-09-05-001` — Vercel automation bypass secret
- `task-2026-08-31-001` — Claudish launch prerequisites (Model Garden, quota, Anthropic auth)

---

## Issues & Technical Debt

**Carried from the alignment review, by the reviewer's own framing:**

1. **[14.3]'s ownership clause closes with a named gap.** The WIF pool, provider and deployer were created by `gcloud` commands pasted into a manual card and recorded in `docs/verification/2026-09-09-wif-and-ci-armed.md`. A grep for `assertion.repository` or `workloadIdentityPools` across `*.tf` and `*.sh` returns nothing — so the one string preventing any repository on earth from minting a token for an identity the record itself calls close to `roles/editor` was declared nowhere and compared to live by nothing. **Partly closed:** UAT scenario 11 now compares the live attribute condition and the deployer's principal-set binding against the committed expectation. **Owed:** declaring them in Terraform, so they have an owner rather than a transcript.

2. **`settings.json` grants are broader than the fix required.** `Bash(bash:*)` and `Bash(python3:*)` now live in the committed file, so any agent can route around `bash-guard`'s string matching by putting a command in a file. Disclosed in `cbdb824` and hardened on the `gh` side (merge, ref-write and release-create blocked, verified against a seven-case matrix). Narrowing to the invocations actually used is the residual — complicated by guv invoking plugin scripts by absolute path outside the repo, which no repo-relative pattern matches.

3. **Six terraform grants in `settings.json` are inert.** Permission patterns are literal prefixes, and every terraform call in this repo uses `terraform -chdir=…`, which does not begin with `terraform plan`. They will still prompt. Dead config, not a hole.

4. **GTM version churn.** Publishing is not gated on drift, so every merge touching `infrastructure/gtm/**` mints a container version even when nothing changed — version 11 was published from an apply that reported `no drift`. Recorded in `docs/runbook/infra-apply-damaged-production.md` with the instruction not to read a high version number as a change. Gating publish on drift is an open decision.

5. **Scenario 3's consent check should graduate to Playwright.** "Are there NO requests to `google-analytics.com/g/collect`" is a network assertion a script can make, and `tests/e2e/phase-10a-uat.spec.ts` is prior art. It currently asks a human. Rule 8's "recurring mechanical patterns graduate to tests" applies.

6. **A new defect the UAT found on its first run: GA4 enhanced-measurement events are unjoinable.** [14.6] resolves the session key for events the *site* authors, which carry `iap_session_id`. It cannot resolve `scroll`, `user_engagement` and some SPA `page_view`s — auto-collected by the GA4 tag rather than pushed through the data layer, so they arrive carrying neither `session_id` (consumed by GA4 as a reserved name) nor `iap_session_id`, leaving `COALESCE` nothing to fall back on. Measured on real non-generator rows: **742 carry `iap_session_id`, 0 carry `session_id`, 252 carry neither** — 118 `scroll`, 102 `user_engagement`, 12 `page_view`, 4 `form_start`, 1 `form_submit`. They run to 2026-09-09 03:24, so this is a live path, not unrecoverable history, and `stg_events` is a full-rebuild table so it is not staleness either. **Carried for a person to scope:** closing it means either a GTM-side parameter on the enhanced-measurement events or a documented decision that they stay unjoinable.

7. **Two of the ultra review's six confirmed findings remain outstanding**, carried from the previous handoff. They are not recoverable from PR #75, the transcript, or this repo. The gap is procedural: that review's output was read in-session and never written down.

**Guv feedback:** no `.claude/feedback/feedback.ndjson` exists; nothing logged. One friction worth naming: `bash-guard` blocks a *commit message* that quotes the patterns it scans for, which happened three times this session while committing changes to the guard itself. Writing the message to a file and using `git commit -F` is the workaround. The guard is right to be dumb about it — failing closed on a false positive is the correct direction.

---

## Review Results

**Gate audit** (`gate-log.sh audit --session session-2026-09-08-002`): `verdict=uncovered`, exit 3. 69 uncovered, 21 degraded in the audit's own scope, which anchors on HEAD and so reaches back through Phase 12 and 13 commits gated under earlier session ids.

**Two full gates ran in-band this session**, both instruments each time:

| Gate | Commits | Instruments | Result |
|---|---|---|---|
| 13:23 | `7bc7a4a`, `c14e24f` | code-review ✔, guv:reviewer ✔ | gated |
| 14:44 | `183bf19`, `e67916a`, `a7915e3`, `d3dd7c5`, `3f71538`, `d4275e1`, `382cd7f`, `cbdb824`, `67f12bb` | code-review ✔, guv:reviewer ✔ | gated |

**Named as uncovered:** `d500a8e` (the fix for the second gate's own findings) and the render-hook commits `155bf17`, `129e348`, `35dcfcf`, `76ebd4d`, `f661467`, plus the merge commits `80a2ff1` and `55cfd12`. Gating the fix for a gate's findings recurses; naming it is the honest alternative to self-certifying.

**What the gates found, and the pattern in it.** Eighteen findings across the two instruments. The through-line is that **the checks were the defect three times running**:

- The platform review proved, by mutation against the real suite, that my pipefail test matched *its own explanatory comment* — deleting the `shell: bash` key left 15/15 green and restored the exit-status masking the commit had just fixed.
- It also proved the guard-position check was file-order, blind to which job: moving the preflight into `validate` left `terraform apply -auto-approve` unguarded and still passed.
- The alignment review then found the guard itself counted protection rules of **any** type. An environment carrying only a wait timer reports 1, the guard prints a reassuring count, and the apply proceeds unreviewed — while the failure message asserts the property the pass branch never checked. Live state is fine today (one rule, `required_reviewers`), which is exactly the point: nothing is broken, and the check could not detect the change that would break it.

Every fix is mutation-verified by reverting the exact defect: counting any rule → red; checkout hoisted ahead of the guard → red; the two guard copies made to diverge → red; a PR title interpolated into a `run:` block → red.

**A false gate-log entry is disclosed rather than rewritten.** Line 8 records `7bc7a4a` under the `level:"max"` ultra, which reviewed `ba8ba5c..9fc9575` — `7bc7a4a` exists *because* of its findings. Both reviewers found it independently. The log is append-only by contract and its audit's trustworthiness rests on that, so the truthful entry was appended instead.

**UAT vet: `guv:reviewer`, verdict NEEDS WORK — 18 findings, 3 Critical.** Invoked by name and independent of the session that wrote the plan. The vet is declared-not-gated, so it does not block this handoff; the artifact carries the verdict on its QA stamp and Ian decides whether to act before accepting the phase.

The three Criticals were each **this phase's own defect appearing in the artifact meant to certify it**:

1. **A check that could not fail.** Scenario 1's `--container` guard grepped for the word `applying`, which the reconciler never prints on any path. Deleting the safety guard entirely — and letting a bare `--apply` write to a container nobody named — left the check green. It now asserts a nonzero exit *and* the refusal string `reconcile.js:91` actually emits.

2. **Six warehouse checks that passed when `bq` failed.** `verify()` runs its command through `bash -c`, which does **not** inherit `set -o pipefail`, and `awk '{exit !($1>0)}'` on empty input never enters the block and exits 0. Every assertion about the warehouse — the whole of [14.6]'s payoff — silently turned green on any bq failure, and this project's credentials expire roughly hourly, mid-run. **This was not hypothetical: two `[14.4]` checks were passing on a query that errors.** `received_timestamp` is `INTEGER` epoch-millis, not `TIMESTAMP`, so `received_timestamp > TIMESTAMP_SUB(…)` returns *"No matching signature"* — and the first draft reported it as PASS. Replaced with a `bq_scalar` helper that fails loudly and prints the value.

3. **`.protection_rules | length` survived here.** The exact defect removed from both workflows in `d500a8e` hours earlier, in the artifact that certifies those workflows. An environment carrying only a wait timer would report the gate healthy while an apply ran unreviewed.

Majors fixed: the site-authored check filtered by event *name*, which discards the site's own `page_view` and `form_submit` (measured: `page_view` occurs both ways, 54 site-authored and 25 GA4-auto) — now discriminated by `iap_source`; Scenario 6's loop passed on zero files and now counts to 2; the Terraform pins named in [14.2] and [14.5] acceptance were never run because they live in `tests/unit/infrastructure/`, not `tests/unit/infra/`; the Claudish proxy's own `/health` was never probed; the dataform-branch mirror is now compared to `main`; and [14.3]'s headline acceptance — a real merge showing the gate then a successful apply — now has a scenario instead of static file checks.

**Not fixed:** the reviewer's suggestion to graduate Scenario 3's `/g/collect` check into Playwright. It is a genuine mechanical assertion wearing a `confirm()`, and prior art exists in `tests/e2e/`. Carried.

**Draft 2 is itself un-vetted.** It is close to a total rewrite — 50 checks against the first draft's 36 — and no reviewer has read it. The stamp says so. A re-vet is the honest next step before the phase is accepted on its strength.

---

## Test State

**2,258 passing across 192 suites. 0 failing, 0 skipped.** Measured suite runtime 5.347s.

Six tests added this session, all mutation-verified rather than assumed green:

- pipefail is a real YAML key, not prose (anchored `/^\s+shell: bash\s*$/m`)
- the environment guard lives *inside* each job that declares `environment: infra-production`
- that guard checks `required_reviewers`, not any protection rule
- it is the genuinely first step of its job
- the two copies are byte-identical
- no `run:` block compiles an untrusted expression into shell

**UAT:** `docs/uat/phase-14-uat.sh` — 31 passed, 0 failed, 5 human-judgment gates correctly SKIPPED under `GUV_NON_INTERACTIVE`. Verdict INCOMPLETE (exit 2), which is the honest state until a person answers them.

---

## Build State

`npm run build` clean. No TypeScript errors, no `any` introduced, nothing stubbed.

Production surfaces verified live: site 200, `/claudish` 200, `io.iampatterson.com/healthy` 200, `bi.iampatterson.com` 302 (the IAP redirect is the healthy response). `terraform plan` reports *"No changes. Your infrastructure matches the configuration."* Both GTM containers report `no drift`.

---

## Next Steps

1. **Run the UAT interactively** — `bash docs/uat/phase-14-uat.sh`. Five human-judgment gates are owed: the consent gate blocking `/g/collect` when analytics is declined, the overlay's honesty to declining visitors, the rollback runbook's standalone usability, and two on whether the BI layer now shows real rather than synthetic traffic. **Phase 14 is not accepted until these pass.**
2. **Push the branch** — `docs/14.3-guard-verified` carries commits not on `main`, including the guard fix. Until they land, `main`'s guard still counts any protection rule.
3. **Decide the two carried questions:** whether GTM publish should be gated on drift, and whether the GA4 enhanced-measurement events should get a session key or be documented as permanently unjoinable.
4. **Initiative 002 is complete** (`archive-initiative.sh --check` → `status=COMPLETE, max_phase=14`). The next initiative goes through `/guv:plan <spec>`, which archives 12–14 and numbers from 15.

---

## Session Notes

**Budget gate:** no ceiling configured in `.claude/project.json` (`budgets` absent, meaning unlimited), so the exit gate is silent and exits 0. No burn comparison line to carry.

**Metering entry** appended for `14.3,14.6` at `rung=degraded` — the token harvest degraded, which the log's contract permits (it never blocks on harvestability). No `BALLOON:` line: no deliverable exceeded its sizing.

**Phase-boundary forecast banked** at `phase-14` → `.claude/metering/calibration.ndjson`. It records `sized_remaining = 0` and `forecast = null`, which is correct rather than a failure: Phase 14 is the last phase of the initiative, so there is no remaining work to forecast.

**The shape of this session is worth naming.** Phase 14 set out to make infrastructure declarative and found, at every level it looked, the same defect: something committed had drifted from something live, and the check that should have caught it either did not exist or could not fail. That held for the Claudish pipelines, the consent gate, the session warehouse, the BigQuery schema — and then, recursively, for the phase's own guards. The preflight that was written because a *comment* could not verify an external fact turned out to count the wrong thing; the test written to stop a guard regressing matched its own comment; the check generalised to close cross-file blindness left cross-job blindness open. Three of those were found by review instruments rather than by me, and each was proven by mutation rather than argued. The lesson the phase keeps teaching is that a check is worth exactly what its last failed run proves, and a check that has never been seen red is a comment with a green tick.
