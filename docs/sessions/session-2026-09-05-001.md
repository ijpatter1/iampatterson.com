# Session 2026-09-05-001 — Phase 13 opened; five of seven, and most of a sixth

> Continues session-2026-09-04-001 (Phase 12, merged). Session id used in the
> tracker's amendment records is `session-2026-09-04-002`, because the session
> began on 2026-09-04 and ran past midnight UTC; the records the engine stamped
> after the rollover carry 2026-09-05 dates under that same id.

## Completed

**Boundary alignment (8 plan mutations, `a5006f8` and `eb62319` on `main`).**
The `/guv:phase 13` spec-alignment reviewer returned eleven findings. Five became
rewords of 13.1–13.5, applied while the phase is open — the correction the 12.3
erratum asks for. Two became new deliverables. One became a deps amendment.

**[13.6] Terraform layer recovered** — `42e9dc1`, record
`docs/verification/2026-09-05-terraform-layer-recovered.md`. 24 files, +1,676
lines, merged from the never-merged `phase/11-operational-readiness` (PR #56).
Nothing applied. Suite 1,827 → 1,875.

**[13.7] Terraform state reconciled** — `416dcfa`, record
`docs/verification/2026-09-05-terraform-state-reconciled.md`. Plan went from
`0 to add, 1 to change, 6 to destroy` to `No changes`, without an apply. 17 pins,
15 of which fail against the pre-13.7 configuration.

**[13.1] Retention and cost controls** — `1699003`, records
`2026-09-05-retention-and-cost-controls.md` and `2026-09-05-retention-measured.md`.
Three changes applied; everything else confirmed at the value it had. 11 pins.

**[13.2] sGTM image lifecycle** — `4ab6e28`, record
`2026-09-05-sgtm-image-lifecycle.md`. Decision: pin the digest. Rehearsed end to
end on `sgtm-preview`. 9 pins. Runbook entry drafted at
`docs/runbook/sgtm-image-update.md`.

**[13.3] Dependency cadence** — `d5e9388`, record `2026-09-05-dependency-cadence.md`.
Nine Dependabot entries; cadence at `docs/runbook/dependency-cadence.md`. 12 pins.

## In progress

**[13.4] Cloud Run adoption — identity and scaling applied and verified; the
proxy import staged, not applied** (`ffc3109`, record
`2026-09-05-cloud-run-adoption-measured.md`).

Permission to run the record's commands was given explicitly after the concern
had been raised twice, and they were run.

Done and proved: sGTM to `maxScale=10`; four dedicated runtime accounts created
with least privilege and all four services moved onto them, so **zero of six
Cloud Run services remain on the default compute account** that holds
`roles/editor`. Each move was proved by sending events through the pipeline and
counting rows — `data-generator` produced exactly +712 in `events_raw`, `sgtm`
exactly +117 — rather than by a health endpoint, which cannot tell you whether a
service can still write. `cloud-run.tf` synced and returned to a clean plan.

Two things worth carrying out of it. `iampatterson_raw` grants write through the
`projectWriters` special group, so moving a service off `roles/editor` without an
explicit dataset entry would take its write path silently. And `data-generator`
had traffic pinned to a revision: updating its service account created a new
revision and left traffic on the old one, so the spec reported the new identity
while the serving revision still ran as default compute. It looked migrated and
was not.

Outstanding: one `terraform apply` to persist six staged imports (`6 to import,
0 to add, 0 to change, 0 to destroy` — refused by the environment guard, so
13.7's clean plan is intentionally suspended); the Secret Manager and IAM halves
of `IMPORT_PLAN.md`, whose modules do not exist in this root; removing
`roles/editor` from the default compute account, deliberately left because Cloud
Build may still need it; and a 30-day abort re-count against the 96 recorded
before.

## Blocked

**[13.5] Operational runbook** — blocked on 13.4 and not started. Beyond the
formal dependency, its acceptance asks for a rehearsal per entry, which means
breaking things on purpose in production; that is the class of action the
environment guards and that should not run unattended. Two of its twelve entries
already exist, drafted by 13.2 and 13.3.

## Not met, and named

**13.3's acceptance clause "at least one update pull request produced"** cannot be
checked from a branch: Dependabot reads its configuration from the default
branch. Checkable within a day of merge. Recorded in the tracker entry and the
record rather than counted as done.

**Drafted for ratification, not applied** (`/guv:replan` headless path): amending
that clause to "committed, and verified after merge". The mutation is a reword of
13.3's acceptance; it needs a person to confirm.

## Findings worth carrying

**The 12.3 `cloud_run_no_instance` metric cannot answer questions about the past.**
Log-based metrics are not retroactive. Created 2026-09-04, queried over seven
days it returns 1 abort and reads like an all-clear; the logs show 13 in seven
days and 96 in thirty. Any "how often did this happen" question goes to the logs.

**The default compute service account holds `roles/editor`**, and four services
run as it. `event-stream` needs no roles at all — it carries no `@google-cloud/*`
dependency and constructs no client.

**Two direct high-severity advisories.** `next` (denial of service via Server
Components, production-reachable) and `postcss`. Both have semver-compatible
fixes. Deliberately not fixed inside 13.3; named as the cadence's first action.

**Last night's Dataform run succeeded.** 2026-09-05 04:00 UTC SUCCEEDED, the
first run with the corrected `assert_volume_anomaly`. That closes the open
question from the previous handoff. The two runs before the merge also succeeded,
which is consistent with the gap rate oscillating across its threshold as days
aged out — the behaviour Phase 12 diagnosed.

**The remote branch is linear.** Pushes go through the Git Data API, which
replays one single-parent commit per first-parent commit, so 13.6's merge arrives
flattened. The eleven Phase 11 commits stay reachable on
`origin/phase/11-operational-readiness`.

## Test state

1,942 passing across 178 suites, 0 failing, 0 skipped. `npm run lint` clean.
`terraform validate` clean. `terraform plan` reports `6 to import, 0 to add,
0 to change, 0 to destroy` — import-only, with 13.7's destroy set still empty.
`infrastructure/monitoring/apply.sh --dry-run apply` still 29 unchanged.

One Phase 12 assertion was updated rather than deleted: `log-metrics-spec.test.ts`
pinned the `_Default` retention as *provisional* to protect the 12.3 → 13.1
handoff. 13.1 honoured it, so the assertion now pins the confirmation.

## Next steps

1. **One `terraform apply`** in `infrastructure/terraform/`, to persist the six
   staged imports and return the plan to clean. It changes no resource.
2. **Update production `sgtm` to the current image digest** —
   `bash infrastructure/sgtm/update-image.sh update sgtm`. Preview has run the
   new digest healthily since 2026-09-05.
3. **The `next` advisory**, as its own reviewed change with a build check.
4. **Consider removing `roles/editor`** from the default compute account. Nothing
   in Cloud Run needs it now; check Cloud Build first.
5. **Then 13.5**, which unblocks when 13.4 closes. Its rehearsals want a person.
6. **Close PR #56** rather than merging it.
7. After merging PR #62, confirm a Dependabot pull request appears, closing
   13.3's outstanding clause.

## Ownership

PR #62 is open as a draft against `main`. Merging is Ian's. `main` carries the
two plan-mutation commits and their render commits; the branch carries the rest.
