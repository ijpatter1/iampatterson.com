# Feedback

## UAT Ecommerce Feedback r2
Desktop and mobile (360x640 combined)
The Preview - Sample Event box in the Demo section is pointless. The homepage is lacking some visuals, should we just use a picture here? My concern is that the section becomes too long for mobile. Would like your input here.
When clicking on "Enter the Demo", randomly assign utm values for the utm capture box on the shop homepage. And is there a way of having the classification pull from live values, or perform live classification? Or should we use a representative value and flag that to the user?
Put the footer text at the top "THIS IS A DEMO". If anything, this should be kept at the top as a persistent reminder.
Make the utm capture box the same style as the underside overlay, aka amber and terminal black
increase font sizes on shop homepage, the header and body copy need to be the same size as the homepage copy (but not the same size as the hero, that's too big)
Remove "everything in the shop", it's pointless fluff.
On the mobile homepage, I think the products need to be swipable left to right rather than a long page scroll.
each screen needs a blurb, like a walkthrough/demo guide. It can be collapsable, all this has to fit on a mobile screen. The data sidebars just lack some introduction. The blurbs in the data sidebar aren't obvious enough. "What am I looking at" is the reaction I anticipate, so let's get ahead of it.
The data sidebar copy is getting clipped on the right side.
On mobile cart page, the data sidebar is getting lost at the bottom of the page. we need a clever UI solution, thoughts?
This is a new feature, but it relates to my previous experience working with CDPs. Should we implement an anonymous_id first party cookie for long term user identification? or in localstorage?
On mobile, the checkout flex box items are spilling past the viewport.
Same issue with the the data sidebar is getting lost at the bottom of the page on mobile checkout page
The lack of caps at the beginning of sentences looks unprofessional: "thanks. the event made it all the way through."; "your $26.00 order just landed in production BigQuery and is rolling into today's revenue below. the dashboard ops reads in the morning is what you're looking at."
"840ms end-to-end" what is this based on?
The dashboard below the fold, and it's even below the "dashboards are not the payoff. answers are. the mart layer is what makes the answers trustworthy. read about the services →" CTA. This flow is conflicting with the dashboard being the demo payoff.