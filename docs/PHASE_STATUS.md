# Phase Status Tracker

> **Current Phase: 9E — Navigation & Overlay Pivot (dev-complete, UAT closed) + 9F — Ecommerce Demo Native-Reveal Rebuild (kickoff — doc pass complete, implementation pending).** Joint ship per the 9E release-coupling. 9E side: all 9 deliverables shipped (D1–D9), UAT performed, eight-block close-out landed, three user-reported follow-ups resolved (`af3a7ad` refresh-persistence audit, `0751a34` hydration-error revert, `c53f924` threshold + dismissal-mode fixes), four full dual-evaluator cycles during F8, test suite 820 → 863 (+43 net). 9F side: branch `phase/9f-ecommerce-native-reveal` cut from current `phase/9e-nav-overlay-pivot` HEAD; doc pass complete (REQUIREMENTS Phase 9F block + ARCHITECTURE Phase 9F section + this PHASE_STATUS Phase 9F block) reconciling canonical `docs/UX_PIVOT_SPEC.md` §3.5 against the hi-fi prototype at `docs/input_artifacts/design_handoff_ecommerce/`. 12 deliverables drafted (4 pattern primitives, 5 per-page rebuilds, component cleanup, SessionPulse reachability verification, palette-token harmony + Tuna Shop brand treatment). Confirmation-page embed-shape decision locked: **one full-dashboard Metabase embed** (user-confirmed 2026-04-21). Cold-start ship-gate upgraded to binary release blocker. **Original 9E status (preserved as historical context):** All 9 deliverables (D1–D9) shipped. UAT performed, eight-block close-out landed, then three user-reported browser-only issues resolved in follow-up commits: full refresh-persistence audit (`af3a7ad` — every SSR'd consumer of session data now reads from the persisted blob + a new timeline ring buffer), hydration-error revert (`0751a34` — eager-loads caused SSR/CSR text-content mismatches; reverted to post-mount useEffect loading with brief flash as trade-off), and threshold + dismissal-mode fixes (`c53f924` — restored threshold CTA `EVENT_TYPE_THRESHOLD` from 5 → 10 per spec after user reported 5 triggered too easily; removed `click_session_pulse` from `NavHintDismissedEvent.dismissal_mode` union because SessionPulse clicks are conversions tracked by `click_cta`, not dismissals). Four full dual-evaluator cycles during F8; final fixes were focused user-directed changes with direct regression pins. Test suite 820 → 863 (+43 net). Branch `phase/9e-nav-overlay-pivot` is shippable as the 9E half of the joint 9E+9F release cut. D8 contact-form transport stub carried to Phase 10 as documented. UAT artifacts: `docs/uat/phase-9e-uat.sh` + `phase-9e-uat-output.md` + `phase-9e-uat-feedback.md`. Session handoffs: `docs/sessions/session-2026-04-21-001.md` (UAT close-out + follow-ups).
>
> **Deferred to Phase 10 (from F8 product review):**
> 1. Mobile overlay swipe-down close gesture (Product Major #1) — Escape-key close is a desktop affordance; iOS-native swipe-down would complement "← Back to site" on mobile. Net-new feature, not UAT close-out scope.
> 2. Real-device iPhone SE CTA-above-fold verification (Product Minor #5) — math says CTAs land above the 667px fold, but Safari chrome eats ~130px leaving ~535px usable. Requires hardware verification.
> 3. Further pipeline compression on mobile (Product Major #2) — A+B compression saved ~50% of the "three screens" complaint; user signaled accept after F6 follow-up. Available future lever: tighter mobile detail copy on stages 03 + 05 specifically.
> 4. Homepage "returning soon" footnote without CTA (Product Minor #4) — "Subscription and lead gen demos · returning soon" line dangles. Either remove (since rebuild banner handles redirected traffic) or add a "notify me" email capture.
> 5. Sparse mobile overlay header (Product — sensibility note) — mobile shows only "Session" title + close button; subtitle hidden. Might read too sparse; consider a short mobile-only tagline.
>
> **UAT close-out summary (F1–F7):**
> - **F1** (commit `0975bce`) — Language consolidation: `your session` / `Session` / `Overview` vocabulary replaces the fragmented "Under the Hood" / "Watch it live" / "Flip" / "Watch it run first" / "Look under the hood" / "menu · under the hood" / "NAV · UNDER THE HOOD" set. Renamed: `OverlayTab.session_state` → `overview`; `SessionStateTabViewEvent` → `OverviewTabViewEvent`; event literal `session_state_tab_view` → `overview_tab_view`; `trackSessionStateTabView` → `trackOverviewTabView`; `CtaLocation.pipeline_watch_it_live` → `pipeline_see_your_session`; `CtaLocation.footer_under_the_hood` → `footer_session`; file `session-state-tab.tsx` → `overview-tab.tsx` + matching test files. Hover tooltip removed (instrument-as-nav stands on its own). `SessionState` domain data model + provider + storage directory preserved (correct domain name, not surface label). Tests 820 → 818.
> - **F2** (commit `de189db`) — Overview tab restructure + per-tab view events: portals + threshold CTA moved to TOP of tab; session header + consent summary render side-by-side on md+ (2-col grid); event coverage + ecommerce funnel stay full-width. All three tab labels render `text-accent-current`; active state is opacity-100 + border-accent-current + `[ LABEL ]` framing, inactive is opacity-60 + border-transparent. New `RENDERABLE_EVENT_NAMES` subset in schema excludes the 4 sub/leadgen event types; chip grid iterates RENDERABLE (denominator drops N/24 → N/20). New `TimelineTabViewEvent` + `ConsentTabViewEvent` parallel to `OverviewTabViewEvent` — each tab gets its own coverage chip; `DATA_LAYER_EVENT_NAMES` 22 → 24. Emission uses a `TAB_VIEW_TRACKERS` map in `overlay-view.tsx` (post-F8 rename; F2 originally landed this in `under-the-hood-view.tsx`) for symmetry. WCAG 2.2 a11y: section kickers text-[10px] → text-xs (rem-based); portal descriptors bumped to text-sm; consent rows text-xs → text-sm; chip grid kept text-xs with `truncate`. Tests 818 → 834.
> - **F3** (commit `a1ea7eb`) — Overlay close affordances: the pre-F3 `-z-10` backdrop button was permanently occluded by flex-column flow children (never received a click). Removed + replaced with Escape-key close (standard modal pattern). Tab A reload repro test added — comprehensive field-by-field SessionStateProvider rehydration pin confirms the code path works correctly. UAT observation was likely the brief `state=null` → hydrated render transition showing the "Warming up session state…" placeholder. Tests 834 → 836.
> - **F4** (commit `4f7b4a0`) — Rebuild banner SSR/CSR hydration fix: pre-F4 the `useState(isRebuildBannerDismissed(...))` initializer called `sessionStorage.getItem()` synchronously, producing a hydration mismatch inside the Suspense boundary on deep-link redirects (UAT S5.3). Replaced with tri-state `dismissed: boolean | null` + post-mount useEffect. First paint always matches server output (null) regardless of storage contents. Deep-link dismissal-persistence pin added. Tests 836 → 838.
> - **F5** (commit `2d11edd`) — Mobile layout bundle (S11 + S2): SessionPulse `justify-end md:justify-start` (top-right on mobile, left-adjacent on desktop). NavHint ring animation scale 2.5 → 1.8 so it doesn't clip past viewport edge. Tab bar `overflow-x-auto` + per-tab `whitespace-nowrap flex-shrink-0` + mobile padding tightening. Event chips `min-w-0 truncate whitespace-nowrap` (fixes spill + wrap). Hero mobile compression: h1 floor 56px → 44px; italic kicker 22px → 18px; body text-base → text-[15px]; section padding and grid rhythm tightened — CTAs now above iPhone SE fold. Overlay header declutter: icon SVG + "Live · yours, right now" subtitle gated `hidden md:flex`/`md:block`; mobile shows only "Session" title + close button. Demos-section secondary tagline gated `hidden md:block`. New `HomeBar` slim "Back to homepage" link rendered below LiveStrip on non-homepage routes. Tests 838 → 843.
> - **F6** (commit `aaf4ce3`) + follow-up (commit `647453d`) — Pipeline polish: dropped dense instrument-panel metadata (`tech` / `sub` / `reads[]`) from `PipelineStage`; replaced with editorial one-liner `detail` under each stage title. `renderDetailLine()` helper parses backtick-fenced tokens for inline mono `<code>` emphasis. Removed the `<dl className="pv-edit__reads">` block + max-height collapse animation — eliminates mobile jitter entirely. Only load-bearing identifier anchor kept in prose: `iampatterson_raw.events_raw` on Stage 04. Container IDs + sGTM subdomain + all illustrative numerics cut (prose carries the weight; overlay's Timeline event-detail already surfaces the real routing metadata). New `pipeline-bleed-consumed.ts` module + sessionStorage key `iampatterson.pipeline_bleed.consumed` — set by `OverlayProvider.open()` + `.toggle()` (closed→open edge); pipeline-section reads on mount and on isOpen edge to tear down the rAF loop + flicker scheduler after first overlay open. Follow-up fix for "animation persists after overlay close" bug: rAF + flicker effects now deps on `isOpen`, cleanup tears down previous loop; new visual-reset effect zeroes `--bleed` + state so section returns to calm, not frozen mid-ramp. Mobile compression pass (A+B from workshop): section padding pt-20/pb-100 → pt-12/pb-16 on <md; h2 mobile floor 36px → 28px; stage py-[18px] → py-3 md:py-6; stage title floor 22px → 20px; detail prose text-[15px] → text-[14px] md:text-[15px]; pv-host + CTA spacing tightened; footnote feed hidden on mobile via `hidden md:block` (4-row mono log duplicates SessionPulse's event counter anyway). Estimated ~450px mobile saving. Tests 843 → 850.
> - **F7** (current) — Housekeeping: UAT script `verify_http` fixed (dropped the `-L` flag that caused the `308000` garbage concatenation; expected status changed 301 → 308 to match Next.js `permanent: true` semantics). `CLAUDE.md` project identity line corrected ("three embedded demo environments" → "single embedded ecommerce demo"). This PHASE_STATUS entry extended with the F1–F7 summary.
>
> **F8 remaining:** dual evaluator pass across the F1–F7 scope before branch push + 9F dev start.
>
> **Release coupling:** 9E ships jointly with Phase 9F (Ecommerce Demo Native-Reveal Rebuild). 9E does not cut a production release on its own — the two phases are authored and evaluated as separate blocks for tracking clarity, but the release is one branch cut when both are dev-complete and dual-evaluator-passing. Shipping 9E alone would land the new nav / overlay / Session State tab / pipeline reveal on top of today's overlay-based ecommerce reveals, which is the disjointed experience the pivot exists to eliminate.
>
> **Cancellations:** Phases 9C and 9D are cancelled. The subscription and lead gen demos are removed from the site in 9E (with permanent 308 redirects per deliverable 7; Next.js emits 308 for `permanent: true`, not 301 — originally documented as 301 and corrected in UAT F7). Reintroduction happens as separate, dedicated later phases — deliberately sequenced after 9F so the cancellation doesn't re-invite the scope-creep pattern of the last two redesigns. See the "Strategic framing" paragraph in the Phase 9E REQUIREMENTS block for the honest tradeoff.
>
> **Source of truth:** `docs/UX_PIVOT_SPEC.md` for the design spec; `docs/input_artifacts/design_handoff_pipeline/` for the pipeline-section redesign bundle.
>
> **Prior phase state (preserved as baseline):**
> Phase 9B — E-Commerce Demo: Tiers 2 & 3 is dev-complete. 6a verified live on Metabase at `https://bi.iampatterson.com/dashboard/2`; 6b shipped across the signer utility → inline iframes + Server-Component page refactor → overlay Dashboards-tab fallback, then five evaluator/product-reviewer iterations (Pass 1-5) closing loading-state, typography, a11y, sanitization, prose-overclaim and IAP-gate findings; services cross-links shipped at commit 08c5458. 673 tests passing, build clean. Pass 5 final scores: Tech 4.60/5, Product 4.55/5 — both PASS, merge-ready.
>
> **9B follow-ups still outstanding (re-evaluate for 9E/9F relevance):**
> 1. **[High priority, operational]** Metabase Cloud Run CPU always-allocated. Surfaced during session-026 live iframe verification: first iframe load after idle takes ~60s. `minScale=1` is set but `cpu-throttling=true` (default) drops CPU to near-zero during idle, forcing JVM warmup on the next hit. Fix: `gcloud run services update metabase --region=us-central1 --project=iampatterson --no-cpu-throttling`. Cost delta ~$20/mo. Still relevant — Phase 9F confirmation-page embeds will hit the same cold-start path.
> 2. `LiveEmbedFrame` load-failure timeout — if Metabase is unreachable, "Querying BigQuery…" persists indefinitely. Add a ~15s `useEffect` timer that flips the placeholder to a retry/open-direct CTA. Still relevant for the 9F confirmation-page rebuild.
> 3. Palette-token harmony — demo surface uses raw `neutral-*`; editorial surface uses `ink/paper/rule`. Needs a unified pass. Absorbable into Phase 9F (demo surfaces get touched again for the native-reveal rebuild).
> 4. Load-order tab reshape — with three `LiveEmbedFrame` instances, tab order reshapes as each iframe's `onLoad` fires in arbitrary order. Low severity; re-evaluate against the Phase 9F confirmation-page embed shape (single full-dashboard embed would eliminate the issue).
> 5. Zombie no-query-params state at `/demo/ecommerce/confirmation` — renders "Order confirmed · ORD-UNKNOWN" + `$0` receipt. Fix if/when the 9F rebuild introduces a deep link that omits params.
>
> **Before Phase 9B (6b) renders in production:** set `MB_EMBEDDING_SECRET_KEY` + `METABASE_EMBED_CONFIG` in Vercel (mirrors of Secret Manager `metabase-embedding-secret-key` + `metabase-embed-config`). Without them, the confirmation page renders a visible fallback block deep-linking to the IAP-gated dashboard. This carries through into Phase 9F — the signer remains the production path for whichever embed shape 9F lands on.
>
> Last updated: 2026-04-20, session-2026-04-20-031 (Phase 9E dev-complete (9/9) — D5 + D8 both landed this session with dual-evaluator convergence to CLEAN on tech + PASS on product. D5 took 4 eval passes; D8 took 3 passes. D8 shipped as optional with a Phase 10 follow-up captured for the contact-form transport stub. Test suite 746 → 820 across the session. Branch ready for joint 9E+9F release cut. Previously, D5 4-pass dual-evaluator convergence to CLEAN on both axes — Tech 5.00/5, Product 4.75/5. Pipeline section progressive bleed-through reveal: scroll-anchored ramp anchored to section's own height; tier state machine warm/hot/peak; four CRT sibling layers; flicker scheduler bleed-coupled; reduced-motion gating end-to-end with mobile readout-expansion fallback; real GTM container IDs + sGTM hostname + BQ raw table verified against `infrastructure/gtm/*.json`; footnote feed reads `useLiveEvents` per-event timestamps; session ID matches SessionPulse last-6 convention. 4 commits: `5afe675` feat → `986d857` (Pass 1) → `dad0543` (Pass 2) → `55bb416` (Pass 3). Test suite 746 → 796 (+50 net across 4 D5 test files). Previously, in session-2026-04-20-030 (Phase 9E Deliverables 1, 2, 6, 7 dev-complete. D2 dual-evaluator CLEAN Pass 4: overlay restructure with 3-tab set (Session State → Timeline → Consent), terminal bracket framing on active label, default_landing/manual_select orthogonality (Pass 1 dual-fire fixed via landingResolvedRef unconditional-close invariant), HomepageUnderside + DashboardView + EcommerceUnderside router deleted. D7: subscription + leadgen demos removed; 7 routes + 8 components + 4 test files deleted; `DemoFooterNav` simplified to single back link; 301 redirects wired in `next.config.mjs` (`/demo/{subscription,leadgen}/:path*` + bare → `/?rebuild=<x>#demos`, `permanent: true`); subscription/leadgen event types preserved in schema for Session State denominator. D6: homepage `DemosSection` rebuilt as single full-width editorial section with eyebrow, serif headline ("Watch a purchase become a KPI."), terminal-styled preview column, Suspense-wrapped `RebuildBanner` reading `?rebuild=` param (Object.create(null) allowlist for prototype-chain safety, per-label sessionStorage dismissal). D1: conventional nav removed; SessionPulse forwardRef + hover tooltip "NAV · UNDER THE HOOD" + ↗ glow + min 44×44 hitbox + desktop-only `md:animate-session-pulse-strong` variant + aria-describedby linkage; `session_pulse_hover` emission debounced 60s with coarse-pointer suppression; `NavHint` with four-mode dismissal state machine, 3s idle trigger / 10s auto-clear / 6s reduced-motion fade, `iampatterson.nav_hint.shown` sessionStorage gate, homepage-entry-scoped; `MobileSheet` deleted. Test suite 783 → 746 (D7 shed 49 tests with the demos, D1+D6+M3/M4/M5 additions offset). Remaining: D5 (pipeline progressive bleed-through) and D8 (contact form ride-along, deferred by default).)

---

## Phase 1 — Foundation

*Goal: A functioning Next.js site with basic consulting content, Cookiebot, client-side GTM, and sGTM deployed.*

- ✅ 2026-03-26, session-2026-03-26-001 — Next.js project scaffolded with TypeScript (strict) and Tailwind CSS
- ✅ 2026-03-26, session-2026-03-26-001 — Site structure: homepage, services overview (the four tiers), about/background, contact
- ✅ 2026-03-26, session-2026-03-26-001 — Data layer event schema defined and wired into all UI components (page_view, scroll_depth, click_nav, click_cta, form events)
- ✅ 2026-03-26, session-2026-03-26-002 — Cookiebot CMP integrated with Consent Mode v2 defaults and consent_update event handler (code-side complete; Cookiebot ID configured)
- ✅ 2026-03-26, session-2026-03-26-002 — Client-side GTM container configured with sGTM same-origin transport (code-side complete; GTM ID configured)
- ✅ 2026-03-26, session-2026-03-26-002 — sGTM container on Stape with custom domain (io.iampatterson.com), DNS verified, GA4 forwarding tag active
- ✅ 2026-03-26, session-2026-03-26-002 — GA4 configured via sGTM (G-9M2G3RLHWF), GA4 forwarding tag with all event/user params
- ✅ 2026-03-26, session-2026-03-26-002 — BigQuery event sink configured: "Write to BigQuery" tag in sGTM writing to `iampatterson.iampatterson_raw.events_raw` with "All Event Data" mode
- ✅ 2026-03-26, session-2026-03-26-001 — Basic SEO and performance optimization
- ✅ 2026-03-26, session-2026-03-26-002 — Deployed to production on Vercel at https://iampatterson-com.vercel.app/
- ✅ 2026-03-26, session-2026-03-26-002 — Web GTM container config spec + importable container (`infrastructure/gtm/web-container.json`, `web-container-import.json`)
- ✅ 2026-03-26, session-2026-03-26-002 — sGTM container config spec + importable container (`infrastructure/gtm/server-container.json`, `server-container-import.json`)

---

## Phase 2 — Real-Time Event Pipeline

*Goal: Build the infrastructure that allows a visitor's session events to be streamed back to their browser in near real time.*

- ✅ 2026-03-26, session-2026-03-26-004 — Session ID mechanism: client-side `_iap_sid` cookie (UUID v4, 30-min sliding window) with `setSessionCookie`, `readSessionCookie`, `getSessionId` utilities
- ✅ 2026-03-26, session-2026-03-26-004 — Pub/Sub topic configured (`iampatterson-events`) with push subscription, sGTM Pub/Sub tag promoted to main config with full message payload spec
- ✅ 2026-03-26, session-2026-03-26-004 — Cloud Run SSE service: Express/Node.js with ConnectionManager, Pub/Sub push endpoint, session-scoped SSE routing, health check, Dockerfile
- ✅ 2026-03-26, session-2026-03-26-004 — Client-side `useEventStream` hook: SSE connection scoped by session ID, in-memory PipelineEvent buffer (most recent first, cap 100), connection status, clearEvents
- ✅ 2026-03-26, session-2026-03-26-004 — Pipeline event schema: PipelineEvent, ConsentState, RoutingResult, RoutingDestination types, isPipelineEvent type guard, createPipelineEvent factory
- ✅ 2026-03-27, session-2026-03-27-005 — Infrastructure deployed: Cloud Run SSE service, Pub/Sub topic + push subscription, sGTM Pub/Sub custom tag template with tests
- ✅ 2026-03-27, session-2026-03-27-005 — End-to-end pipeline verified: browser → GTM → sGTM → Pub/Sub → Cloud Run → SSE → browser

---

## Phase 3 — Flip-the-Card UI

*Goal: Build the overlay component system that lets visitors see the instrumentation layer on the consulting site pages.*

- ✅ 2026-03-27, session-2026-03-27-006 — Persistent flip trigger: fixed bottom-right button with OverlayProvider context for open/closed state
- ✅ 2026-03-27, session-2026-03-27-006 — Mobile bottom sheet component: pull-up with drag handle, swipe-down dismiss (>80px), backdrop, max 85vh
- ✅ 2026-03-27, session-2026-03-27-006 — Desktop overlay component: right-anchored w-96 sidebar with backdrop blur, hidden on mobile (md:flex)
- ✅ 2026-03-27, session-2026-03-27-006 — Event timeline renderer: reverse-chronological list with routing destination badges (sent/blocked/error styling)
- ✅ 2026-03-27, session-2026-03-27-006 — Event detail panel: full pipeline view — data layer parameters, consent state, routing confirmations with status badges
- ✅ 2026-03-27, session-2026-03-27-006 — Narrative flow visualization: "You did X → Data Layer → sGTM → Destinations" step-by-step cards with arrow connectors
- ✅ 2026-03-27, session-2026-03-27-006 — Consent-specific overlay: consent signal cards (granted/denied), active/suppressed destination lists, descriptions per signal
- ✅ 2026-03-27, session-2026-03-27-006 — Page context awareness: useFilteredEvents hook reads Next.js route, filters events by page path with demo namespace support

---

## Phase 4 — Background Data Generator

*Goal: Build a service that continuously generates realistic historical and ongoing data for the three demo business models.*

- ✅ 2026-03-28, session-2026-03-28-009 — Cloud Run service (Node.js/TypeScript) sends events to sGTM via /g/collect (gtag.js format), deployed to Cloud Run with Express server and Dockerfile
- ✅ 2026-03-28, session-2026-03-28-009 — Three data profiles: e-commerce (Tuna Shop — 6 products, funnel with configurable drop-off), subscription (Tuna Subscription Box — 3 plans, lifecycle with churn curves), lead generation (Tuna Brand Partnerships — form funnel with qualification scoring)
- ✅ 2026-03-28, session-2026-03-28-009 — Simulated ad platform data: campaign spend, impressions, clicks by platform, campaign, and date. Includes intentional naming inconsistencies (3-4 UTM variants per campaign) across platforms
- ✅ 2026-03-28, session-2026-03-28-009 — Historical backfill: 18-month backfill deployed and executed for all three models, streaming day-by-day with 30 concurrent requests. Data verified in BigQuery
- ✅ 2026-03-28, session-2026-03-28-009 — Ongoing generation: Cloud Scheduler jobs configured (3 jobs, staggered 02:00-03:00 UTC daily) with OIDC auth to Cloud Run
- ✅ 2026-03-28, session-2026-03-28-009 — Configurable parameters: traffic volume, channel mix (6 platforms), conversion rates, seasonality curves, growth rate, backfill months — all adjustable via GeneratorConfig

---

## Phase 5 — Data Infrastructure (Tier 2 Demonstration)

*Goal: Build the full Dataform transformation layer, campaign taxonomy, data quality framework, and AI access layer.*

- ✅ 2026-03-28, session-2026-03-28-011 — BigQuery dataset structure: `iampatterson_raw` (expanded to 51 cols), `iampatterson_staging`, `iampatterson_marts`. Ad platform table `ad_platform_raw` created
- ✅ 2026-03-28, session-2026-03-28-011 — Dataform repository with full medallion architecture: 3 staging models (`stg_events`, `stg_sessions`, `stg_ad_platform`), constants module, project config
- ✅ 2026-03-28, session-2026-03-28-011 — Mart models: `mart_campaign_performance` (ROAS), `mart_channel_attribution`, `mart_customer_ltv` (by client_id), `mart_session_events`, `mart_subscription_cohorts`, `mart_lead_funnel`
- ✅ 2026-03-28, session-2026-03-28-011 — Automated Campaign Taxonomy: AI.CLASSIFY model + rule-based regex fallback (tested live, 100% classification on 33 campaign variants). Validation table with event volumes
- ✅ 2026-03-28, session-2026-03-28-011 — Automated Data Quality Framework: 6 assertions — schema validation, null checks, purchase revenue, subscription fields, volume anomaly, source freshness
- ✅ 2026-03-28, session-2026-03-28-011 — Data Dictionary: BQ table from INFORMATION_SCHEMA + exportable DATA_DICTIONARY.md with column-level descriptions
- ✅ 2026-03-28, session-2026-03-28-011 — AI Access Layer: GCS export scripts (setup.sh, export.sh), read-only service account setup, 30-day lifecycle policy
- ✅ 2026-03-28, session-2026-03-28-011 — RAG infrastructure: text-embedding-005 + Gemini 2.0 Flash models, vector embeddings on 3 mart tables, IVF indexes, `semantic_search` and `query_business` stored procedures

---

## Phase 6 — Three Demo Front-Ends

*Goal: Build the three mini-sites within iampatterson.com, each representing a different business model and fully instrumented.*

- ✅ 2026-03-29, session-2026-03-29-012 — E-commerce demo (Tuna Shop): product listing (6 products), product detail pages with product_view, cart with add_to_cart, checkout with begin_checkout + purchase, order confirmation
- ✅ 2026-03-29, session-2026-03-29-012 — Subscription demo (Tuna Subscription Box): landing page with 3 plans, plan_select events, trial signup form with trial_signup, account dashboard with upgrade/downgrade/cancel
- ✅ 2026-03-29, session-2026-03-29-012 — Lead gen demo (Tuna Brand Partnerships): landing page, multi-select partnership inquiry form with form_start/form_field_focus/form_complete/lead_qualify, thank you page
- ✅ 2026-03-29, session-2026-03-29-012 — Each demo has its own data layer specification (8 event types) matching the background generator's output
- ✅ 2026-03-29, session-2026-03-29-012 — Route namespaces (/demo/ecommerce, /demo/subscription, /demo/leadgen) with shared DemoNav, demo landing page, Demos dropdown in header
- ✅ 2026-03-29, session-2026-03-29-012 — Flip-the-card overlay from Phase 3 works on all three demos via useFilteredEvents route-aware filtering
- ✅ 2026-03-29, session-2026-03-29-012 — Navigation between demos and back to the main consulting site is seamless (DemoNav + header Demos dropdown)
- ✅ 2026-04-02, session-2026-04-02-014 — Web GTM container updated with 18 DLV variables, 8 custom event triggers, 8 GA4 event tags for demo events (product_view, add_to_cart, begin_checkout, purchase, plan_select, trial_signup, form_complete, lead_qualify). Published as version 7
- ✅ 2026-04-02, session-2026-04-02-014 — sGTM container updated with ce-conversions trigger, Meta CAPI Simulation and Google Ads EC Simulation custom templates + tags. Published as version 13

---

## Phase 7 — BI Layer & Demo Dashboards (Tier 3 Demonstration) (COMPLETE)

*Goal: Build the dashboard and reporting layer for each business model, embeddable or linkable from the demo back-end views.*

- ✅ 2026-04-03, session-2026-04-03-015 — E-commerce dashboards: executive summary (revenue, AOV, conversion rate by channel), campaign performance (spend vs revenue with AI-classified taxonomy), product performance, acquisition funnel. Built with Recharts on mock data matching BigQuery mart schemas
- ✅ 2026-04-03, session-2026-04-03-015 — Subscription dashboards: cohort retention curves, trial-to-paid conversion by channel, MRR/ARR trending, churn analysis, LTV by acquisition source
- ✅ 2026-04-03, session-2026-04-03-015 — Lead gen dashboards: lead funnel (visits → form starts → submissions → qualified leads), cost per lead by channel, lead quality distribution, conversion timeline
- ✅ 2026-04-03, session-2026-04-03-015 — Dashboard embed/deep-link integration with flip-the-card UI: Dashboards tab in overlay, contextual Analytics link in DemoNav

---

## Phase 8 — Frontend Redesign (COMPLETE)

*Goal: Transform the site from a text-heavy layout into a scroll-driven, visually distinctive portfolio experience while preserving all existing event tracking and backend functionality.*

- ✅ 2026-04-03, session-2026-04-03-016 — Design system overhaul — color palette (midnight navy brand), typography (Space Grotesk + Inter), spacing, shadow, animation tokens in Tailwind config
- ✅ 2026-04-03, session-2026-04-03-016 — Scroll-driven homepage rebuild — full-viewport sections, demo spotlights as gallery with per-demo color identity
- ✅ 2026-04-03, session-2026-04-03-016 — Scroll animation system — Framer Motion ScrollReveal component with fade/slideUp/slideLeft/scale variants, reduced-motion support
- ✅ 2026-04-03, session-2026-04-03-016 — Pipeline visualization homepage section — 5-step pipeline teaser (superseded by Phase 9A live visualization)
- ✅ 2026-04-03, session-2026-04-03-016 — Demo visual differentiation — DemoThemeProvider context with per-demo color accents (amber/teal/indigo)
- ✅ 2026-04-03, session-2026-04-03-016 — Dashboard integration into demo flow — DashboardPreview component with KPI cards and narrative at bottom of key demo pages
- ✅ 2026-04-03, session-2026-04-03-016 — Navigation redesign — sticky header with scroll transform and dark-hero awareness, dark footer, DemoNav analytics link removed
- ✅ 2026-04-03, session-2026-04-03-016 — Services page visual hierarchy — full-width tier sections with numbered badges, two-column layouts, dark Tier 4
- ✅ 2026-04-03, session-2026-04-03-016 — About page personality — grid layout with Tuna brand card, dark "What I Believe" section
- ✅ 2026-04-03, session-2026-04-03-016 — Contact page refinement — split layout with form card, branded focus states

*Note: Three Phase 8 deliverables (demo entry transitions, micro-interactions/polish, performance validation) are absorbed into Phase 9A and Phase 10 respectively. Remaining neutral-* class migration across inner demo components deferred to Phase 9 demo-specific work.*

---

## Phase 9A — Homepage & Core Architecture (COMPLETE)

*Goal: Rebuild the homepage interaction model. Replace the sidebar/bottom-sheet overlay with ambient event bubbles + full-page "under the hood" view. The homepage IS the Tier 1 showcase. Remove DemoNav. Full-width demo spotlight sections.*

- ✅ 2026-04-03, session-2026-04-03-017 — Strip "flip the card" language from all copy — footer, proof section, content guide, demo confirmations. Replaced with "under the hood" language
- ✅ 2026-04-03, session-2026-04-03-017 — Ambient event bubbles (Layer 1) — ephemeral, non-interactive event indicators on homepage/consulting pages. Route-aware (hidden on demo pages). Polls dataLayer, auto-fade after 3s
- ✅ 2026-04-03, session-2026-04-03-017 — Full-page "under the hood" view (Layer 2) — replaces Phase 3 sidebar/bottom-sheet with fixed inset-0 full-page view. Old overlay components removed
- ✅ 2026-04-03, session-2026-04-03-017 — Homepage underside content (Tier 1 showcase) — consent visualization, event stream reference, pipeline architecture. Overview tab default on homepage
- ✅ 2026-04-03, session-2026-04-03-017 — Replaced "See the Stack Running Live" static section with dark CTA section featuring pipeline path diagram and "Look under the hood" button that opens overlay
- ✅ 2026-04-03, session-2026-04-03-017 — Full-width demo spotlight sections — 3 full-width scroll sections with per-demo color world, tier badge, highlight bullets, and prominent CTAs
- ✅ 2026-04-03, session-2026-04-03-017 — Kill DemoNav — replaced with DemoFooterNav at bottom of demo pages with cross-demo navigation and back-to-homepage link
- ✅ 2026-04-03, session-2026-04-03-017 — Kill `/demo` landing page — redirects to `/#demos`
- ✅ 2026-04-04, session-2026-04-04-018 — Homepage restructured: hero-first narrative, event stream fix, tier overview moved to Services, demos intro added
- ✅ 2026-04-04, session-2026-04-04-018 — Design system: fonts finalized (Lora + Plus Jakarta Sans + JetBrains Mono), real font files, viewport meta tag

---

## Phase 9A-redesign — Editorial Homepage, Services, and Under-the-Hood Overlay

*Goal: Reskin the Phase 9A surfaces (homepage, services, under-the-hood overlay) per the editorial direction in `docs/input_artifacts/iampatterson-com/` — serif-forward magazine-grid on paper, terminal/CRT vocabulary in the overlay, persimmon accent that flips to phosphor amber during the overlay boot. Prototype copy verbatim where strong; current-site copy pulled in only where the prototype is thin. Demo front-ends out of scope.*

- ✅ 2026-04-18, commit 9c0dc7c — Design tokens + typography — Instrument Serif added (local font files bundled from google/fonts OFL; Lora removed); persimmon `#EA5F2A` + phosphor amber `#FFA400` tokens; dynamic `accent-current` Tailwind utility reading `var(--accent)`; paper/ink/rule scales; OverlayProvider sets `--accent` on `documentElement` with 130ms delay on open (lands mid-boot), instant revert on close, instant flip under prefers-reduced-motion, cancels pending swap if overlay closes during the hold
- ✅ 2026-04-18, commit a101fbb — Editorial chrome — `SessionPulse` (pulsing dot + session ID + live event count from useDataLayerEvents; clickable opens overlay), `LiveStrip` (SESSION/STACK/CONSENT/PIPELINE/DASHBOARDS/ATTRIB ticker, seamless loop), `MobileSheet` (numbered full-height slide-in menu), 4-column editorial Footer (brand/pages/demos/under-the-hood). FlipTrigger component and associated tests deleted.
- ✅ 2026-04-18, commit e36e9ca — Homepage hero masthead — "I build / *measurement* / infrastructure." three-line Instrument Serif display, deck with italic lede + body, primary/ghost CTAs. Shared `EditorialButton` / `EditorialLink` components.
- ✅ 2026-04-18, commit b013a0c — Pipeline section — 5-stage grid (Browser → Client GTM → sGTM → BigQuery → Dashboards) with 1400ms active-stage rotation, new `useLiveEvents` hook merging SSE+dataLayer sources, live log feed showing real events, "Watch it live" pill CTA
- ✅ 2026-04-18, commit cd0e5ba — Demos section — horizontal-scroll card track (mobile) / 3-col grid (desktop), swipe-bar indicator, 3 cards linking to `/demo/ecommerce`, `/demo/subscription`, `/demo/leadgen`
- ✅ 2026-04-18, commit ddbf5a1 — Services teaser + full Services page — TIERS content extracted to `src/lib/content/tiers.ts`; homepage 4-tier teaser list; services page rewritten with sticky tier-nav sidebar (scroll-spy, scroll listener, accent active), per-tier numbered core/optional grids, tier-summary blocks with accent left-border, "Watch it run first" closer
- ✅ 2026-04-18, commit e6e2f82 — Proof section — "Evidence" kicker, editorial headline, 3-metric grid (2.5M audience / $45K revenue / 24/7 live events) with oversized serif metrics + accent-colored unit suffixes
- ✅ 2026-04-18, commit d6f4227 — Final CTA — persimmon contact eyebrow (mailto), "Watch it / run first. / Then hire me." three-line headline, sub copy, primary/ghost CTAs
- ✅ 2026-04-18, commit 54aeef4 — Under-the-hood overlay redesign — full-page ink/terminal chrome with paper content pane, boot phase state machine (idle → boot → on, 260ms hold, skipped under prefers-reduced-motion and on re-opens within the same browser session via `sessionStorage`), CRT flicker/bloom/scanlines layers gated by phase-on, tab-flash settle animation on open/tab-change, Tabs reduced to 4 (Overview/Timeline/Consent/Dashboards), Narrative moved to contextual-only (below event detail), backdrop click closes, uses useLiveEvents
- ✅ 2026-04-18 — Wiring & route integration — `OverlayProvider` drives `--accent` and opens full-page view; demo routes untouched; ambient bubbles preserved via existing `AmbientBubblesWrapper` (`/demo/*` guard unchanged); `/demo` redirect preserved; flip-trigger / header-demos components fully removed; `Narrative` standalone tab removed from overlay

*Homepage composition: Hero → Pipeline → Demos → ServicesTeaser → Proof → FinalCta. Test suite: 527 → 609 (+82 net) after evaluation passes 1-3 + handoff close-out + session-024 CRT boot fidelity/once-per-session refinement + debt-cleanup pass. Production build clean.*

---

## Phase 9B — E-Commerce Demo: Tiers 2 & 3 (Data Infrastructure + BI)

> ✅ **DEV-COMPLETE** — all deliverables shipped on `phase/9b-ecommerce-tiers-2-3`; Pass 5 dual-evaluation PASS. See the header block at the top of this file for the summary and follow-ups. Phase 9E supersedes parts of 9B's overlay-tab routing (the Dashboards-tab content moves inline on the confirmation page in Phase 9F, which ships jointly with 9E).

*Goal: Each page in the checkout funnel demonstrates a different Tier 2 deliverable. Confirmation page pivots to Tier 3 with actionable insights. Looker Studio / Metabase integration.*

- ✅ 2026-04-04, commit 389d086 — Product listing underside: Campaign Taxonomy — AI-classified UTM parameters shown for visitor's session
- ✅ 2026-04-04, commit 389d086 — Product detail underside: Staging Layer — event flattening and enrichment visualization
- ✅ 2026-04-04, commit 389d086 — Cart underside: Data Quality Framework — live assertion checklist (schema validation, null checks, volume anomaly)
- ✅ 2026-04-04, commit 389d086 — Checkout underside: Warehouse Write — real-time BigQuery write visualization
- ✅ 2026-04-04, commit 389d086 — Confirmation page: Tier 3 pivot — funnel metrics, AOV trends, actionable insight with revenue impact
- ✅ 2026-04-18, commit 7d8a683 — (6a) Metabase "E-Commerce Executive" dashboard — 6 questions as YAML specs in `infrastructure/metabase/dashboards/`, applied to the live instance via `apply.sh`. Dev commits 493aad0…3d3dde0 (session-2026-04-18-021), Metabase v0.59+ compatibility fix 7d8a683, live apply verified 2026-04-18 per `docs/manual/task-2026-04-17-005.md`. Dashboard at `https://bi.iampatterson.com/dashboard/2` (cards 40-45); `metabase-embed-config` published to Secret Manager (`{dashboardId: 2, cardIds: {funnel: 40, aov: 41, dailyRevenue: 45}}`) and `metabase-embedding-secret-key` stored alongside — both ready for deliverable 6b's signer.
- ✅ 2026-04-18, commits a238a77 + 0fe8927 + fed0034 — (6b) Confirmation-page signed embed — 3 live Metabase iframes inline on `/demo/ecommerce/confirmation` below the order details (daily revenue → funnel → AOV ordering), each with short narrative prose tying the chart back to the visitor's just-placed order. Signed by a Next.js Server Component using `MB_EMBEDDING_SECRET_KEY` + card IDs from `METABASE_EMBED_CONFIG` (10-minute HS256 JWTs via `jsonwebtoken`). Non-embeddable three (ROAS q=42, Revenue share q=43, LTV q=44) live in the overlay's Dashboards tab on the confirmation route as summary cards deep-linking to individual Metabase questions, with a "See the full dashboard" link into `/dashboard/2`. Replaces deliverable 5's static Tier 3 mocks. Pending Vercel deploy: `MB_EMBEDDING_SECRET_KEY` + `METABASE_EMBED_CONFIG` env vars must be set in Vercel project settings (mirror of Secret Manager `metabase-embedding-secret-key` + `metabase-embed-config`).
- ✅ 2026-04-18, commit 08c5458 — Services page cross-links — "See it live →" link in the Tier 2 and Tier 3 summary boxes on `/services`: Tier 2 → `/demo/ecommerce`, Tier 3 → `/demo/ecommerce/confirmation` (pre-filled demo order so the inline BI embeds are meaningful on arrival).

---

## Phase 9B-infra — Metabase Deployment (COMPLETE)

*Goal: Self-hosted Metabase on GCP, gated by IAP, connected to BigQuery mart tables. Enables Phase 9B deliverable 6 (Metabase dashboard embed). Full spec: `docs/input_artifacts/metabase-deployment-plan.md`.*

*Phase 9B-infra is complete. Metabase is live at `https://bi.iampatterson.com/` behind the IAP gate, connected to the `iampatterson_marts` BigQuery dataset. Phase 9B application-layer deliverables 6 (Metabase embed in ecommerce confirmation page) and 7 (services cross-links) are now unblocked.*

- ✅ 2026-04-17, commit d1c4086 — Task 1: Cloud SQL Postgres app DB — idempotent setup script, daily backups, PITR, password stored in Secret Manager
- ✅ 2026-04-17, commit 9d38493 — Task 2: Service accounts + IAM — metabase-runtime (Cloud SQL + secrets) and metabase-bigquery (dataset-scoped read-only), keys in Secret Manager
- ✅ 2026-04-17, commit 7b4708e — Task 3: Cloud Run service deployment — deployed `metabase/metabase:v0.59.6` to `https://metabase-eb4xrwmo3q-uc.a.run.app`, Ready=True, ingress locked to internal-and-cloud-load-balancing.
  - First-deploy gotcha 1: Metabase's bundled pgjdbc concatenates `:PORT` onto `MB_DB_HOST`, so a Cloud SQL Auth Proxy socket path turns into a malformed JDBC URL. Switched to TCP via the Cloud SQL private IP over the existing VPC peering (resolved at render time in `deploy.sh`).
  - First-deploy gotcha 2: Metabase's Jetty defaults to port 3000 and doesn't auto-read Cloud Run's `PORT`. Added `MB_JETTY_PORT=8080` to align with the container port + startup probe.
- ✅ 2026-04-17, commit e60bd98 — Task 4: Environment variable documentation — .env.example covering every var with sources and the DO NOT REGENERATE warning on the encryption key
- ✅ 2026-04-17, commit a77e431 — Task 5: Load balancer + SSL cert — static IP `34.102.206.180`, Google-managed cert ACTIVE for `bi.iampatterson.com`, all 7 LB components provisioned. `curl -sI https://bi.iampatterson.com/api/health` returns HTTP/2 200 from Metabase. Two first-run gotchas surfaced: serverless-NEG backend can't carry a portName (dropped `--protocol=HTTPS` + heal-in-place), and LB-via-serverless-NEG needs `allUsers run.invoker` on the Cloud Run service (added to deploy.sh; ingress lock + IAP in Task 6 are the real security)
- ✅ 2026-04-17, commit 8366674 — Task 6: IAP configuration — OAuth consent screen configured (Internal, app `iampatterson BI`), OAuth client `metabase-iap-client` created, client ID/secret stored in Secret Manager, IAP enabled on `metabase-backend`, `ian@tunameltsmyheart.com` granted `roles/iap.httpsResourceAccessor`. `curl -sI https://bi.iampatterson.com/` returns HTTP/2 302 → `accounts.google.com`. Script hardened: auto-enables `iap.googleapis.com` (first run silent-failed because the API wasn't on); README flags the deprecated IAP OAuth Admin APIs (Mar 19, 2026 sunset for new projects — grandfathered for existing brands)
- ✅ 2026-04-17, commit 927e8db — Task 7: Metabase initial setup — admin account created, user signups disabled, public sharing and embedding confirmed OFF, BigQuery data source `iampatterson marts` connected via SA key from `metabase-bq-sa-key`, dataset filter Inclusion=`iampatterson_marts`, `mart_campaign_performance` returns rows end-to-end. Caveats: 2FA skipped (Pro/Enterprise-only on Metabase; OSS image has no MFA option — README step 6 updated to reflect this); Google Sign-In skipped by choice (solo user, IAP allowlist is sufficient). Deployed on top of Task 6 fixes including the IAP service agent provisioning that surfaced during first browser access
- ✅ 2026-04-17, commit 75035de — Task 8: Backup + upgrade runbooks — backup.sh, upgrade.sh, README sections complete and hardened. Execution deferred to first real upgrade (nothing to exercise on day zero). Cloud SQL automated daily backups (7-day retention) are already running independently via Task 1 config

---

## Phase 9E — Navigation & Overlay Pivot

*Goal: Reframe the site as one world in two states. SessionPulse is the primary nav; overlay restructures around a new Overview tab (post-UAT F1 rename; originally "Session State tab"); pipeline section becomes a progressive bleed-through reveal; Demos section collapses to a single ecommerce section; subscription + leadgen demos removed from the site. Demo reveal mechanics stay overlay-based until Phase 9F rebuilds them. Full spec: `docs/UX_PIVOT_SPEC.md`. Pipeline redesign: `docs/input_artifacts/design_handoff_pipeline/`.*

- ✅ 2026-04-20, session-2026-04-20-030 — Header navigation pivot. Conventional nav links removed from `src/components/header.tsx` (Home / Services / Demos / About / Contact). `SessionPulse` is the sole header nav affordance, promoted to `forwardRef` so `NavHint` can classify clicks as `click_session_pulse` vs `click_outside`. Hover affordance upgrade on desktop: tooltip "NAV · UNDER THE HOOD" in mono amber positioned below the button on hover/focus, ↗ indicator glows via `drop-shadow`, button surface gains amber ring + paper-alt bg on hover. `aria-describedby` links the button to the tooltip id only while visible (ARIA hygiene). Min 44×44 click target via `min-h-[44px]`. Desktop-only stronger pulse via `md:animate-session-pulse-strong` (1.9s cycle / scale→2.6) overriding the base mobile `animate-session-pulse` (2.4s cycle / scale→2.2) per spec's "slightly stronger on desktop" directive. `session_pulse_hover` emission (debounced 60s per session; suppressed when `e.pointerType !== 'mouse'`). First-session hint: new `src/components/chrome/nav-hint.tsx` — 3s idle trigger / 10s auto-clear (animated amber ring via `animate-nav-hint-ring`) / 6s fade under `prefers-reduced-motion` (static text "← menu · under the hood"). Idle defined as absence of `scroll` / `click` / `keydown` / `pointermove` on `document`; any of the four resets the 3s timer. Four dismissal modes matching `NavHintDismissedEvent.dismissal_mode` enum: `scroll`, `click_session_pulse` (target is SessionPulse or descendant), `click_outside` (any other click), `timeout` (10s/6s elapsed). `sessionStorage` gate at `iampatterson.nav_hint.shown` set on the idle → showing transition so a tab that never reaches the threshold doesn't burn its one chance. Homepage-entry-scoped (`usePathname() === '/'`); re-visits within the same session suppressed by the gate. `MobileSheet` deleted entirely — no backup hamburger, no alternate nav in the header. Commit `b423a39`. 22 new NavHint tests + 8 session-pulse extensions (hover tooltip, ARIA linkage, 44×44 contract, coarse-pointer suppression, 60s debounce, desktop-strong variant, sessionStorage throw fallback, homepage re-visit cycle) + 7 header integration tests. Build clean.
- ✅ 2026-04-20, session-2026-04-20-030 — Under-the-hood overlay restructure. Default `viewMode` changed from `'overview'` (homepage-conditional) to `'session_state'` unconditionally in `src/components/overlay/under-the-hood-view.tsx`. `OverlayTab` narrowed from 5 values to 3: `session_state | timeline | consent`. Tabs render in fixed order (Session State → Timeline → Consent); active tab label gets terminal-style bracket framing (`[ SESSION STATE ]`), inactive labels stay plain — `aria-label` preserves the clean tab name for assistive tech and test queries. `HomepageUnderside`, `DashboardView`, `EcommerceUnderside` router + 5 per-page underside sub-components deleted (1,603 lines); `HomepageUnderside` content salvage intentionally deferred to git history for 9F's native-reveal pattern re-derivation per UX_PIVOT_SPEC §3.5. Pathname-dependent `showOverview` + `isHomepage` + `isEcommerce` gating removed — tab set is identical on every route. Footer "Under the hood" column's `Pipeline architecture → overview` entry replaced with `Session state → session_state` (matches the new tab set). Overlay header subtitle revised from "Live · streaming your session events" (Timeline-centric) to "Live · this session, under the hood" (portal-framing). New `default_landing` emission: fires `session_state_tab_view` with `source: 'default_landing'` once per overlay-open on the landing-phase resolution (first post-pendingTab render); orthogonal to D3's click-bound `manual_select` emission. `landingResolvedRef` closes the gate unconditionally on resolution (not only on emission), which prevents the Pass 1 dual-fire bug where a `open('timeline')` landing + later click-back to Session State would fire both `manual_select` and a spurious `default_landing`. Commits `bbe23ca` (feat) → `a02f8de` (Pass 1 dual-fire fix) → `9e32303` (Pass 2 coverage gaps) → `2b25ec2` (Pass 3 product copy). 4-pass dual-evaluator convergence to CLEAN on both axes. 7 new default-landing tests + regression coverage in session-state-tab-emission + updated under-the-hood-view + integration + footer tests.
- ✅ 2026-04-20, session-2026-04-19-029 — Session State tab (new) rendered by `SessionStateTab` in `src/components/overlay/session-state-tab.tsx`. Sections: session header (last-6-char session ID matching SessionPulse/LiveStrip + started UTC time + current page), 16-cell ASCII coverage bar with `Math.round(fired/total*16)` fill (bar width hardcoded at 16 per spec; denominator 22 read from state, never hardcoded) + one-shot typewriter readout with `prefers-reduced-motion` fallback, chip grid iterating `state.event_type_coverage.total` (derive-from-schema), ecommerce funnel rendered via `ECOMMERCE_FUNNEL_SEQUENCE` + `getStageStatus` (canonical order; `[OK]`/`[SKIPPED]`/`[  ]` tags distinguishing reached / bypassed-by-deep-link / pending; fixed-width column for aligned mono layout), consent summary (three-row from `consent_snapshot`), three portal links with descriptors (> SERVICES / > ABOUT / > CONTACT), threshold-gated contextual contact CTA (>10 event types fired OR begin_checkout reached; diagnostic kicker + warm outcome-framed link per REQUIREMENTS §3.3 D8-deferred treatment). Registered as a new tab in the overlay's tab list; not yet default (deliverable 2 promotes it). Emission is click-bound via `handleTabChange`: `session_state_tab_view` with `source: 'manual_select'` fires only on active tab-bar clicks (programmatic opens via pendingTab, pathname resets, and reopen-with-sticky-tab do not emit). `portal_click` with `destination` enum on link clicks; `click_cta` with `cta_location: 'contact_cta_threshold'` on contextual CTA click. Coverage milestone emission wired end-to-end: `SessionState.coverage_milestones_fired` memoizes crossed thresholds across reloads; provider emits `coverage_milestone` data-layer events only for newly-crossed entries. Commits: `5b752c0` (feat), `e76199a` (Pass 1), `b15c1de` (Pass 2), `82b3c2a` (Pass 3), plus Pass 4 dual-fire fix. 40 new tests across milestones + component + emission (6 scenarios) + provider + storage/reconcile invariants. Suite 743 → 783; build clean; tsc --noEmit 22 baseline errors (zero in D3 paths).
- ✅ 2026-04-20, session-2026-04-19-029 — Session State data model (`src/lib/session-state/`, `SessionStateProvider`, `useSessionState`). Pure reducer + rehydration reconciliation (`reconcileRehydrated` refreshes `event_type_coverage.total` from the live `DATA_LAYER_EVENT_NAMES` on hydrate so a tab open across a deploy doesn't show a stale denominator; drops stale event names from BOTH `events_fired` keys AND `coverage.fired` so the two fields stay in lockstep; `session_id` reconciled against the current `_iap_sid` cookie). `consent_snapshot` seeded from Cookiebot-aware `getCurrentConsent()` on fresh init, then re-read on the first poll tick to heal the slow-Cookiebot-load case. `visited_paths: string[]` added to the blob to back `page_count` across reloads — **not** transmitted in the ride-along; `toRideAlongPayload()` projection helper enforces the narrow spec shape. Deep `hasValidShape` validation rejects structurally-partial blobs and enforces the `fired ⊆ total` invariant at load. Types tightened to `DataLayerEventName`-constrained unions. `isKnownEventName` shared predicate consolidates three derive-from-schema consumers. 61 tests across 6 files (reducer, init, storage, reconcile, ride-along, provider). Suite 682 → 743; build clean. Commits: `9a0e0f9` (feat), `d96ec24` (Pass 1 fix), `b53531f` (Pass 2 fix), plus Pass 3 doc-sweep + tick-dedupe fix commit
- ✅ 2026-04-20, session-2026-04-20-031 — Pipeline section progressive bleed-through reveal. `src/components/home/pipeline-section.tsx` rewritten as the outer shell that owns the scroll-driven `--bleed` ramp (anchored to the section's own height per the prototype's load-bearing math: `enter = vh*0.25`, `peak = vh*0.95 - h`, `p² ease-in` via `computeBleed` extracted to `src/components/home/pipeline-bleed.ts` for DOM-free unit testing), the tier-class state machine (`warm > 0.18`, `hot > 0.55`, `peak > 0.85` strict-greater-than; React re-renders only on threshold crossings), and the random flicker burst scheduler (`240 + (1-b)*2200 + rand*400` ms delay; `90 + rand*120` ms duration; gated to tier ≥ warm). Four absolute-positioned sibling layers (scanlines / phosphor / vignette / RGB sweep) read `var(--bleed)` and animate via the `.pipeline-section.*` rules added to `src/styles/globals.css`. `requestAnimationFrame` loop replaces the prototype's 16ms `setTimeout`; `IntersectionObserver` (rootMargin 200px) pauses the loop when the section is fully off-screen so cost is bounded to viewport time. New `src/components/home/pipeline-editorial.tsx` carries the editorial schematic — five numbered stages on a hairline rail with active-stage rotation on a 1800ms interval (disabled under reduced-motion), per-stage `is-hot` background highlight (base persimmon + hot-tier amber escalation bound to `--bleed`), inter-stage `↓` arrows, and a per-session footnote feed seeded with three "already happened" events then appended live events from `useLiveEvents` (capped to 4 visible). New content schema `src/lib/content/pipeline.ts` carries the **real production identifiers** verified against `infrastructure/gtm/web-container.json` (`GTM-MWHFMTZN` for browser + cgtm) and `server-container.json` (`GTM-NTTKZFWD` for sgtm), real sGTM hostname `io.iampatterson.com` in the readouts, real BQ raw table `iampatterson_raw.events_raw`. Footnote header session ID matches SessionPulse's `last-6` convention via `getSessionId()`; per-event timestamps use `Date.parse(e.timestamp) - t0` (not `Date.now()` per render) so the feed reads as a chronological log rather than ticking in lockstep on every re-render. Reduced-motion gating: rAF loop, flicker scheduler, stage rotation, CTA halo, and peak jitter all suppressed; mobile readouts force-expanded so reduced-motion visitors don't lose 4/5 of the substantive content. The "Watch it live" CTA preserves `cta_location='pipeline_watch_it_live'` across the rebuild for analytics time-series continuity. Section padding spec-honest at every viewport (`pt-20 pb-[100px]` matching the design handoff's `padding: 80px 0 100px`). Commits `5afe675` (feat) → `986d857` (Pass 1 fix — Critical fabricated GTM ID + 4 Major + 7 Important + 6 Minor) → `dad0543` (Pass 2 fix — wall-clock-flaky timestamp test + tight flicker bracket + 4 Minor) → `55bb416` (Pass 3 fix — try/finally on Date.now spy + fixture aligned with useLiveEvents newest-first contract). 4-pass dual-evaluator convergence to CLEAN on both axes (Pass 4: Tech 5.00/5 CLEAN, Product CLEAN carried from Pass 3 4.75/5). Test suite 746 → 796 (+50 net across 4 D5 test files: pure ramp math 14, content schema 9, editorial 14, section 19). Build clean. Homepage `/` still Static (`○`).
- ✅ 2026-04-20, session-2026-04-20-030 — Homepage Demos section rebuild. `src/components/home/demos-section.tsx` rewritten from a three-card horizontal-scroll track to a single full-width editorial section. Composition: eyebrow kicker (`Demo · Ecommerce · Tiers 2 + 3`), oversized serif headline ("Watch a purchase become a KPI." with persimmon italic on `KPI`), two paragraphs of supporting copy framing the native-reveal narrative (Tier 2 instrumentation reveal + Tier 3 Metabase payoff), primary `Enter the demo →` CTA linking to `/demo/ecommerce` (`trackClickCta(..., 'demo_card_ecommerce')` — identifier retained across the card→section refactor for analytics time-series continuity), editorial preview column with a terminal-styled sample `product_view` event readout + `[OK]` routing tags, honesty footnote ("Subscription and lead gen demos · returning after the ecommerce rebuild ships"). `RebuildBanner` leaf reads `?rebuild=` query param set by D7's 301 redirects, surfaces a one-line honesty banner ("The {label} demo is being rebuilt — it'll return after the ecommerce rebuild ships.") with dismissible × control. Closed allowlist via `Object.create(null)` + `Object.hasOwn()` — prototype-chain-safe against URLs like `?rebuild=toString` (Pass 1 Important finding closed). Per-label `sessionStorage` dismissal persistence (`iampatterson.rebuild_banner_dismissed.{label}`) so a visitor who dismisses and navigates away doesn't re-see the same banner. `useSearchParams()` isolated in the leaf and wrapped in `<Suspense fallback={null}>` to preserve the homepage's static generation — without the boundary, `/` would become Dynamic. Three-card DEMOS array, scroll listener + idx tracking, mobile swipe-hint bars all removed. Commit `7db063e` + prototype-chain fix. 14 tests in rewritten suite pinning anchor id, eyebrow tokens, serif headline, CTA emission, absence of sub/leadgen cards/swipe-hint, banner presence/absence by param, closed allowlist, prototype-chain safety, dismiss interaction, cross-mount persistence, per-label independence.
- ✅ 2026-04-20, session-2026-04-20-030 — Remove subscription and lead gen demos from the site. Deleted: 7 route folders (`src/app/demo/{subscription,leadgen}/` all pages), 8 components (`src/components/demo/{subscription,leadgen}/` — PlanCards, TrialSignupForm, AccountDashboard, SubscriptionDashboard, LeadgenLanding, PartnershipForm, LeadgenThankYou, LeadGenDashboard), `src/lib/demo/plans.ts`, 4 test files (subscription/leadgen pages + dashboards). Narrowed: `DemoId` in `src/components/demo/demo-theme.tsx` + `src/app/demo/layout.tsx` to bare `'ecommerce'` literal; `DemoFooterNav` simplified from currentDemo-prop with "Also explore" cross-demo links to a single back-to-homepage affordance (component retained per spec's simplify-or-remove choice); `DEMO_PREFIXES` in `src/hooks/useFilteredEvents.ts` narrowed; `DEMO_LINKS` in `src/components/chrome/nav-links.ts` reduced to ecommerce-only; `sitemap.ts` sub/leadgen URLs removed; `dashboard-data.ts` + `dashboard-types.ts` pruned of subscription + leadgen bundles and their type interfaces. `CtaLocation` legacy values removed from schema: `subscription_dashboard`, `demo_card_subscription`, `demo_card_leadgen`. Preserved per spec: subscription/leadgen event types (`PlanSelectEvent`, `TrialSignupEvent`, `FormCompleteEvent`, `LeadQualifyEvent`) in `src/lib/events/schema.ts`, their event-name literals in `DATA_LAYER_EVENT_NAMES`, and their tracking helpers in `track.ts` — the event-type denominator stays at 22 for Session State coverage, communicating "more of the site exists" via unfired chips. 301 redirects wired in `next.config.mjs` via `redirects()` async function: `/demo/subscription` + `/demo/subscription/:path*` → `/?rebuild=subscription#demos`, same for leadgen; `permanent: true` on all four entries so search engines re-index. Commit `23be3ec`. 4 new tests in `tests/unit/next-config-redirects.test.ts` pinning shape + permanent flag + deep-link glob coverage (Pass 1 M1 coverage gap closed). Updated: `tests/unit/components/footer.test.tsx` (Demos column reduced to ecommerce), `tests/unit/components/demo-footer-nav.test.tsx` (single-link simplified), `tests/unit/components/demo-theme.test.tsx` (ecommerce-only theme), `tests/unit/demo/dashboard-data.test.ts` (sub/leadgen blocks removed).
- ✅ 2026-04-20, session-2026-04-20-031 — Contact form session-state ride-along. New leaf component `src/components/contact/session-state-ride-along.tsx` renders below the message textarea: visible checkbox labeled "Share my session state with this message," human-readable summary reading `toRideAlongPayload()` (pre-existing projection helper from session 029), hidden `<input name="session_state">` serialized with `JSON.stringify(payload)` when checked. **Four-variant copy** keyed on (checked, marketing-granted): (1) checked+granted shows "You've triggered X of Y event types, completed Z%, visited W pages. Your consent state and session ID will ride along" with concrete numbers; (2) unchecked+granted shows "Session state will not be included — check the box above to share it"; (3) checked+denied shows "Overriding your denied marketing consent. You've triggered X of Y..." (transparency preserved at the override moment — the spec's "transparency is the point" conceit survives the explicit override path); (4) unchecked+denied shows spec §3.6 verbatim honest-override invitation. **Checked-state derivation** uses `userOverride: boolean | null` pattern + `checked = userOverride ?? consentDefault` every render — closes the `useState(initialFromAsyncContext)` anti-pattern where `useSessionState()` returns null on first render (provider inits in `useEffect`, not synchronously) and the checkbox would otherwise capture false forever. Same pattern also closes the consent-revocation path: a visitor who loads with marketing granted (auto-checked) and revokes via Cookiebot auto-unflips the box and strips the hidden field — no silent transmission under a denied consent signal the user never explicitly overrode. User's explicit click pins across subsequent consent changes in both directions. A11y: `id` on summary `<p>` linked via `aria-describedby` on the checkbox so screen-reader users hear the concrete payload breakdown on focus. Commits `ef5da37` (feat) → `70b2ba1` (Pass 1 fix — 2 Critical + 2 Major + 1 a11y) → `7fad7dd` (Pass 2 fix — 3 Minor test-hardening). 3-pass dual-evaluator convergence: Tech 2.30 → all-closed → **5.00/5 CLEAN**; Product 3.50 → **4.70/5 PASS** (both Majors closed Pass 2; 2 Minors explicitly flagged as "needs human spot-check — do not change preemptively" per product reviewer, carried). Test suite 796 → 820 (+24 net: 12 unit + 3 integration in initial feat, +9 in Pass 1 for async-init/consent-revocation/4-variant copy/a11y + 0 net in Pass 2 test-hardening). **Known Phase 10 follow-up**: the contact form's `handleSubmit` fires `trackFormSubmit('contact', true)` and navigates to `/contact/thanks` without sending the form body to any backend — a pre-existing Phase 8 lineage gap predating D8. D8's hidden field is therefore serialized and present in the DOM (inspectable in dev tools, testable, and ready to transmit) but lands nowhere until a real submit endpoint is wired up. Documented in the component JSDoc and tracked here for Phase 10 operational-readiness scope.
- ✅ 2026-04-19, session-2026-04-19-028 — Nav & Session State analytics (accelerated from Phase 10). Six new event interfaces + `DataLayerEvent` union 16 → 22: `NavHintShownEvent`, `NavHintDismissedEvent` (`dismissal_mode` enum, disjoint + exhaustive), `SessionPulseHoverEvent`, `SessionStateTabViewEvent` (`source` enum), `PortalClickEvent` (`destination` enum), `CoverageMilestoneEvent` (`threshold: 25 | 50 | 75 | 100` union). Plus the `click_cta` / `cta_location` closed-enum audit: `CtaLocation` closed union (14 values — 7 nav-adjacent + 4 editorial + 3 legacy carryovers); `ClickCtaEvent.cta_location: CtaLocation` not `string`; 9 existing call sites migrated to snake_case. `DATA_LAYER_EVENT_NAMES` runtime array + `_AssertEventNamesInSync` compile-time sentinel = source of truth for three consumers (`useDataLayerEvents`, `ambient-bubbles`, Session State deliverable 4 when built). BigQuery raw schema extended with 4 columns (`dismissal_mode`, `source`, `destination`, `threshold`) so discriminator fields reach BQ intact. 9 tests added (7 event interface + 1 CtaLocation closed-enum + 1 BQ column-check). Commits: `9903e8d` (feat), `224de92` (Pass 1 fix — derive-from-schema + CtaLocation + callsite migration), `78b5445` (Pass 2 fix — BQ columns + doc-sweep), `03df6c5` + `1d5b3de` (Pass 3-5 doc-sweep remainders). Dual-evaluator CLEAN after 5 iterative passes

---

## Phase 9F — Ecommerce Demo Native-Reveal Rebuild

*Goal: Rebuild the ecommerce demo's Tier 2/3 reveal mechanics on top of Phase 9E's foundation, using the four-pattern native-reveal language (toast, live sidebar, inline diagnostic, full-page moment) from `docs/UX_PIVOT_SPEC.md` §3.5. Confirmation page becomes the Tier 3 payoff via a single full-dashboard Metabase embed. Adopts the Tuna Shop hi-fi brand treatment from `docs/input_artifacts/design_handoff_ecommerce/`. Ships jointly with 9E — see the header block's "Release coupling" note. Full requirements in `docs/REQUIREMENTS.md` Phase 9F block; architecture in `docs/ARCHITECTURE.md` Phase 9F section.*

- ⬜ Pattern 1 — Event toast component — reusable non-blocking notification, warm amber mono on near-black pill (`#0D0B09` bg / `#EAD9BC` ink / `#F3C769` amber accent — prototype's warm Tuna Shop terminal palette), `aria-live="polite"`, ~2.2–2.6s lifetime, contextual positioning (`near-cart` / `near-product` / `near-form` / `viewport-top`), 220ms slide-down + fade entry / 180ms fade exit, max 3 stacked, route-change cancellation, reduced-motion-aware. `ToastProvider` + `useToast()` API. `src/components/demo/reveal/event-toast.tsx`
- ⬜ Pattern 2 — Live sidebar component — collapsible right-side panel (≥1024px) / top accordion (<1024px), dark panel with amber borders, warm cream body, low-opacity scanlines, collapses to thin amber strip / accordion handle, scrolls with content (not fixed), open-by-default on product detail / cart / checkout, collapse state resets on route change but persists within-route via `iampatterson.sidebar.collapsed.<route>`, `Esc` collapses (no focus trap), reduced-motion-aware. `src/components/demo/reveal/live-sidebar.tsx`
- ⬜ Pattern 3 — Inline diagnostic block — presentation pattern via `InlineDiagnostic` styled wrapper; dark bg / amber headers / warm cream body, terminal-style separators, `>` prompt prefixes. `src/components/demo/reveal/inline-diagnostic.tsx`
- ⬜ Pattern 4 — Full-page diagnostic moment — full-bleed dark overlay, amber text, scanlines + bloom, ~7-line typed sequence (~1.9s with 120ms stagger), skippable (any key) and reduced-motion-skipped, `role="dialog"` with focus trap. Reserved for exactly one moment (checkout submit, between form submission and `/confirmation` navigation). `src/components/demo/reveal/full-page-diagnostic.tsx`
- ⬜ Product listing — three-toast cascade on hydration (`session_start` ~700ms → `taxonomy_classified` ~1.9s → `view_item_list` ~3.6s); reads UTM from `searchParams` with deterministic seed fallback; renders prototype's listing-hero block (your-utm / classified-as panel) for durable post-toast reference; salvage classifier from `CampaignTaxonomyUnderside` into `src/lib/demo/reveal/campaign-taxonomy.ts`
- ⬜ Product detail — `product_view` toast (`near-product`) + staging-layer sidebar (field extraction, `[CAST x → y]` rules, stitch-and-enrich ops `dedupe / session_stitch / param_extract / geo_enrich`, terminating in `→ stg_product_views [written]` footer); salvage from `StagingLayerUnderside` into `src/lib/demo/reveal/staging-layer.ts`
- ⬜ Cart — `view_cart` toast + data-quality assertions sidebar (6 assertions: `schema_validation / null_check / volume_anomaly / session_join_integrity / freshness / referential_integrity`, `[OK]` / `[FAIL]` with one-line context, header `source: raw.events · streaming`, 6-cell ASCII pipeline-health meter); cart storage shape adopts prototype's `iampatterson.tunashop.cart.v1` localStorage key + `{ id, qty, price, name }[]` shape, replaces existing `cart-context.tsx`; salvage assertions from `DataQualityUnderside` into `src/lib/demo/reveal/data-quality.ts`
- ⬜ Checkout — `begin_checkout` toast + warehouse-write sidebar (real `iampatterson_raw.events_raw` destination, 21 of 51 columns shown, field-by-field row preview updating on form change, `stream · p50 38ms · p95 112ms` footer) + full-page diagnostic moment on submit (7-line typed sequence ending in `dashboards · revenue KPI refreshing [LIVE]`, then `cart.clear()` + `router.push('/demo/ecommerce/confirmation')`); salvage 51-column schema from `WarehouseWriteUnderside` into `src/lib/demo/reveal/warehouse-write.ts`
- ⬜ Confirmation — `purchase` toast + inline diagnostic block (timestamped 6-step pipeline-journey list, `+0ms` → `+840ms`) + **full-dashboard Metabase embed** (not six individual) replacing today's three `LiveEmbedFrame` instances + IAP-gated fallback. Single signed dashboard JWT via Metabase `/embed/dashboard/:jwt`; `max-w-[1200px]` at ≥1024px; per-order narrative in one lead paragraph above the embed (with `$total` zombie-state fallback for the 9B follow-up #5 fix); embed wrapped in `InlineDiagnostic`; mobile (<768px) carries IAP-gated "View full dashboard → (Google SSO required — internal BI)" deep-link as honest production affordance. **Ship-gate (binary blocker):** 9B operational follow-up #1 (Metabase `--no-cpu-throttling`) applied AND cold-start probe from verifiable idleness loads embed in <10s first-paint / <15s interactive, with result recorded in session handoff
- ⬜ Component removal + content salvage cleanup — delete `CampaignTaxonomyUnderside`, `StagingLayerUnderside`, `DataQualityUnderside`, `WarehouseWriteUnderside`, `Tier3Underside`, `EcommerceUnderside` (audit at impl time — most already deleted in 9E D2); helper utilities migrate to `src/lib/demo/reveal/`; audit `LiveEmbedFrame` usage and delete or fold in 9B follow-up #2 (load-failure timeout)
- ⬜ SessionPulse + Overview reachability on demo pages — Playwright E2E across all 5 demo pages confirms overlay opens, Overview is default landing, `demo_progress.ecommerce` updates monotonically through `product_view` → `add_to_cart` → `begin_checkout` → `purchase`; site header (SessionPulse + LiveStrip + HomeBar) renders above demo's own `EcomHeader` (brand + shop/cart nav)
- ⬜ Palette-token harmony + Tuna Shop brand treatment — (a) Tuna Shop palette scoped via `data-demo="ecommerce"` (cream `#FBF6EA` / terracotta `#C4703A` / warm-brown `#5C4A3D` editorial; near-black `#0D0B09` / cream `#EAD9BC` / warm amber `#F3C769` terminal — distinct from site-wide phosphor `#FFA400`); (b) migrate demo components from raw `neutral-*` to `ink / paper / rule` editorial scale (addresses 9B follow-up #3); (c) typography tokens — Instrument Serif display, Plus Jakarta Sans body, JetBrains Mono terminal — already loaded site-wide; (d) brand voice rules verbatim from prototype README (lowercase headings, no em dashes, no rule-of-three, first-person plural when shop voices itself, no-kill rescues mention on footer/PDP/confirmation)

---

## Phase 9C — Lead Gen Demo: Tier 1 Privacy/Consent + Tier 3 BI + AI

> 🚫 **CANCELLED** as part of the Phase 9E UX pivot. The lead gen demo is removed from the site in Phase 9E deliverable 7. Consent-enforcement and AI-narrative storylines will be reintroduced in a later, separately-numbered phase after Phase 9F, rebuilt from the pattern language in `docs/UX_PIVOT_SPEC.md` §3.5. The original deliverables below are preserved as seed material for the reintroduction phase; they will not be implemented as specified. Rationale: `docs/UX_PIVOT_SPEC.md` §4.

*Goal: Make consent enforcement tangible. Form interaction shows PII handling, consent-gated routing. Thank-you page shows BI + AI narrative reporting.*

- 🚫 Form interaction underside: Consent & Privacy visualization — consent signals, platform routing, email hashing, ad_user_data gating
- 🚫 Live consent enforcement demonstration — deny/grant marketing consent, see routing differences (BigQuery+GA4 only vs full routing with hashed PII)
- 🚫 Thank-you page: Tier 3 BI — lead funnel dashboard, cost per qualified lead by channel
- 🚫 Automated Narrative Reporting (AI) — RAG pipeline generating weekly written summaries for lead gen model, viewable in under-the-hood
- 🚫 Services page cross-links: Tier 1 privacy → lead gen form, Tier 3 AI → narrative reporting sample

---

## Phase 9D — Subscription Demo: Tier 4 (Attribution & Advanced Analytics)

> 🚫 **CANCELLED** as part of the Phase 9E UX pivot. The subscription demo is removed from the site in Phase 9E deliverable 7. Shapley attribution, cohort retention, and LTV storylines will be reintroduced in a later, separately-numbered phase after Phase 9F, rebuilt from the pattern language in `docs/UX_PIVOT_SPEC.md` §3.5. The original deliverables below are preserved as seed material for the reintroduction phase; they will not be implemented as specified. Rationale: `docs/UX_PIVOT_SPEC.md` §4.

*Goal: Multi-touch attribution, cohort retention by channel, LTV analysis. The most analytically sophisticated tier on the model that benefits most.*

- 🚫 Multi-touch attribution model (Shapley value) built in Dataform for subscription business model
- 🚫 Attribution comparison visualization: Shapley vs last-click vs platform-reported, with narrative methodology explanation
- 🚫 Cohort retention curves by acquisition source — channels that produce high-retention vs high-churn subscribers
- 🚫 LTV by channel analysis using mart_customer_ltv and mart_subscription_cohorts tables
- 🚫 Lightweight MMM or geo-lift demonstration (static analysis if geo data insufficient)
- 🚫 Services page cross-links: Tier 4 → subscription attribution comparison, cohort retention

---

## Phase 10 — Polish, Performance & Launch Prep

*Goal: Optimize the full experience for production readiness.*

- ⬜ Performance optimization: Core Web Vitals, Lighthouse scores, WebSocket connection reliability, overlay rendering performance
- ⬜ Mobile testing across devices and screen sizes
- ⬜ Error handling: graceful degradation if WebSocket drops, BigQuery is slow, or Pub/Sub has latency spikes
- ⬜ Analytics on the site itself: demo interaction patterns, funnel reach, time-on-site / scroll-depth distribution, contact-form conversion signal. *Nav-specific + Session State analytics moved into Phase 9E deliverable 9 — see rationale there.*
- ⬜ Copy and content refinement across all consulting pages
- ⬜ SEO: meta tags, structured data, sitemap, content strategy for organic discoverability
- ⬜ Security review: ensure demo interactions can't expose real data, service accounts properly scoped, no PII leakage in event stream
- ⬜ Load testing on background data generator and WebSocket service

---

## Phase 11 — Operational Readiness & Maintenance Infrastructure

*Goal: Build the monitoring, alerting, and operational tooling needed to keep the full stack healthy in production.*

- ⬜ Cloud Monitoring dashboard: single pane for Cloud Run services, Pub/Sub throughput, BigQuery costs, SSL status
- ⬜ Alerting policies: 5xx error rates, pipeline latency, container crashes, BigQuery cost threshold, Dataform failures, SSL expiry
- ⬜ Uptime checks: automated health probes for sGTM, event-stream, and Vercel site with notification channels
- ⬜ sGTM container lifecycle: update process for gtm-cloud-image releases, version pinning strategy
- ⬜ Data retention and cost controls: BigQuery partition expiration, GCS lifecycle rules, GCP budget alerts
- ⬜ Operational runbook: documented procedures for common failure modes (doubles as portfolio content)
- ⬜ Log aggregation: structured logging across Cloud Run services, log-based metrics, retention policy
- ⬜ Dependency update process: cadence for Node.js, Next.js, Cloud Run images, npm deps, security advisories
