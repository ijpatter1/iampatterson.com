# Session Handoff — 2026-04-17-019

**Phase:** 9B-infra — Metabase Deployment
**Date:** 2026-04-17
**Branch:** `phase/9b-infra-metabase-deployment` (not pushed)

## Completed This Session

### Phase setup + Phase 9B status reconciliation

- Updated stale `PHASE_STATUS.md`: Phase 9B application-layer deliverables 1–5 were merged on main (commit `389d086`) across sessions 017–018 but had never been marked complete. Fixed.
- `jest.config.ts` import path fixed from `next/jest` to `next/jest.js` (was already committed as `7753eac`; confirmed working).
- Accent color decision captured: white/black/grey clean slate, no accent color — marks the last Phase 9A item ✅.

### Phase 9B-infra scaffolded (`5b221b1`)

- New sub-phase added between Phase 9B and Phase 9C in
  `PHASE_STATUS.md`, `REQUIREMENTS.md`, `ARCHITECTURE.md`.
- Eight tasks enumerated, spec cross-referenced to the input artifact
  at `docs/input_artifacts/metabase-deployment-plan.md`.

### Task 1 — Cloud SQL Postgres app DB (✅ complete)

**Commits:** `f758a62` (initial), `d1c4086` (prompt-disable hardening), `ea41ac5` (status + tech debt note).

- `infrastructure/metabase/setup-cloudsql.sh` — idempotent, dry-run, VPC peering precheck, private IP only, daily backups, PITR, budget alert auto-discovering the linked billing account.
- README Task 1 section with prerequisites, verification, password retrieval.
- **Executed on GCP by you** — instance `metabase-app-db` (`10.13.0.3`) created, `metabase` database + user, secret `metabase-db-password`, budget `metabase-project-budget` @ $100/mo.
- **Idempotent re-run verified.** Evaluator subagent: **PASS** (all 5 checks).
- Tech debt recorded in README: secret/user creation could split into independent idempotency gates; `--password` briefly visible in `ps` during user create.

### Task 2 — Service accounts + IAM (✅ complete)

**Commits:** `db77988` (initial), `2eb14ad` (BigQuery GRANT fix), `9d38493` (verify filter fix), `e66b305` (status + evaluator minors).

- `infrastructure/metabase/setup-iam.sh` creates:
  - `metabase-runtime` SA → `roles/cloudsql.client` (project) + `secretmanager.secretAccessor` scoped per-secret on the three metabase secrets
  - `metabase-bigquery` SA → `roles/bigquery.dataViewer` **dataset-scoped** to `iampatterson_marts` + `bigquery.jobUser` (project)
  - Secrets: `metabase-encryption-key` (generated once — DO NOT REGENERATE), `metabase-bq-sa-key` (JSON key via mktemp → stdin → Secret Manager → shred verified)
- `bq add-iam-policy-binding` initially tried, failed with "This feature requires allowlisting." Switched to BigQuery DCL (`GRANT ... ON SCHEMA ...`), which is GA and idempotent.
- **Executed on GCP by you.** Evaluator subagent: **PASS**.

### Task 3 — Cloud Run service spec + deploy script (🔄 code only, not deployed)

**Commits:** `e8cb763` (initial), `afa1d42` (evaluator hardening).

- `cloudrun.yaml` — templated Knative spec. Ingress `internal-and-cloud-load-balancing`, gen2, Direct VPC egress (`private-ranges-only`), Cloud SQL Auth Proxy via `run.googleapis.com/cloudsql-instances`, 2Gi/1CPU, min-instances=1, startupProbe `/api/health` allowing 120s cold start. `MB_DB_TYPE=postgres` explicit, secrets via `valueFrom.secretKeyRef`.
- `deploy.sh` — envsubst + sed fallback rendering, precondition checks (Task 1+2 artifacts), image-tag pinned-semver regex guard, post-render placeholder leak guard.
- **Hardening from evaluator:** tightened image tag regex to positive semver allowlist, placeholder-leak guard scoped to `${VAR}` convention, explicit `containerPort: 8080` + probe port, expanded README failure-mode list, drift warning for manual console edits.
- Evaluator: **PASS WITH ISSUES → addressed**. Runtime checks deferred.

### Task 4 — `.env.example` (✅ complete)

**Commit:** `e60bd98`.

- Documents all 9 env vars `cloudrun.yaml` delivers, no secret values, DO NOT REGENERATE warning on `MB_ENCRYPTION_SECRET_KEY` with full consequence statement.
- Evaluator: **PASS** (pure doc, no deploy needed).

### Task 5 — Load balancer + SSL cert (🔄 code only)

**Commits:** `70516dd` (initial), `c5dea66` (evaluator hardening).

- `setup-domain.sh` — 7 components: static IP, SSL cert, serverless NEG, backend service, URL map, HTTPS proxy, forwarding rule. Idempotent via describe-before-create. Prints DNS A record instruction, polls cert status up to 60 min, resumes cleanly after Ctrl+C.
- **Hardening:** NEG backend-attach match tightened from substring glob to exact basename via `gcloud`'s `.basename()` projection. Cert poll now exits on any `FAILED_*` status (not just `FAILED_NOT_VISIBLE`) with tailored guidance per failure class. EXTERNAL_MANAGED scheme choice documented.
- Evaluator: **PASS WITH ISSUES → addressed**. Runtime deferred.

### Task 6 — IAP configuration (🔄 code only)

**Commits:** `0efb93b` (initial), `120d827` (evaluator hardening).

- `setup-iap.sh` — OAuth client creation (requires one-time manual consent screen setup in Console, documented), stores client ID/secret in Secret Manager, enables IAP on `metabase-backend`, reconciles allowlist additively (never removes — rationale documented).
- **Hardening:** removed `--condition=None` from `gcloud iap web add-iam-policy-binding` (not supported on that subcommand — would have errored at runtime). Added warning that renaming `OAUTH_CLIENT_NAME` creates a duplicate client. Resolved the plan's allowlist-text inconsistency with a `> Note on plan fidelity:` block in the README.
- Evaluator: **PASS WITH ISSUES → addressed (Critical flag fix)**. Runtime deferred.

### Task 7 — Metabase first-run runbook (🔄 pure doc, not executed)

**Commits:** (initial rolled in with README updates), `d7711c2` (initial runbook), `e1ba6ea` (evaluator hardening).

- README Task 7 section: 9 steps covering admin account, disable signups, optional Google Sign-In with domain restriction, 2FA, public sharing OFF, BigQuery data source with SA key from Secret Manager, dataset filter Inclusion + `iampatterson_marts`, verification via `mart_campaign_performance`.
- **Hardening:** named the Metabase UI control precisely (Inclusion dropdown + comma-separated field). Added Google Sign-In domain-restriction failure mode to troubleshooting.
- Evaluator: **PASS WITH ISSUES → addressed**. Execution requires browser + allowlisted Google account.

### Task 8 — Backup + upgrade runbooks (🔄 code only)

**Commits:** `5dfae08` (initial), `75035de` (evaluator hardening).

- `backup.sh` — on-demand Cloud SQL backup tagged with timestamp + deployed Metabase image tag. Prints backup ID via post-create filtered list.
- `upgrade.sh` — validates target version regex, captures current image for rollback, runs backup.sh (unless `--skip-backup`, now guardrailed to <24h backup freshness), prints release notes URL and reads y/N from `/dev/tty`, re-runs deploy.sh, polls `gcloud run services describe` for Ready=True on target image (10 min), prints verification + rollback on success/failure.
- **Hardening (Critical fix):** original poll used gcloud format JMESPath-like filter `conditions[?type=Ready].status` which gcloud's format grammar doesn't support. Every successful upgrade would have falsely reported timeout. Switched to `--format=json | jq`. jq now a hard prerequisite with install hint.
- **Other hardening:** poll deadline 5→10 min (Metabase cold start + Liquibase migrations), `--skip-backup` freshness guardrail with GNU+BSD date parsing, rollback README block now explains how to obtain `<BACKUP_ID>` inline.
- README Task 8 section + operational summary table covering backup, restore, upgrade, rollback (image-only + image+DB), annual BQ SA key rotation, allowlist add/remove.
- Evaluator: **PASS WITH ISSUES → addressed (Critical filter fix)**. Runtime deferred to first real upgrade.

## In Progress

Six tasks (3, 5, 6, 7, 8) have code/docs written, per-task evaluator review complete, and the code-level Important/Critical findings from each review addressed. They are not **deployed** or **executed** — each needs you to run the respective script (or, for Tasks 3, 6, 7, do the associated manual steps).

## Blocked

Nothing blocked at the code level. At the execution level, the chain must be walked in order:

- Task 3 (deploy Cloud Run) — needs `roles/run.admin`, `roles/iam.serviceAccountUser` on `metabase-runtime`, `compute.googleapis.com` API enabled.
- Task 5 (LB + cert) — needs Task 3 deployed; one manual DNS A record at the registrar.
- Task 6 (IAP) — needs Task 5 done + one-time manual OAuth consent screen setup in GCP Console.
- Task 7 (Metabase UI) — needs Task 6 passing (IAP gate reachable).
- Task 8 (backup/upgrade) — backup.sh can run anytime Task 1's instance exists (already). upgrade.sh is only exercised on the first real upgrade.

## Evaluator Results

### Per-task (during session)

| Task | Code-level verdict | Runtime deferred? |
|---|---|---|
| 1 | PASS | Already executed |
| 2 | PASS | Already executed |
| 3 | PASS WITH ISSUES → addressed | Yes (deploy) |
| 4 | PASS | Pure doc |
| 5 | PASS WITH ISSUES → addressed | Yes (exec + DNS + cert) |
| 6 | PASS WITH ISSUES → addressed (1 Critical: `--condition` flag unsupported, fixed) | Yes |
| 7 | PASS WITH ISSUES → addressed | Yes (manual UI) |
| 8 | PASS WITH ISSUES → addressed (1 Critical: gcloud format filter invalid, fixed with json+jq) | Yes |

### Session-level aggregate (during /handoff)

Weighted score: **3.75/5.00**. Verdict: **PASS WITH ISSUES** → addressed in `c3274d7`.

Cross-cutting findings (not per-task redo):
- README had orphaned "Tasks 4–8 upcoming" bullets at file tail — would read as if infra was half-built when opened bottom-up.
- `upgrade.sh` failure message said "5 minutes" while the poll deadline was 10 min (stale from a hardening edit).
- `setup-domain.sh` header listed `certificatemanager.googleapis.com` as a prereq — wrong product; Google-managed SSL certs use `compute.googleapis.com`.
- `backup.sh` was missing the Cloud SQL precondition check every other script has.
- `deploy.sh` didn't warn about missing `roles/iam.serviceAccountUser` on the runtime SA — the most likely first-run failure with a cryptic Cloud Run error.

All Critical + Important cross-cutting findings fixed in `c3274d7`.

## Product Review Results

Session-level review during /handoff.

Weighted score: **4.5/5.0**. Verdict: **PASS**.

| Criterion | Pre-fix | Post-fix |
|---|---|---|
| Vision alignment | 5/5 | 5/5 |
| Portfolio readiness | 4/5 | 5/5 |
| Content quality | 4/5 | 5/5 |
| Feature depth | 5/5 | 5/5 |
| Solo-developer fit | 4/5 | 4/5 (gap: Metabase tag not recorded until Task 3 runs) |

Four of six Minor findings addressed in `939745b`: traffic-path diagram at README top, aggregated IAM role list in Prerequisites, tightened plan-fidelity note on allowlist semantics, expanded "clipboard is fine" with the why.

Deferred:
- Input artifact `metabase-deployment-plan.md` has `backup.sh` labelled Task 7 in its directory listing; should be Task 8. Left alone — input artifacts are reference, not deliverables.
- Pinned Metabase image tag isn't recorded in the repo. Record in `.env.example` or a new `VERSIONS.md` once Task 3 is executed with a concrete patch.

## Test State

- Total tests: 486
- Passing: 486
- Failing: 0
- Skipped: 0

No application-layer code changed this session. Tests ran clean after every commit.

## Next Steps (in order)

1. **Deploy Task 3** — resolve the pinned Metabase image tag:
   ```
   curl -s https://api.github.com/repos/metabase/metabase/releases/latest | jq -r '.tag_name'
   ```
   Or browse `https://github.com/metabase/metabase/releases`. Pick a `v0.59.6.x` patch, then:
   ```
   export METABASE_IMAGE=metabase/metabase:v0.59.6.<patch>
   ./infrastructure/metabase/deploy.sh --dry-run
   ./infrastructure/metabase/deploy.sh 2>&1 | tee /tmp/metabase-task3.log
   ```
   Paste log + `gcloud run services describe metabase --format='value(status.url,status.conditions[0].status,spec.template.spec.containers[0].image)'` output when done.

2. **Provision Task 5 LB + cert.** This has the long manual step (cert provisioning can take 15–60 min after DNS is set):
   ```
   ./infrastructure/metabase/setup-domain.sh
   ```
   The script pauses with a DNS record to create at your domain registrar, then polls cert status. Re-running is safe — idempotent across the 7 components and polling resumes from wherever it left off.

3. **Configure OAuth consent screen (manual, one-time)** before running Task 6:
   - <https://console.cloud.google.com/apis/credentials/consent?project=iampatterson>
   - User Type: Internal, App name `iampatterson BI`, Support email your Google account, Scopes default.

4. **Run Task 6 IAP setup:**
   ```
   ./infrastructure/metabase/setup-iap.sh 2>&1 | tee /tmp/metabase-task6.log
   ```
   Verify `curl -sI https://bi.iampatterson.com/` returns 302 to `accounts.google.com`.

5. **Walk Task 7 runbook** in the browser with an allowlisted Google account. Grab the BQ SA key from Secret Manager at step 7.

6. **Optional: exercise Task 8 backup.sh** any time (no Metabase state depends on it). `upgrade.sh` is only tested by a real upgrade, so it'll first see action on the next Metabase release.

## Session Notes

- **Branch**: `phase/9b-infra-metabase-deployment`. 19 commits this session on top of `505749d` on main. Not pushed.
- **Evaluator per task:** used the evaluator subagent directly rather than `/evaluate` (which runs the dual technical + product review). Product-reviewer adds less value to pure infrastructure scripts; evaluator alone was sufficient. Each invocation averaged ~45s and caught concrete issues at every task (notably the `bq add-iam-policy-binding` allowlist surprise in Task 2, the `--condition` flag mismatch in Task 6, and the gcloud format filter bug in Task 8 — all would have burned the first real run).
- **CLAUDE.md "Current Phase"** was updated to reflect Phase 9B-infra as active. Revert when 9B-infra completes and attention returns to 9B application layer (deliverables 6 + 7 still ⬜).
- **Phase 9B app-layer gap:** deliverables 6 (Metabase embed in confirmation page under-the-hood) and 7 (services cross-links to ecommerce funnel) were not touched this session. Deliverable 6 is now unblocked *after* Task 7 is executed (the Metabase instance needs to exist and have the dashboard built). Deliverable 7 is independent and could be picked up any time.
- **Tech debt from Task 1 carries forward:** secret/user split into independent idempotency gates; `--password-file=-` stdin path for `sql users create`. Neither urgent.
- **One cosmetic:** the README's "Upcoming tasks" list kept getting appended to as tasks were implemented; it should be collapsed to an empty section or removed. Left alone to keep this handoff focused.
