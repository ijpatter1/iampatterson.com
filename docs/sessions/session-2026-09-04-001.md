# Session 2026-09-04-001 — guv adoption, initiative 002 plan, Phase 12 delivered

**Phase:** 12 — Runtime currency and observability (initiative 002, operational readiness)
**Branch:** `phase/12-runtime-currency-and-observability` (from `main` ec73ff4)
**Model:** Fable 5.1 until the weekly limit; Ian said the session continues on Opus 5 after that. This file is written early and updated at close so a switch loses nothing.

## Completed

- **guv adoption** (PR #60, merged as ec73ff4). Shell and manifest deployed; conventions moved from the pre-guv `CLAUDE.md` into `.claude/rules/project-*.md`; `CLAUDE.md` rendered from the template with the project facts; README status block. Initiative 001 (Phases 1–11) archived at `docs/initiatives/001-greenfield-through-launch/` with the legacy `CLAUDE.md` beside it.
- **Initiative 002 planned** from `docs/spec/operational-readiness.md`: Phases 12–14, 14 deliverables, one PR per phase under the ultrareview ceiling (500 files / 8,000 lines). Estimates in `docs/estimates.json`; opening forecast banked; ceremony `phased`.
- **Phase 12 opened** through `/guv:phase 12`. Baseline 1,770 tests green. Spec-alignment review returned ten findings; nine were vetted and applied under Ian's standing approval: rewords of 12.1–12.5 through the replan engine (amendment records in `docs/PHASE_STATUS.md`), acceptance and risk-note edits (a 2026-09-24 hatch for shipping 12.1 alone), ARCHITECTURE corrections.
- **12.1 Node.js 24 runtime — delivered** (commits eb650ee, cf133a2, a0f924a, 1e98400, 817120c, 0e1af88):
  - `engines.node` 24.x; the three Dockerfiles on `node:24-slim`; CLAUDE.md facts carry the pin and the keg-only toolchain path (`/opt/homebrew/opt/node@24/bin`); ARCHITECTURE updated; the sync-dataform workflow pins no Node (confirmed, recorded).
  - Pins: `tests/unit/infra/runtime-currency.test.ts`, the proxy's `dockerfile.test.ts`.
  - Verified under Node 24.20.0: root suite 1,775 (now 1,79x with the new pins), the three service suites, the golden gate.
  - Vercel preview built on Node 24 with the cache skipped for the version change and zero deprecation warnings (`iampatterson-hs5tfy4n3…`, agent-initiated `vercel deploy` from the branch, not a production deploy).
  - Three services redeployed on the new image through the new `scripts/deploy-cloud-run.sh`: no-traffic revision, field diff (only the image digest changed on each; diffs under `docs/verification/deploys/`), promoted; serving revisions event-stream-00009-vz7, data-generator-00008-n2r, claudish-proxy-00030-lmt. A stray no-traffic revision data-generator-00009-xlp exists from a dry run; it serves nothing.
  - Record: `docs/verification/2026-09-04-node24-runtime.md`, generated by `scripts/verify-node24.sh` (suites, golden with fall-through count, Vercel log, Cloud Builds matched by digest, health with identity token for the private data-generator, proxy smoke both directions, the redeploy diffs). Ian's rule applied: nothing a machine can verify goes on a card.
- **Review gate round 1** (alignment reviewer plus three harvested code-review angles; the platform review fork was stopped because it inherited the main model and burned usage): fixed in 0e1af88. Deploy script restructured into `deploy` / `diff` / `promote <revision>` so the reviewed revision is the promoted one, one-shot promote refuses drift; identity-token health probe; golden harness falls through only pre-token, rethrows otherwise, fails when more than half the cases fall through; verification record extended; ARCHITECTURE and CLAUDE.md name the deploy path; the 12.1 acceptance names the scripted record.

- **12.2 Notification channel and uptime checks — delivered** (38af29b, 1b78235). Two channels (`ops-email` to ian@, `ops-pubsub` to the `monitoring-alerts` topic) and five uptime checks, all declared in `infrastructure/monitoring/spec/` and applied by `apply.sh`, which drives the Monitoring, Logging and Pub/Sub REST APIs with an access token and needs no gcloud alpha or beta component. Every check has its own alert policy on both channels. The rehearsal is scripted: `apply.sh rehearse claudish-proxy-health` pointed the check at a missing path, read the OPEN notification off a pull subscription 6 minutes later, restored the path in a `finally` block, and read the CLOSED notification 6 minutes after that. Records: `docs/verification/2026-09-04-uptime-checks.md` (five checks, 212–216 of 216 results passed in 30 minutes) and `2026-09-04-uptime-rehearsal.md`.
- **12.3 Log-based metrics and retention — delivered** (38af29b). Four metrics: `claudish_proxy_events` (labels event, severity, pct), `data_generator_ad_insert_failures`, `cloud_run_no_instance`, `event_stream_errors`. `_Default` retention stays at 30 days, marked provisional against the 13.1 decision, with no sink because nothing needs to outlive the bucket. Four 24-hour "what failed" queries, one per service, all returning. Record: `docs/verification/2026-09-04-log-metrics.md`.
- **12.4 Alerting policies — delivered** (1b78235, 595bca0). Eleven policies in `spec/policies.json`, each carrying its threshold, its firing record or the reason it cannot be fired safely, and its 13.5 runbook entry. The capacity policy was fired on purpose: `apply.sh rehearse-policy` turned the kill switch on, requested one translation (HTTP 503), turned the switch off 109 seconds later, and read the OPEN notification 2 minutes after that. Two policies were rebuilt after querying the project instead of trusting the plan: `cloudscheduler.googleapis.com/job/attempt_count` returns 404 NOT_FOUND here, so the scheduler policy reads the execution log; the Dataform policy's filter matches 28 real failures (below). Records: `docs/verification/2026-09-04-alert-policies.md` and `2026-09-04-policy-rehearsal.md`.
- **12.5 Operations dashboard — delivered** (595bca0). Eleven tiles from a 141-line spec against the 300-line acceptance, because `apply.sh` generates the mosaic layout from tiles declared in reading order. The acceptance criterion is run rather than asserted: `apply.sh rehearse-dashboard` deleted the live dashboard, confirmed it was gone, rebuilt it from the spec and diffed the two layouts. Records: `docs/verification/2026-09-04-dashboard.md` and `2026-09-04-dashboard-rehearsal.md`.
- **Review gate on 12.2 to 12.5** (platform review 12 findings, alignment review 10; commit `baef986` and the assertion fix). The two that mattered most: both policy comparisons listed their fields by hand and omitted `aggregations`, so the reconciler could not see a whole class of spec edit — applying the corrected comparison updated all eleven live policies, which is drift the old one had been hiding; and `--dry-run` was honoured only in first position, so `apply.sh apply --dry-run` ran a real apply, which on a rehearsal means touching production. The verification records were also ticking rows nothing had checked; each ✓ now names the comparison behind it, and every log-match policy reports how many entries its filter selects over 30 days.
- **Ultra review of PR #61** (50 agents, 2 rounds, 31 minutes; 6 findings confirmed, all fixed in `06ef71b`). It found two things the local gate did not, both of a kind the local pair is structurally unlikely to reach. `scripts/deploy-cloud-run.sh` collapsed a failed drift snapshot (`write_diff` rc 2) and real drift (rc 1) into one flag, so `--allow-drift` promoted revisions whose diff had never been computed (CLAUDE.md documents `deploy` then `promote <revision>`, not the flag — my commit message overstated that); that same function had been through two local review rounds this session, and round one is what introduced the rc 2 distinction without updating its caller. And `rehearse-dataform` depended on the nightly assertion failure that this same branch removes, so after merge it would have waited out a 25-minute deadline before declaring the log-match policies unproven. The four nits were real too: `check_differs` and `metric_differs` carried the same subset-comparison blind spot the local review found in the policy path and stopped at; the verify record's sample chain could never reach its `httpRequest.status` arm because `json.dumps({})` is truthy, which was hiding a real HTTP 404 on the event-stream row; `rehearse-dataform` reported every HTTP failure as a strict act-as refusal; and `verify-node24.sh` matched the `@types/node` range with a literal-prefix glob that accepts `^24.13.3` and rejects `~24.13.3`. The run exited on its wall clock with three lines abandoned and reports itself incomplete, so those six are what it confirmed, not necessarily all there was.
- **Closing alignment pass on the last three commits** (11 findings, one Critical, all fixed). The Critical one was mine: the ultra-review fix to `scripts/deploy-cloud-run.sh` replaced `if write_diff …; then` with `write_diff …; DIFF_RC=$?`, and under `set -euo pipefail` a nonzero return aborts there — before the diff path, before the promote instruction, and before both gates. The safety property held only by accident, and the source-text pin I added passed against a branch that never executed. Fixed with `DIFF_RC=0; write_diff … || DIFF_RC=$?`, and `tests/unit/infra/deploy-cloud-run-harness.test.ts` now runs the script against a stubbed gcloud: it fails 4 of 5 against the broken form and passes against the fix. That harness is the pattern this class of script needs — a text assertion cannot fail when the code is unreachable.
- **`assert_volume_anomaly` corrected.** The assertion counted weekend days as pipeline gaps against a weekday-only generator. It now builds its date spine from weekdays and anchors the window to `CURRENT_DATE()` rather than to `MAX(event_timestamp)` — the closing pass showed that a spine ending at the newest row can never contain a missing day, so a pipeline that stopped dead scored a perfect gap rate forever, which is the one failure the assertion exists to catch. Measured against the live table: 22 weekdays in the 30-day window, 0 missing, and three missing weekdays would now fire it. Pinned in `tests/unit/dataform/dataform-models.test.ts`.
- **`apply.sh` is idempotent across the whole spec.** A second run reports 29 unchanged and nothing to create or update. Getting there took one fix: the API omits proto3 zero values, so a `thresholdValue` of 0 came back absent and the two proxy policies reported a phantom update on every apply.

## Found while instrumenting

- **The nightly Dataform production workflow has failed every night for a month.** 28 FAILED completions in the 30 days to 2026-09-04, at 04:00 UTC. One action fails: `iampatterson_assertions.assert_volume_anomaly`, "Assertion failed, expected zero rows"; the other 19 actions succeed, so the models build and only the data-quality assertion trips. Corrected in `0a1c1bf` and `06ef71b`. The new Dataform policy matches new entries only, so it will fire at the next nightly run and route to both channels.
- **sGTM aborts requests when no instance is available.** 16 in the week to 2026-09-04, spread across six hours on four days, now counted by `cloud_run_no_instance` and covered by two policies. The fix (min instances, or spreading the scheduler) belongs to 13.5.
- **Cloud Scheduler publishes no metrics in this project.** Descriptor list and time-series query both come back empty; the execution log is the only signal.

## In progress

- Nothing open on 12.1. The Phase 12 pull request is open as a draft with 12.1 only; 12.2 to 12.5 land on the same branch and the draft flips to ready for Ian's ultra review when the phase is complete (or 12.1 ships alone by 2026-09-24 under the hatch).

## Blocked

- Nothing. gcloud credentials were valid through 17:33 UTC; they expire roughly hourly, and an expired credential pauses only the live checks.

## Decisions this session

- Decision #44 (guv adoption path): archive 1–11, plan 12–14 as a new initiative, three phases for three ultra reviews; Claudish and mobile-perf follow-ups moved to a post-initiative roadmap.
- Decision #45 (golden gate semantics): the gate mirrors production's fall-through rule and fails when more than half the cl2en cases fall through to the Claude lane; a fall-through is logged with its cause. Observed: one Gemini stall past the 3 s first-token deadline in two runs on 2026-09-04, an operational signal for 12.4's alerting (a `loop_fell_through` rate policy).
- Preview deploys for verification are agent-initiated through the linked Vercel CLI; production deploys stay Ian's (the merge).
- Decision #46 (verify the signal before writing the policy): every metric type in `spec/policies.json` and `spec/dashboard.json` was queried against the live project before it was applied. Two of them did not exist, and both were caught this way rather than by an alert that never fired.
- Decision #47 (a rehearsal must not hold the site in a degraded state): `rehearse-policy` turns the kill switch off as soon as the refused request is logged, because the log entry is what fires the alert. The wait for the notification runs with the proxy serving normally. Exposure went from up to 25 minutes to 109 seconds.

## Evaluator results

- Spec alignment (phase entry): 10 findings, 9 applied, 1 no-action (tracker session pointer resolves with this file).
- Alignment review of 12.1: 9 findings; 4 Major fixed (record clauses, deploy-script structure, health probe, golden semantics), 1 Major already fixed, 4 Minor fixed (provenance, acceptance wording, doc drift, verify-script fragility). Round 1: commit 0e1af88.
- Platform code review: the first fork inherited the main model and was stopped after 3 of 8 angles (harvested and fixed in 0e1af88). Round 2 ran on an Opus-pinned subagent invoking `/code-review high .`: 9 findings (2 HIGH: suite rows ignored exit codes; the deploy script's clean verdict was the absence of a string), all fixed in e98a89e, plus `@types/node` 24 on the three services. Gate log appended (f1ee270) covering the Phase 12 commits with both instruments `ran`.
- Gate log: every commit in `1febcba..HEAD` is covered — `audit` returns `verdict=all-gated-in-band`. Three entries: the local pair over `1febcba..e1cc585`; the cloud ultra run on PR #61 at head `60cfe5b`; and the closing alignment pass over `0a1c1bf..06ef71b`, each entry also covering the fixes it produced. One correction: an earlier note named `1b97deb` as the uncovered assertion fix, which is a pre-replay local hash that is not in the branch — `api-push.py` rewrites SHAs, so the gate log has to be written after the push, not before. The live commit is `0a1c1bf`.
- 12.2 to 12.5 had not been through the review gate when this line was first written. Their machine records are in `docs/verification/`, but neither the alignment reviewer nor the platform review has seen them.
- Verification record after round 2: 24 of 25 machine checks; the one miss is the golden gate at 33/34 on cl2en-13 (first person), the known v11 prompt-variance case parked under Decision #42; zero fall-throughs to the Claude lane.

## Test state

Root 1,821 passing across 168 suites under Node 24.20.0 (exit 0), up 28 from the monitoring and script pins; `npm run build` and `npm run lint` both clean (exit 0). Service suites unchanged: event-stream 26, data-generator 180, claudish-proxy 264 (35 live-gated skips). Golden gate 33/34 (cl2en-13 only).

## Live state at close

Nothing is left in a rehearsal's hands: the proxy's `KILL_SWITCH` is `off`, all five uptime checks are back on their spec paths, the dashboard exists, and `apply.sh apply` reports 29 unchanged. The working tree is clean and the branch matches `origin` at `06ef71b`.

## Next steps

1. PR #61 is ready for review and carries all of Phase 12. Merging is Ian's. Hold the merge until after 04:00 UTC if the Dataform firing record is wanted (next step 3); after that it costs nothing to keep.
2. Phase 13 is next; the resolver's serial pick is 13.1 (retention and cost controls). 13.2, 13.3, 13.4, 14.1 and 14.2 are also ready. 13.5, the runbook, is blocked on 13.1 and will inherit the failure modes recorded above.
3. Watch for the Dataform alert at 04:00 UTC. It is the only chance to bank a real firing for the log-match policy kind: the corrected assertion reaches the nightly run through the `dataform` branch on merge, and after that no log-match policy in this project can be fired on demand. `rehearse-dataform` now says so and stops rather than waiting out its deadline.
4. The `assert_volume_anomaly` failure is fixed on this branch, not in production. The `dataform` branch is mirrored from `main` by a GitHub Action, so the corrected assertion reaches the nightly run only after the merge. Tonight's 04:00 UTC run still fails, which is what fires the Dataform policy and gives the log-match class its firing record; append it to `docs/verification/` when it lands.
