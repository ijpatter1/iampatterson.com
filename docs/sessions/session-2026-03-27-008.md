# Session 008, Phase 4: Background Data Generator

**Date:** 2026-03-27
**Phase:** 4, Background Data Generator
**Branch:** `phase/4-background-data-generator`

---

## Completed

### Core Types & Configuration (1868722)
- TypeScript types for all three business models: `EcommerceProfile`, `SubscriptionProfile`, `LeadGenProfile`
- Channel configs with 6 platforms (google, meta, tiktok, email, organic, direct) and 3-4 intentional UTM naming inconsistencies per campaign
- `SeasonalityConfig` with monthly, day-of-week, and hour-of-day multipliers
- `GeneratorConfig` with all configurable parameters (dailySessions, growth rate, backfill months, seed)
- Config validation module catching distribution sum errors, rate bound violations, profile/model mismatches
- Seeded PRNG (`SeededRandom`) with mulberry32 for reproducible generation
- Default profiles for all three Tuna demos matching `docs/CONTENT_GUIDE.md` products/plans/campaigns

### E-Commerce Engine (c348f4b)
- Full Tuna Shop funnel: page_view → product_view(s) → add_to_cart → begin_checkout → purchase
- Weighted product selection, configurable funnel drop-off rates, realistic cart sizes
- Session context with channel selection, UTM params, consent simulation

### Subscription & Lead Gen Engines (fa62b94)
- Subscription: page_view → plan_select → trial_signup, plus lifecycle (renewal, churn, upgrade/downgrade) with cohort churn curves
- Lead Gen: page_view → scroll_depth → form_start → form_field_focus → form_complete → lead_qualify with budget/partnership-based qualification scoring

### Orchestrator, Ad Platform, Transport (bf90faa, 9295bdd)
- Generator orchestrator: backfill (configurable months), daily, and date-range generation modes
- Ad platform data: daily spend/impressions/clicks records with seasonality and naming inconsistencies
- HTTP transport to sGTM via GA4 Measurement Protocol with session-grouped batching
- Express server: `/generate`, `/backfill`, `/health`, `/stats` endpoints with dry-run mode
- Dockerfile for Cloud Run deployment

### Evaluator Fixes (e99cafe)
- Fixed daily session count bug (hour-of-day multiplier was suppressing volumes to ~20%)
- Wired subscription lifecycle events into orchestrator (was dead code)
- Fixed transport to batch events per session (was 1 HTTP request per event)
- Used simulated timestamps in order/lead IDs instead of wall clock time
- Fixed false green growth test

## Commits

- `1868722`, feat(data-generator): scaffold project with types, profiles, validation, and seeded RNG
- `c348f4b`, feat(data-generator): add session context and e-commerce engine with funnel simulation
- `fa62b94`, feat(data-generator): add subscription and lead gen engines
- `bf90faa`, feat(data-generator): add ad platform data, backfill, and generator orchestrator
- `9295bdd`, feat(data-generator): add HTTP transport, Express server, and Dockerfile
- `f2bdc7e`, chore(data-generator): add barrel export for public API
- `e99cafe`, fix(data-generator): address evaluator critical issues

## Test State

- **148 tests passing** in data-generator (12 suites)
- **242 tests passing** in main project (32 suites)
- Build clean (Next.js + TypeScript strict)
- Data-generator TypeScript compiles cleanly

## Evaluator Results

**Score: 3.15/5, PASS WITH ISSUES** (critical issues subsequently fixed)

### Fixed Issues
1. Subscription lifecycle events now wired into orchestrator
2. Daily session count no longer suppressed by midnight hour multiplier
3. Transport batches events per session in single MP requests
4. Order/lead IDs use simulated timestamps
5. Growth test validates actual growth behavior

### Remaining Minor Issues
- Lead gen missing `meeting_booked` and `deal_closed` follow-up events (meetingRate configured but unused)
- Leadgen scroll_depth and form_field_focus use type assertions instead of proper event types
- No input validation on server `model` parameter (invalid values caught by try/catch, not explicit validation)

## Next Steps

1. **Deploy to Cloud Run**, Build and deploy the data-generator service
2. **Run initial backfill**, Generate 18 months of historical data for all three models
3. **Set up Cloud Scheduler**, Configure daily trigger for ongoing generation
4. **Add meeting/deal events** to lead gen funnel (minor evaluator issue)
5. **Phase 5**, Data Infrastructure (Dataform transformations on the generated data)
