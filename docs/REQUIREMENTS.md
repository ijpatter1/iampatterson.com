# iampatterson.com — Development Plan & Requirements

> **Lineage:** Phases 1–11 — greenfield build through the Claudish launch, archived at `docs/initiatives/001-greenfield-through-launch/`.
> **This initiative:** Phases 12–14 — governed by `docs/spec/operational-readiness.md`.

## Project Vision

iampatterson.com is the consulting website for Patterson Consulting and a live demonstration of a full measurement stack: Cookiebot consent, a client-side GTM data layer, self-hosted server-side GTM on Cloud Run, GA4 and BigQuery, a real-time overlay that shows visitors their own session moving through the pipeline, Dataform marts, a self-hosted Metabase, and the Claudish translator. Everything a visitor can see is built and running as of the archived initiative; the site is the portfolio.

This initiative turns that working demo into a production system one person can hand off or maintain without babysitting. It adds the monitoring, alerting, uptime checks, cost and retention controls, an operational runbook, a dependency cadence, and declarative deploys for the two infrastructure surfaces that still run on one-shot scripts. The operational layer is part of what the site demonstrates, so it is built to be read as well as run.

One constraint shapes the plan more than any other: each phase ships as one pull request that stays under the ultrareview ceiling (500 changed files, 8,000 changed lines), so the three free ultra reviews cover the three phases. The Node.js 24 runtime is the first deliverable because Vercel stops building Node 20 projects on 2026-10-01.

---

## Phase 12 — Runtime currency and observability

**Goal:** Meet the Node 24 deadline and stand up the monitoring foundation: a notification channel, uptime checks, log-based metrics, alerting policies and a dashboard, all as committed configuration applied by script.

**Deliverables:**

1. **[12.1]** Node.js 24 runtime: pin `engines.node` to `24.x`, move both stages of the three Cloud Run Dockerfiles to `node:24-slim`, record the Node 24 pin and the Homebrew arm64 toolchain note in the CLAUDE.md project facts (retiring the "Runtime deadline" fact), update the Node references in ARCHITECTURE, and confirm the sync-dataform workflow pins no Node; all suites and the proxy golden gate pass under Node 24, the Vercel production build carries no deprecation warning, and the three services are redeployed on the new image with healthy endpoints `[deps: none]`
   - *Acceptance:* root suite, three service suites and `scripts/run-claudish-golden.sh` green under Node 24 locally; the absence of the Node 20 warning confirmed in a Vercel build log (a preview build before the merge is enough; `scripts/verify-node24.sh` records it in `docs/verification/`, no card); each redeploy diffs the new revision's env, scaling and service account against the prior one before traffic moves, the diff kept under `docs/verification/deploys/`; `event-stream`, `data-generator` and `claudish-proxy` answer `/health` on new revisions; the proxy's live smoke passes in both directions.
2. **[12.2]** Notification channel and uptime checks: an email channel (Slack optional) and Cloud Monitoring uptime checks on sGTM `https://io.iampatterson.com/healthy`, event-stream `/health`, claudish-proxy `/health`, `https://www.iampatterson.com/` and the Metabase load balancer (the IAP redirect counts as healthy), every check alerting to the channel, all declared under `infrastructure/monitoring/` and applied by `apply.sh`, with one deliberate failure rehearsed and its notification recorded `[deps: none]`
   - *Acceptance:* five checks green in Cloud Monitoring; one check driven to failure on purpose produces a notification inside its window; the rehearsal is recorded in a manual task card.
3. **[12.3]** Log-based metrics and retention: metrics for the structured error events the proxy already emits (`request_error`, `loop_fell_through`, `loop_retry_failed`, `loop_empty_result`, `loop_failed_midstream`, `lane_failed`, `capacity_ladder_exhausted`) and for data-generator's `[ad-insert]` failure line, a recorded note that event-stream emits no error logs today, a provisional retention period for the `_Default` log bucket to be confirmed against the 13.1 data retention decision, a sink if anything must outlive it, and one "what failed in the last 24 hours" query each for sGTM, event-stream, data-generator and claudish-proxy for the runbook `[deps: none]`
   - *Acceptance:* metrics listed by `gcloud logging metrics list`; the provisional retention value applied and recorded in ARCHITECTURE with the 13.1 confirmation pending; the four queries run without error and each returns the injected failure from the 12.2 rehearsal.
4. **[12.4]** Alerting policies routed to the channel: Cloud Run 5xx rate per service, container restarts and crash loops, Pub/Sub oldest unacked age and undelivered count on the push subscription, Cloud Scheduler job failures, Dataform run failures, BigQuery daily spend, the claudish-proxy `budget_threshold` and `capacity_no_budget` log events, and managed certificate expiry, each with a documented threshold, any log-based metric it needs, and either a recorded test firing or a written reason it cannot be fired safely `[deps: 12.2, 12.3]`
   - *Acceptance:* every policy present with its threshold in a committed spec; a firing record or a safety note per policy; the policy specs applied by an idempotent script.
5. **[12.5]** Cloud Monitoring dashboard generated from a committed spec under `infrastructure/monitoring/` and applied by script: request rate, latency and error rate per Cloud Run service, Pub/Sub throughput and backlog, BigQuery slot usage and daily cost, scheduler outcomes, certificate status and the proxy's per-instance budget threshold crossings (`budget_threshold` at 50/80/100 %) `[deps: 12.4]`
   - *Acceptance:* the dashboard exists and can be deleted and recreated from the spec; the spec is under 300 lines because the panels are generated.

**Why this is Phase 12:** the runtime deadline is dated, and every later phase's rehearsals need a channel and alerts to land in.

**Validation:** the suites named above, the review gate (`/guv:eval`), a session handoff, and manual task cards for the rehearsals. The phase ships as one pull request under 8,000 changed lines.

---

## Phase 13 — Cost, lifecycle and the runbook

**Goal:** Put retention and cost controls in place, decide the sGTM image lifecycle, adopt the Cloud Run services into declared identity and configuration, set a dependency cadence, and write the runbook that ties every alert to a procedure.

**Deliverables:**

1. **[13.1]** Retention and cost controls, measured by a script under `infrastructure/retention/` and recorded in `docs/verification/`: measure partition expiration on every raw and staging table, table sizes, GCS lifecycle rules and Dataform schedule state; then apply partition expiration on raw event tables, lifecycle rules on the AI export bucket — `gs://iampatterson-ai-exports`, declared by `infrastructure/bigquery/ai_access_layer/setup.sh` and not provisioned as of 2026-09-04, so the deliverable either creates it with rules or records that the access layer never shipped — a notification channel on the existing `metabase-project-budget`, and a Vertex spend budget for the Claudish proxy if the in-app cap is not enough on its own; and confirm or change the provisional `_Default` log bucket retention 12.3 handed forward, clearing the `status` field in `infrastructure/monitoring/spec/retention.json` `[deps: 12.2]`
   - *Acceptance:* a before/after table in the deliverable's record under `docs/verification/`; every budget carries a channel; retention values — BigQuery partition expiration, GCS lifecycle and `_Default` logs — recorded in ARCHITECTURE with the provisional marker gone.
2. **[13.2]** sGTM container lifecycle: a recorded decision between the floating `:stable` tag and a pinned digest, and an operator script with a dry run that updates `gtm-cloud-image` on `sgtm` and `sgtm-preview`, rehearsed once end to end on `sgtm-preview`, with its runbook entry drafted for 13.5 rather than written into a runbook that depends on this deliverable `[deps: none]`
   - *Acceptance:* the decision in ARCHITECTURE; the script's dry run and one real run on `sgtm-preview` recorded; the runbook entry drafted for 13.5 to absorb.
3. **[13.3]** Dependency update process: a cadence documented at `docs/runbook/dependency-cadence.md` for Node, Next.js and React, the Cloud Run base images, the `gtm-cloud-image` releases whose update procedure 13.2 decides, Metabase (baseline v0.59.6, updated through the release-notes gate `infrastructure/metabase/upgrade.sh` already enforces), npm dependencies and security advisories, with a monthly "read the platform notices" step, and Dependabot or an equivalent configured for the root and the three services `[deps: 12.1, 13.2]`
   - *Acceptance:* the configuration committed and at least one update pull request produced; the cadence doc names the reviewer and the schedule, and carries a row for every surface listed above.
4. **[13.4]** Cloud Run adoption: the claudish-proxy service, service account, IAM and secret imported into the declarative layer per `infrastructure/cloud-run/claudish-proxy/IMPORT_PLAN.md` with `KILL_SWITCH` and the source-deploy fields in `ignore_changes`, `event-stream`, `data-generator`, `sgtm` and `sgtm-preview` moved off the default compute service account onto dedicated runtime accounts with least-privilege roles, and the scaling settings measured before they are declared: the 12.3 `cloud_run_no_instance` metric shows sGTM aborting requests at `maxScale=3` and data-generator aborting cold-start runs behind its three schedulers, so the configuration adopted is the corrected one on both — sGTM's ceiling, and data-generator's minimum instances or scheduler spread — and not today's; the abort counts recorded on 2026-09-04 disagree between this line's earlier 16 for sGTM and the 13 for sGTM plus 1 for data-generator in `docs/verification/2026-09-04-first-real-alert.md`, which are different query windows, so the deliverable re-measures both before changing anything `[deps: 12.3, 13.7]`
   - *Acceptance:* a plan run shows no drift after import; each service's runtime account and roles listed in ARCHITECTURE; every service answers its health endpoint after the identity change; the sGTM and data-generator abort counts recorded from the 12.3 metric before the scaling change and again after.
5. **[13.5]** Operational runbook at `docs/runbook/`: one entry per failure mode (sGTM not responding, event pipeline backlog, Dataform assertion failure, data generator stuck, a Cloud Run revision that will not start, rolled back with `scripts/deploy-cloud-run.sh promote <revision>`, the site or the Metabase load balancer failing its uptime check, BigQuery daily spend above the alert threshold, certificate renewal failure, Claudish proxy over budget or misbehaving — kill switch, revert to a previous revision, the golden gate — a Vercel build failing on runtime deprecation, expired gcloud credentials during an incident, preview protection for automated checks), each linked to the alert that leads to it and either rehearsed once or carrying a written reason it cannot be rehearsed safely, written for a reader who has never seen the stack; the Vercel automation-bypass secret is its manual task `[deps: 12.4, 13.1, 13.2, 13.4]`
   - *Acceptance:* every one of the sixteen Phase 12 alerts — the eleven policies in `infrastructure/monitoring/spec/policies.json` plus one per uptime check — links to an entry; one dated rehearsal per entry or a written reason it cannot be rehearsed safely; the manual task card for the bypass secret exists.
6. **[13.6]** Terraform layer recovered onto the phase branch: the twenty-three files of `infrastructure/terraform/` and `tests/unit/infrastructure/` from the unmerged `phase/11-operational-readiness` branch (PR #56, open since 2026-06-04) brought across without their obsolete tracker edits, the `@cdktf/hcl2json` devDependency, and `.github/workflows/infra-terraform.yml` whose plan and apply jobs stay inert until 14.3 sets `GCP_WIF_PROVIDER`; nothing is applied and no state is written; and the provider boundary the branch documented — Terraform owns the GCP backend, GTM container config stays reconciler-managed, Vercel stays on its own pipeline — recorded in ARCHITECTURE in place of the "Terraform is not introduced for them yet" line `[deps: none]`
   - *Acceptance:* the four Terraform suites green in the root run (48 tests) with no regression elsewhere; `terraform fmt -check` and `terraform validate` clean; one read-only `terraform plan` captured in `docs/verification/` as 13.7's recorded starting point, showing the six orphaned entries; no `terraform apply` in this deliverable; the provider-boundary decision in ARCHITECTURE; PR #56 named as superseded.
7. **[13.7]** Terraform state reconciled to a clean plan: the remote state holds forty-six resources against a configuration that declares forty, because a session on 2026-06-08 imported the four BigQuery datasets and the Pub/Sub topic and push subscription without committing their configuration, so `terraform plan` reads "0 to add, 1 to change, 6 to destroy" and an apply would take the live event pipeline with it; author the missing BigQuery and Pub/Sub configuration to match live or remove those entries from state, recording which and why, and settle the one real drift — state and live pin `data-generator` traffic to an exact revision while the configuration wants `TRAFFIC_TARGET_ALLOCATION_TYPE_LATEST`, against `scripts/deploy-cloud-run.sh promote` — with `ignore_changes` on the traffic block, the treatment `IMPORT_PLAN.md` already specifies for `KILL_SWITCH` `[deps: 13.6]`
   - *Acceptance:* `terraform plan` reports no changes, captured in `docs/verification/`; a test pins the `ignore_changes` set so a later edit cannot silently drop the traffic or `KILL_SWITCH` exclusion; the recovered or removed resources listed with the decision in ARCHITECTURE; every service answers its health endpoint and the event pipeline still delivers, checked after reconciliation.

**Evidence for 13.4's scaling clause (2026-09-04).** The `cloud_run_no_instance` metric counted 13 aborts on `sgtm` and 1 on `data-generator` in the week to 2026-09-04. The data-generator abort is the one the alerting caught unprompted at 20:40 UTC, recorded in `docs/verification/2026-09-04-first-real-alert.md`: the service runs with no minimum instances behind three schedulers firing at :00, :20 and :40 across nine weekday hours, so every run is a cold start. sGTM is the systematic case at `maxScale=3`; data-generator is the incidental one. Both are the same fix and both belong to 13.4's measurement.

**Carried into Phase 13 from 12.4 — history, not open work (2026-09-04).** The nightly Dataform `production` workflow had completed FAILED every night for a month on one action, `iampatterson_assertions.assert_volume_anomaly`, while the other 19 succeeded. It counted days with no events across the full data range, and every empty day was a weekend, which the weekday-only generator produces by design. **Corrected in `0a1c1bf` and `06ef71b`**: the spine counts weekdays only and is anchored to `CURRENT_DATE()` rather than to the newest row, so a pipeline that stops and stays stopped is now detectable — three missing weekdays in the 30-day window trips it. What Phase 13 actually inherits is two things: 13.5 needs a "Dataform assertion failure" runbook entry, and the Dataform alert policy has no firing record, because correcting the assertion closed the only window in which a `conditionMatchedLog` policy in this project could be fired on demand.

**Errata on the sealed Phase 12 (2026-09-04).** Two acceptance clauses were not met as written. Phase 12 is complete and immutable, so they are recorded here rather than corrected in place, and `docs/PHASE_STATUS.md` carries a pointer to this paragraph.

The first is a divergence from the governing spec, not only from these requirements: `docs/spec/operational-readiness.md` (deliverable 2) asks that "the manual task records the received alert", and 12.2's acceptance repeats it as a manual task card. No card was written. The rehearsal was scripted instead and recorded in `docs/verification/`, on the standing rule that nothing a machine can verify goes on a card. That rule does not fully cover this case, and the substitution is narrower than it looks: the scripted rehearsal observes the Pub/Sub receipt, which is what proves a policy fired, but it cannot observe that the email arrived. **Closed on 2026-09-04 by observation rather than by script:** Ian confirmed receipt of all four rehearsal notifications and of the first real alert, so `ops-email` does deliver despite an unset `verificationStatus`. The record is `docs/verification/2026-09-04-first-real-alert.md`. The general point stands for 13.5 — a channel's delivery is a person's observation, not a machine's — but it is no longer an open question for this channel.

The second was not satisfiable as written: 12.3's acceptance asks that each of the four 24-hour queries "returns the injected failure from the 12.2 rehearsal", but one rehearsal on one uptime check cannot inject a failure into four services, and the data-generator query correctly returns zero entries. The lesson for Phase 13 is to amend an acceptance clause while its phase is still open, before the last deliverable is marked complete.

**Why this is Phase 13:** the controls need the channel from Phase 12, and the runbook needs the alerts, the retention decisions and the identity changes to describe.

**Validation:** scripts and tests named per deliverable, the review gate, a session handoff, manual task cards. One pull request under 8,000 changed lines.

---

## Phase 14 — Declarative infrastructure

**Goal:** Replace the one-shot deploy scripts for GTM and the Metabase load balancer with spec-driven reconcilers, run them from a workflow with a dry-run diff on pull requests and a gated live apply, and use the GTM reconciler to wire the two events still waiting on it.

**Deliverables:**

1. **[14.1]** GTM reconciler: the census recorded in `docs/verification/2026-09-08-gtm-container-census.md` found both committed specs materially incomplete, so `infrastructure/gtm/server-container.json` is first regenerated from live — the brownfield contract ARCHITECTURE states for Terraform, converge onto live and never destroy-and-recreate, since applying that spec as written would create nine triggers and three duplicate tags against a working pipeline — while `web-container.json` stands as authoritative — ahead of live by the four Claudish pipelines `deploy-claudish.js` never applied, and behind it on the shared `gtes` event-settings variable and the `measurementId` every live tag already carries, both adopted into the spec rather than applied against live; **and divergent from it on consent, which is this deliverable's largest visitor-facing effect**: the spec has declared `analytics_storage: required` since Phase 1 and the container has carried `notNeeded` on every tag throughout, so the apply gates twenty-one of twenty-two tags and a visitor who declines analytics stops generating GA4 cookieless pings — the record conflict this resolves, and what it costs, are recorded in ARCHITECTURE's "Where the gate actually was"; because that gate makes a declining visitor's overlay legitimately empty, `buildRouting` and the timeline's empty state are corrected in the same deliverable so the overlay reports what happened rather than asserting delivery; built-in variables, folders and custom templates are declared outside the reconciler's ownership and exempt from both the drift report and `--allow-deletes`; then `infrastructure/gtm/reconcile.js` diffs the committed specs against the live Default Workspace on each container through a documented mapping between the specs' human-readable shapes (`firingTrigger` by name, `parameters` as a map) and the Tag Manager API v2 resources (`firingTriggerId`, `parameter[]`), applies adds and updates, takes deletes only behind `--allow-deletes`, defaults to dry run, and creates and publishes a container version only behind `--publish`, defaulting off, so dry run, apply and publish are three distinct steps; the web apply lands the four `claudish_*` pipelines as a consequence, and their arrival in `events_raw` is proven rather than assumed; the deploy identity is `gtm-reconciler@iampatterson.iam.gserviceaccount.com`, a member of GTM account `6346433751` by manual card and reached by impersonation locally and Workload Identity Federation in CI, never by a key; the four assertions in `tests/integration/web-container-spec.test.ts` that parse `deploy-phase6.js`'s source are re-homed onto the reconciler's spec-to-API mapping rather than deleted, and a `bridgeToGtagConsent` ↔ container `consentSettings` parity pin is added while that code is open; `deploy-phase6.js` and `deploy-claudish.js` are both retired once parity is reached `[deps: none]`
   - *Acceptance:* the census recorded before the first apply (done, 2026-09-08); `server-container.json` regenerated from live with a dry run against it reporting no drift; a dry run after the web apply reports no drift and the published version id recorded; `claudish_translate`, `claudish_detected`, `claudish_share` and `claudish_rate` queryable in `events_raw` after the publish, against the zero rows the fourteen days before it returned; the integration pin green with its four re-homed assertions and the consent parity pin, both failing against a mismatched spec; every GA4 tag but `GA4 - consent_update` requiring `analytics_storage` in the published container, and a declining visitor shown neither a delivery claim nor a waiting message; `deploy-phase6.js` and `deploy-claudish.js` deleted with their callers updated.
2. **[14.2]** Metabase load-balancer retirement onto the Terraform layer: confirm `infrastructure/terraform/metabase-lb.tf` describes the live forwarding rule, the URL map with the `/app/*`, `/api/*` and `/embed/*` split, both backend services and the managed certificate against a captured `terraform plan` no-op, rather than reimplementing that topology in a second writer against the provider boundary ARCHITECTURE records; give the residue neither Terraform nor that file covers a home — the IAP OAuth brand and client with their two Secret Manager versions stay a documented console procedure, the IAP service agent's `run.invoker` binding is declared, and the `roles/iap.httpsResourceAccessor` allowlist goes to a `docs/runbook/` procedure rather than declared configuration, because declaring it would make removing a name from a file revoke that person's access on the next apply and reverse the additive-only design `setup-iap.sh` documents; then retire `setup-domain.sh` and `setup-iap.sh`, recording in ARCHITECTURE that the Metabase load balancer is Terraform-owned and that the reconciler pattern stays reserved for GTM container configuration `[deps: none]`
   - *Acceptance:* the `terraform plan` no-op captured in `docs/verification/`; `tests/unit/infrastructure/terraform-metabase-lb.test.ts` green and extended to cover any resource added; each residue item has a named home and either a test or a runbook entry; the two one-shots deleted with their callers updated and no live capability lost.
3. **[14.3]** Reconcile workflow: `.github/workflows/infra-reconcile.yml` runs the GTM reconciler in dry-run mode on pull requests touching `infrastructure/gtm/**` and posts the diff as a comment, passing reconciler output through `env:` rather than `${{ }}` interpolation — the expression-injection sink `004f7a3` removed from the sibling workflow, pinned by a check over `.github/workflows/*.yml` — and runs the live apply on merge to `main` behind an `infra-production` environment with manual approval, authenticated through Workload Identity Federation with no service-account key in the repository; that environment is created with a required reviewer **before** `vars.GCP_WIF_PROVIDER` is set, because the same variable arms `.github/workflows/infra-terraform.yml`'s `terraform apply -auto-approve` on every push to `main` and a referenced-but-missing environment is created unprotected, and it is set only after every import in flight in this phase has landed and `terraform plan` is a captured no-op, so no unattended apply performs the import of a live service; each apply job's first step asserts its environment carries protection rules and stops loudly if it does not; the WIF pool, the provider with its repository-scoped attribute condition and the deployer service account are given an owner — declared in Terraform, or created by an operator script and recorded, stated on entry — with the deployer's roles recorded and justified rather than minimised; and `docs/runbook/` gains an entry for a reconciler or Terraform apply that damaged production, keeping `tests/unit/infra/runbook.test.ts` green. The Metabase path is not covered here: 14.2 leaves that topology to `infra-terraform.yml`'s existing `infrastructure/terraform/**` trigger `[deps: 14.1]`
   - *Acceptance:* one pull request shows the dry-run comment; one merge shows the approval gate and a successful apply on each of the two workflows that consume the variable; the `infra-production` required-reviewer setting evidenced by `gh api repos/…/environments` and recorded in a manual task card; the workflow-injection check failing against the pre-fix shape; WIF configured and recorded in ARCHITECTURE.
4. **[14.4]** `web_vital` and `page_engagement` wiring: data layer variables for the seven parameters the two events carry — `metric_name`, `metric_value`, `metric_rating`, `metric_id`, `navigation_type`, `engagement_seconds`, `max_scroll_pct` — plus custom-event triggers and GA4 event tags in the web container spec, applied and published through the reconciler, so real-user Web Vitals and engagement rows land in `iampatterson_raw.events_raw` and the `web_vital` coverage chip on the Overview tab stops claiming a pipeline the container does not carry; the sGTM container needs no route change, and the deliverable confirms that rather than building it, its three destination tags all firing on `clientName - GA4`, a `type=always` trigger filtered on `{{Client Name}} contains GA4` — not the `All GA4 Events` trigger the committed spec, `deploy-claudish.js:8` and `pubsub-tag-template.js:16` name, which the 2026-09-08 census found does not exist `[deps: 14.1]`
   - *Acceptance:* both events queryable in BigQuery from production traffic within a day of the publish, against the zero rows the fourteen days before it returned; `tests/integration/web-container-spec.test.ts` extended to assert a `customEvent` trigger, a GA4 tag bound to it and the parameter-to-DLV bindings for each of the two events, with the new assertions failing against the pre-14.4 spec.
5. **[14.5]** Claudish adoption completed and the identity layer declared: import `google_cloud_run_v2_service.claudish_proxy` into `cloud-run.tf` with `client`, `client_version`, `build_config`, the rolling image and `template[0].containers[0].env` in `ignore_changes` — the `KILL_SWITCH` treatment `IMPORT_PLAN.md` specifies and calls the class of thing that plan exists to catch — adding `claudish_proxy` to the `SERVICES` and `SOURCE_DEPLOY` arrays in `tests/unit/infrastructure/terraform-cloud-run.test.ts` with a pin on the env exclusion, and a structural pin asserting every service directory under `infrastructure/cloud-run/` has a matching `google_cloud_run_v2_service` declaration, which runs offline and would have failed on 2026-09-03; declare the 13.4 runtime-account role grants and the proxy's `aiplatform.user` binding so the least-privilege posture is described rather than only applied, this root holding no IAM resources of any kind today; take the recorded decision on `IMPORT_PLAN.md`'s unused break-glass secret; correct ARCHITECTURE's 13.4 "Still open" paragraph, which names `roles/editor` and the Secret Manager and IAM halves but omits the service itself; and record what still depends on `roles/editor` on the default compute account as a finding and a recommendation, explicitly without revoking it `[deps: none]`
   - *Acceptance:* `terraform plan` reports no changes after the import, captured in `docs/verification/`; one test fails if the env exclusion is dropped from `claudish_proxy` and another fails if a service directory has no declaration; `claudish-proxy` answers `/health` and the golden gate passes after the import; the secret decision, the `roles/editor` finding and the corrected "Still open" paragraph recorded in ARCHITECTURE.
6. **[14.6]** Session key resolved so the warehouse holds real visitors: `session_id` is reserved in gtag.js, which consumes it before the hit is built, so every real-browser row reaches `iampatterson_raw.events_raw` with it NULL while the generator — hand-building its query string against the same `/g/collect` endpoint — sets it explicitly; all twelve Dataform models keyed on that column and `stg_sessions` filtered `WHERE session_id IS NOT NULL`, so the session layer held 193,323 sessions of which zero were real and every mart and Metabase dashboard above it described the generator alone; resolve `COALESCE(iap_session_id, session_id)` at the staging boundary in the passthrough column, the dedup partition and the `event_id` hash, matching the precedence `pubsub-tag-template.js` already uses so the warehouse and the real-time overlay key a visitor identically; fix `is_synthetic`, which derived from `iap_source` that the real site also sends and therefore labelled every recovered visitor synthetic, by having the generator send its own `iap_synthetic` marker with its user agent as the fallback for rows already in the sixty-day raw window; and replace the `assert_stg_sessions` null check — which tested a predicate `stg_sessions` itself filters out and so could never fail — with one that asserts non-synthetic sessions exist, plus a generic check that no assertion tests a null its source model removes `[deps: none]`
   - *Acceptance:* real sessions present in `stg_sessions` with `is_synthetic = FALSE` after a Dataform run; the resolved key measured against live raw data before and after; a test that fails when the SELECT-list resolution is reverted, and one that fails against the historical vacuous assertion, both proven by inducing those exact states; the RAG snapshot tables rebuilt from the corrected marts and the `01_funnel_conversion_by_channel` session threshold lowered so real volume is not filtered out; the sixty-day raw partition expiry recorded as the limit of what history can be recovered.

**Errata on the sealed Phase 13 (2026-09-08).** Two acceptance clauses were not met as written. Phase 13 is complete and immutable, so they are recorded here rather than corrected in place, exactly as Phase 12's were recorded in the Phase 13 notes above.

The user approved amending both through `/guv:replan`, and the engine refused both: `status=REFUSED — phase 13 is completed and immutable`. That refusal is correct and the amendment was not forced by hand. What follows is therefore an erratum, not a rewrite.

The first is **13.3's "at least one update pull request produced."** Dependabot reads its configuration from the repository's default branch, so no pull request can exist until this work merges to `main`. The clause is unmeetable from a feature branch by construction — not by omission — and the deliverable was marked ✅ with the gap named in its tracker entry and its verification record rather than hidden. What it should have said: *the configuration committed, and a Dependabot pull request confirmed after merge.* **Discharged 2026-09-08**: PR #62 merged at 16:05 UTC and Dependabot opened two pull requests within seconds — #63 and #64, bumping `node:24-slim` to `node:26-slim` for `data-generator` and `claudish-proxy`. They arrived ungrouped and one per service, which is exactly what the docker entries specify and the reasoning behind them: a base image is a runtime change and earns its own review. The substance of the clause is met even though its wording could not be amended.

The second is **13.4's "abort counts recorded from the 12.3 metric before the scaling change and again after."** The before half is impossible as written: log-based metrics are not retroactive, the `cloud_run_no_instance` metric was created on 2026-09-04, and queried over seven days it returns a single abort — a misleading all-clear against the 96 the logs actually record for the 30 days to 2026-09-05. Substituting the logs was the right call and finding the trap was worth more than the clause, but the clause was substituted rather than amended, and the *after* half needed no substitution at all since the metric is valid forward. What it should have said: *recorded from the logs before the change, and from the 12.3 metric after.*

**The lesson, twice now.** The Phase 12 errata above closed with "amend an acceptance clause while its phase is still open, before the last deliverable is marked complete." That sentence was written into this document and then not followed: all seven Phase 13 deliverables were marked ✅ before the review gate ran, which sealed the phase before the gate could find anything wrong with its wording. **The gate belongs before the last ✅, not after it.** Phase 14 runs its review gate while at least one deliverable is still open.

**Why this is Phase 14:** it is the largest surface and the one with the most moving parts, so it goes last, after the alerts that would catch a bad apply exist. If its pull request approaches the ceiling, **14.5** ships as a separate small pull request without an ultra review, and merges *before* the phase pull request sets `vars.GCP_WIF_PROVIDER` and arms the apply. The hatch named 14.4 when the phase held four deliverables; 14.5 is the better candidate because its proof is mechanical — `terraform plan` reports no changes or it does not — so a second expert pass adds little, while delaying 14.4 leaves the `web_vital` coverage chip claiming a pipeline the container does not carry, on the overlay this site exists to demonstrate.

**Validation:** the integration pins, the workflow's own runs, the review gate, a session handoff, and a manual task card for anything that needs a browser or a console — the `infra-production` required reviewer, the two repository variables, the deploy identity's membership on the GTM account, and any OAuth consent-screen work. The review gate runs while at least one deliverable is still ⬜. One pull request under 8,000 changed lines, watched.

---

## Dependencies & Risk Notes

- **One person.** No team coordination, and no one to catch mistakes except the review gate and the tests. Tests come first on every deliverable.
- **Ownership lines.** Pushes, merges, site deploys and model choices are Ian's. The sandbox hook blocks pushes from the CLI; with explicit permission in chat a push can go through the GitHub Git Data API.
- **Secrets and data.** Secrets stay in Secret Manager or environment; nothing sensitive enters the repository, the logs or the analytics events.
- **Pull-request budget.** Each phase is one pull request under 500 changed files and 8,000 changed lines, so an ultra review can run on it. Generated configuration (dashboard panels, alert specs) is produced by scripts from compact specs rather than committed in full. Dependabot's own pull requests are separate.
- **Infrastructure discipline.** Read-only inspection first, then a dry run, then apply; every reconciler and apply script is safe to re-run; deletes are behind flags.
- **Credentials.** gcloud credentials expire hourly and overnight; the default gcloud project is not this one, so every command names the project. An expired credential mid-deliverable is a pause, not a failure.
- **Cost.** The monitoring layer costs cents per month; the retention deliverable measures before it changes anything; budgets gain channels before any new spend.
- **Deadline and its hatch.** 12.1 must merge and deploy before 2026-10-01. If the Phase 12 pull request is not ready to merge by 2026-09-24, 12.1 ships as its own small pull request without an ultra review, the same hatch 14.4 has.

## Post-initiative roadmap

Items that become possible once this initiative lands, kept out of it deliberately so the three pull requests stay reviewable:

- Mobile performance levers and SPA-navigation Web Vitals attribution, measured against the field data 14.4 delivers.
- `useEventStream` status-ref race hardening and the `window.__iapWebVitals()` console helper.
- Claudish follow-ups, each an isolated change judged on all 99 pool pairs: latch thresholds under the served detector rule, the mechanical first-person retry gate, the "lists and tables keep their shape" prompt edit, and a watch on acronym expansion.
