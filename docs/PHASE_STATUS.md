# Phase Status Tracker

> **Current Phase: 14 — Declarative infrastructure (complete — 6/6 ✅; initiative 002 complete)**
> Last updated: 2026-09-08, session-2026-09-08-001 (Phase 13 merged as `035d735`; review gate found 30 findings across three passes, all fixed)
>
> **Amendments:**
> - 2026-09-04 — deps-amend [12.1] (session-2026-09-04-001) — names the real Node references (CLAUDE.md facts, both Docker stages, ARCHITECTURE), adds the toolchain note, records that the workflow pins no Node (alignment finding 1)
> - 2026-09-04 — deps-amend [12.2] (session-2026-09-04-001) — checks declared under infrastructure/monitoring/ and applied by apply.sh, every check alerting, the io.iampatterson.com host named (alignment finding 5)
> - 2026-09-04 — deps-amend [12.3] (session-2026-09-04-001) — metrics scoped to the events the services actually emit, event-stream silence recorded, log retention provisional until 13.1 (alignment findings 2 and 3)
> - 2026-09-04 — deps-amend [12.4] (session-2026-09-04-001) — exact proxy event names, the push subscription, crash loops, and who creates the metrics (alignment finding 8)
> - 2026-09-04 — deps-amend [12.5] (session-2026-09-04-001) — the proxy panel shows budget threshold crossings, the only budget signal the proxy logs (alignment finding 4)
> - 2026-09-04 — errata [12.2, 12.3] (session-2026-09-04-001) — two acceptance clauses were not met as written and the phase is sealed; recorded in the Phase 13 notes of `docs/REQUIREMENTS.md`, not corrected in place (hand-added: the engine refuses a completed phase)
> - 2026-09-04 — deps-amend [13.4] (session-2026-09-04-001) — deps: none → 12.3; sGTM scaling measured and corrected before the services are adopted, so the declared configuration is not the one that aborts requests; deps gain 12.3 for the metric that measures it
> - 2026-09-04 — insert [13.6] (session-2026-09-04-002)
> - 2026-09-04 — insert [13.7] (session-2026-09-04-002)
> - 2026-09-04 — deps-amend [13.4] (session-2026-09-04-002) — deps: 12.3 → 12.3, 13.7; the proxy is imported into a layer that must exist and plan clean first; 13.7 is that gate
> - 2026-09-05 — deps-amend [13.1] (session-2026-09-04-002) — carries the _Default log retention confirmation 12.3 handed forward and clears retention.json's provisional status; names the measuring script and the record path; states that the AI export bucket is not provisioned
> - 2026-09-05 — deps-amend [13.2] (session-2026-09-04-002) — the runbook entry is drafted for 13.5 rather than written into a runbook that depends on this deliverable
> - 2026-09-05 — deps-amend [13.3] (session-2026-09-04-002) — deps: 12.1 → 12.1, 13.2; names the cadence doc path, keeps the spec's Metabase v0.59.6 baseline and its Dependabot-or-equivalent latitude, and covers the gtm-cloud-image cadence 13.2 decides
> - 2026-09-05 — deps-amend [13.4] (session-2026-09-04-002) — the scaling clause and its acceptance cover data-generator alongside sGTM, matching the Evidence paragraph that already assigns both here; the two disagreeing abort counts are named and re-measured rather than reconciled on paper
> - 2026-09-05 — deps-amend [13.5] (session-2026-09-04-002) — adds the four failure modes Phase 12 alerts point at with no entry (revision rollback, site and Metabase outage, BigQuery spend), restores the spec's kill-switch/revert/golden-gate parenthetical, names docs/runbook/, and carries 12.4's rehearsal hatch
> - 2026-09-08 — errata [13.3, 13.4] (session-2026-09-04-002) — two acceptance clauses were not met as written; `/guv:replan` refused both because the phase is sealed, so they are recorded in the Phase 14 notes of `docs/REQUIREMENTS.md` (hand-added: the engine refuses a completed phase)
> - 2026-09-08 — notes-edit [Phase 14] (session-2026-09-08-002) — the Validation line restores the spec's manual-task-card clause, which Phases 12 and 13 carry and 14 dropped, and records that the gate runs while a deliverable is still open; the pull-request hatch names 14.5 rather than 14.4, the phase having gained a fifth deliverable (hand-added: a phase-note edit is not a deliverable mutation, so the engine does not own it)
> - 2026-09-08 — deps-amend [14.1] (session-2026-09-08-002) — census measured and recorded before any write with a named replan escalation; the spec-to-API mapping named; publish split out behind --publish defaulting off; the GTM account membership and OAuth scopes placed here rather than in 14.3, which depends on this deliverable; the four assertions re-homed off deploy-phase6.js source rather than deleted; a bridgeToGtagConsent/consentSettings parity pin kept from the Phase 11 carry-forward; deploy-claudish.js added to the retirement list
> - 2026-09-08 — deps-amend [14.2] (session-2026-09-08-002) — the deliverable becomes verification and retirement rather than construction: metabase-lb.tf and terraform-metabase-lb.test.ts already own this topology, and a second writer would contradict the provider boundary at ARCHITECTURE:822 and re-arm the 9F /app/* drift family; the IAP residue gains four named homes, with the allowlist kept as a runbook procedure so the additive-only design survives auto-apply; the dry-run and delete-flag clauses dissolve with the reconciler rather than being dropped
> - 2026-09-08 — deps-amend [14.3] (session-2026-09-08-002) — deps: 14.1, 14.2 → 14.1; deps: 14.1, 14.2 -> 14.1; the Metabase dry-run path is dropped because 14.2 leaves that topology to infra-terraform.yml; the infra-production environment is created with a required reviewer before the variable is set, since that variable also arms an existing terraform apply -auto-approve and a missing environment is created unprotected; an ordering clause keeps an unattended apply from performing an in-flight import; an in-workflow preflight replaces a comment asserting an external fact; WIF resources gain an owner and bounded role scope; the injection sink is kept out and pinned; a rollback runbook entry is added
> - 2026-09-08 — deps-amend [14.4] (session-2026-09-08-002) — the seven data layer variables the two events need are added to scope, without which the GA4 tags fire with unresolved parameters and the BigQuery columns stay null; the acceptance clause that pinned the event roster is replaced, because DATA_LAYER_EVENT_NAMES already carries both events and schema.test.ts already asserts them, so the clause passed before the work began; the container-wiring pin that can fail replaces it; the sGTM route becomes a confirmation with an answer rather than an open task
> - 2026-09-08 — insert [14.5] (session-2026-09-08-002)
> - 2026-09-08 — deps-amend [14.1] (session-2026-09-08-002) — the census discharged its own precondition and fired the escalation it named: server-container.json is regenerated from live under the brownfield contract rather than applied as written, which would create nine triggers and three duplicate tags against a working pipeline; web-container.json stands authoritative, ahead of live only by the four Claudish pipelines deploy-claudish.js never applied; built-in variables, folders and custom templates are declared outside the reconciler ownership; the four claudish_* events gain a verified home here rather than a deliverable of their own, since the web apply creates them from the spec that already holds them; the deploy identity is named now that it exists
> - 2026-09-08 — deps-amend [14.4] (session-2026-09-08-002) — the sGTM trigger is named correctly: the census found no All GA4 Events trigger in the live server container, and the generic route is clientName - GA4, a type=always trigger filtered on Client Name contains GA4; the wording amended earlier this session took the wrong name from a spec the same census falsified, and the two source comments carrying it are named so the next reader is not sent looking; the acceptance now states the measured zero-row baseline the claim is against
> - 2026-09-08 — deps-amend [14.1] (session-2026-09-08-002) — the consent change is named in the plan of record rather than only in commit messages: the spec has declared analytics_storage required since Phase 1 while the container carried notNeeded throughout, so the apply gates 21 of 22 tags and stops a declining visitor generating cookieless pings; the claim that the web spec was ahead of live only by the Claudish pipelines is corrected, since the first dry run measured 24 adds and 18 changes; the overlay honesty fix is brought into scope because the gate makes a declining visitor legitimately empty; acceptance gains the published consent state and what a decliner must not be shown
> - 2026-09-08 — insert [14.6] (session-2026-09-08-002)

---

## Phase 12 — Runtime currency and observability

_Goal: Meet the Node 24 deadline and stand up the monitoring foundation: a notification channel, uptime checks, log-based metrics, alerting policies and a dashboard, all as committed configuration applied by script._

- ✅ **[12.1]** Node.js 24 runtime: pin `engines.node` to `24.x`, move both stages of the three Cloud Run Dockerfiles to `node:24-slim`, record the Node 24 pin and the Homebrew arm64 toolchain note in the CLAUDE.md project facts (retiring the "Runtime deadline" fact), update the Node references in ARCHITECTURE, and confirm the sync-dataform workflow pins no Node; all suites and the proxy golden gate pass under Node 24, the Vercel production build carries no deprecation warning, and the three services are redeployed on the new image with healthy endpoints `[deps: none]` ✅ 2026-09-04, session-2026-09-04-001, record `docs/verification/2026-09-04-node24-runtime.md` (21 of 22 machine checks; the golden gate's one miss is the known v11 prompt-variance case cl2en-13, first person, zero fall-throughs, parked under Decision #42; not runtime-related)
- ✅ **[12.2]** Notification channel and uptime checks: an email channel (Slack optional) and Cloud Monitoring uptime checks on sGTM `https://io.iampatterson.com/healthy`, event-stream `/health`, claudish-proxy `/health`, `https://www.iampatterson.com/` and the Metabase load balancer (the IAP redirect counts as healthy), every check alerting to the channel, all declared under `infrastructure/monitoring/` and applied by `apply.sh`, with one deliberate failure rehearsed and its notification recorded `[deps: none]` ✅ 2026-09-04, session-2026-09-04-001, records `docs/verification/2026-09-04-uptime-checks.md` and `2026-09-04-uptime-rehearsal.md` (five checks alerting to ops-email and ops-pubsub; the rehearsal pointed claudish-proxy-health at a missing path and read the OPEN notification off the Pub/Sub channel 6 min later, the CLOSED notification 6 min after the path was restored)
- ✅ **[12.3]** Log-based metrics and retention: metrics for the structured error events the proxy already emits (`request_error`, `loop_fell_through`, `loop_retry_failed`, `loop_empty_result`, `loop_failed_midstream`, `lane_failed`, `capacity_ladder_exhausted`) and for data-generator's `[ad-insert]` failure line, a recorded note that event-stream emits no error logs today, a provisional retention period for the `_Default` log bucket to be confirmed against the 13.1 data retention decision, a sink if anything must outlive it, and one "what failed in the last 24 hours" query each for sGTM, event-stream, data-generator and claudish-proxy for the runbook `[deps: none]` ✅ 2026-09-04, session-2026-09-04-001, record `docs/verification/2026-09-04-log-metrics.md` (four metrics live and matching the spec, `_Default` retention 30 days provisional pending 13.1, no sink needed, all four 24-hour queries returning)
- ✅ **[12.4]** Alerting policies routed to the channel: Cloud Run 5xx rate per service, container restarts and crash loops, Pub/Sub oldest unacked age and undelivered count on the push subscription, Cloud Scheduler job failures, Dataform run failures, BigQuery daily spend, the claudish-proxy `budget_threshold` and `capacity_no_budget` log events, and managed certificate expiry, each with a documented threshold, any log-based metric it needs, and either a recorded test firing or a written reason it cannot be fired safely `[deps: 12.2, 12.3]` ✅ 2026-09-04, session-2026-09-04-001, records `docs/verification/2026-09-04-alert-policies.md` and `2026-09-04-policy-rehearsal.md` (eleven policies applied and idempotent; capacity_no_budget fired on purpose through the kill switch, OPEN 2 min later, switch off after 109 s; the scheduler policy reads the execution log because `cloudscheduler.googleapis.com/job/attempt_count` returns 404 in this project; the Dataform policy will be fired by the real nightly assertion failure)
- ✅ **[12.5]** Cloud Monitoring dashboard generated from a committed spec under `infrastructure/monitoring/` and applied by script: request rate, latency and error rate per Cloud Run service, Pub/Sub throughput and backlog, BigQuery slot usage and daily cost, scheduler outcomes, certificate status and the proxy's per-instance budget threshold crossings (`budget_threshold` at 50/80/100 %) `[deps: 12.4]` ✅ 2026-09-04, session-2026-09-04-001, records `docs/verification/2026-09-04-dashboard.md` and `2026-09-04-dashboard-rehearsal.md` (eleven tiles generated from a 141-line spec against the 300-line acceptance; the live dashboard was deleted and rebuilt from the spec with an identical layout, so the criterion is run rather than asserted)

---

## Phase 13 — Cost, lifecycle and the runbook

_Goal: Put retention and cost controls in place, decide the sGTM image lifecycle, adopt the Cloud Run services into declared identity and configuration, set a dependency cadence, and write the runbook that ties every alert to a procedure._

- ✅ **[13.1]** Retention and cost controls, measured by a script under `infrastructure/retention/` and recorded in `docs/verification/`: measure partition expiration on every raw and staging table, table sizes, GCS lifecycle rules and Dataform schedule state; then apply partition expiration on raw event tables, lifecycle rules on the AI export bucket — `gs://iampatterson-ai-exports`, declared by `infrastructure/bigquery/ai_access_layer/setup.sh` and not provisioned as of 2026-09-04, so the deliverable either creates it with rules or records that the access layer never shipped — a notification channel on the existing `metabase-project-budget`, and a Vertex spend budget for the Claudish proxy if the in-app cap is not enough on its own; and confirm or change the provisional `_Default` log bucket retention 12.3 handed forward, clearing the `status` field in `infrastructure/monitoring/spec/retention.json` `[deps: 12.2]` ✅ 2026-09-05, session-2026-09-04-002, record `docs/verification/2026-09-05-retention-and-cost-controls.md` (measured by `infrastructure/retention/apply.sh`, idempotent on the second run; three changes applied — lifecycle on the two build-artifact buckets and a notification channel on a budget that had none; the 60-day raw expiry confirmed and measured pruning; `_Default` confirmed at 30 days and no longer provisional; the AI export bucket recorded as never provisioned; no Vertex budget, with the reason)
- ✅ **[13.2]** sGTM container lifecycle: a recorded decision between the floating `:stable` tag and a pinned digest, and an operator script with a dry run that updates `gtm-cloud-image` on `sgtm` and `sgtm-preview`, rehearsed once end to end on `sgtm-preview`, with its runbook entry drafted for 13.5 rather than written into a runbook that depends on this deliverable `[deps: none]` ✅ 2026-09-05, session-2026-09-04-002, record `docs/verification/2026-09-05-sgtm-image-lifecycle.md` (decision: pin the digest — measured that `:stable` is not floating, both services having served the 2026-04-03 digest for five months while the tag moved on; `infrastructure/sgtm/update-image.sh` with dry run and health checks either side; rehearsed end to end on sgtm-preview, now current and healthy; production sgtm deliberately left behind for a person to run)
- ✅ **[13.3]** Dependency update process: a cadence documented at `docs/runbook/dependency-cadence.md` for Node, Next.js and React, the Cloud Run base images, the `gtm-cloud-image` releases whose update procedure 13.2 decides, Metabase (baseline v0.59.6, updated through the release-notes gate `infrastructure/metabase/upgrade.sh` already enforces), npm dependencies and security advisories, with a monthly "read the platform notices" step, and Dependabot or an equivalent configured for the root and the three services `[deps: 12.1, 13.2]` ✅ 2026-09-05, session-2026-09-04-002, record `docs/verification/2026-09-05-dependency-cadence.md` (nine Dependabot entries covering five npm surfaces, three base images and the workflows, monthly and grouped; cadence doc names the reviewer, the schedule, the monthly platform-notices step and the two images no bot watches; baseline measured at 25 outdated and 32 advisories, two direct. **One acceptance clause is not met**: "at least one update pull request produced" needs the config on `main` before Dependabot will run, so it is checkable only after merge — named here, not counted as done)
- ✅ **[13.4]** Cloud Run adoption: the claudish-proxy service, service account, IAM and secret imported into the declarative layer per `infrastructure/cloud-run/claudish-proxy/IMPORT_PLAN.md` with `KILL_SWITCH` and the source-deploy fields in `ignore_changes`, `event-stream`, `data-generator`, `sgtm` and `sgtm-preview` moved off the default compute service account onto dedicated runtime accounts with least-privilege roles, and the scaling settings measured before they are declared: the 12.3 `cloud_run_no_instance` metric shows sGTM aborting requests at `maxScale=3` and data-generator aborting cold-start runs behind its three schedulers, so the configuration adopted is the corrected one on both — sGTM's ceiling, and data-generator's minimum instances or scheduler spread — and not today's; the abort counts recorded on 2026-09-04 disagree between this line's earlier 16 for sGTM and the 13 for sGTM plus 1 for data-generator in `docs/verification/2026-09-04-first-real-alert.md`, which are different query windows, so the deliverable re-measures both before changing anything `[deps: 12.3, 13.7]` ✅ 2026-09-05, session-2026-09-04-002, record `docs/verification/2026-09-05-cloud-run-adoption-measured.md` (zero of six Cloud Run services remain on the default compute account that holds roles/editor; four dedicated accounts with least privilege, event-stream deliberately holding none; each move proved by sending events through and counting rows, +712 then +117, not by a health endpoint; sGTM to maxScale=10 against 96 aborts in 30 days; the proxy adoption imported — 6 imported, 0 added, 0 changed, 0 destroyed — and the plan back to No changes. Open, and named rather than counted: roles/editor is still granted to the default compute account though nothing uses it, IMPORT_PLAN's secret and IAM halves target modules this root lacks, and the after-abort count is 0 over 12 hours where the meaningful comparison is another 30-day window)
- ✅ **[13.5]** Operational runbook at `docs/runbook/`: one entry per failure mode (sGTM not responding, event pipeline backlog, Dataform assertion failure, data generator stuck, a Cloud Run revision that will not start, rolled back with `scripts/deploy-cloud-run.sh promote <revision>`, the site or the Metabase load balancer failing its uptime check, BigQuery daily spend above the alert threshold, certificate renewal failure, Claudish proxy over budget or misbehaving — kill switch, revert to a previous revision, the golden gate — a Vercel build failing on runtime deprecation, expired gcloud credentials during an incident, preview protection for automated checks), each linked to the alert that leads to it and either rehearsed once or carrying a written reason it cannot be rehearsed safely, written for a reader who has never seen the stack; the Vercel automation-bypass secret is its manual task `[deps: 12.4, 13.1, 13.2, 13.4]` ✅ 2026-09-05, session-2026-09-04-002, record `docs/verification/2026-09-05-operational-runbook.md` (twelve entries plus an index under docs/runbook/; all sixteen Phase 12 alerts resolve to an entry, asserted by test; six dated rehearsals, five written reasons and one sibling-inference labelled as such, with five rehearsals performed here — a deliberately broken revision on sgtm-preview that never took traffic, the site-www check failing for six minutes while the site served 200, the generator under a changed identity, the preview auth wall, and a credential expiry that happened for real; two of the entries were corrected by their own rehearsals)
- ✅ **[13.6]** Terraform layer recovered onto the phase branch: the twenty-three files of `infrastructure/terraform/` and `tests/unit/infrastructure/` from the unmerged `phase/11-operational-readiness` branch (PR #56, open since 2026-06-04) brought across without their obsolete tracker edits, the `@cdktf/hcl2json` devDependency, and `.github/workflows/infra-terraform.yml` whose plan and apply jobs stay inert until 14.3 sets `GCP_WIF_PROVIDER`; nothing is applied and no state is written; and the provider boundary the branch documented — Terraform owns the GCP backend, GTM container config stays reconciler-managed, Vercel stays on its own pipeline — recorded in ARCHITECTURE in place of the "Terraform is not introduced for them yet" line `[deps: none]` ✅ 2026-09-05, session-2026-09-04-002, record `docs/verification/2026-09-05-terraform-layer-recovered.md` (24 files, +1,676; suite 1,827 → 1,875 across 173 suites; fmt and validate clean; nothing applied; the baseline plan reads 0 to add, 1 to change, 6 to destroy and is 13.7's starting point)
- ✅ **[13.7]** Terraform state reconciled to a clean plan: the remote state holds forty-six resources against a configuration that declares forty, because a session on 2026-06-08 imported the four BigQuery datasets and the Pub/Sub topic and push subscription without committing their configuration, so `terraform plan` reads "0 to add, 1 to change, 6 to destroy" and an apply would take the live event pipeline with it; author the missing BigQuery and Pub/Sub configuration to match live or remove those entries from state, recording which and why, and settle the one real drift — state and live pin `data-generator` traffic to an exact revision while the configuration wants `TRAFFIC_TARGET_ALLOCATION_TYPE_LATEST`, against `scripts/deploy-cloud-run.sh promote` — with `ignore_changes` on the traffic block, the treatment `IMPORT_PLAN.md` already specifies for `KILL_SWITCH` `[deps: 13.6]` ✅ 2026-09-05, session-2026-09-04-002, record `docs/verification/2026-09-05-terraform-state-reconciled.md` (plan went from 0 to add, 1 to change, 6 to destroy to "No changes"; the six recovered as configuration rather than dropped from state; no apply run; 17 pins, 15 of which fail against the pre-13.7 configuration)

---

## Phase 14 — Declarative infrastructure

_Goal: Replace the one-shot deploy scripts for GTM and the Metabase load balancer with spec-driven reconcilers, run them from a workflow with a dry-run diff on pull requests and a gated live apply, and use the GTM reconciler to wire the two events still waiting on it._

- ✅ **[14.1]** GTM reconciler: the census recorded in `docs/verification/2026-09-08-gtm-container-census.md` found both committed specs materially incomplete, so `infrastructure/gtm/server-container.json` is first regenerated from live — the brownfield contract ARCHITECTURE states for Terraform, converge onto live and never destroy-and-recreate, since applying that spec as written would create nine triggers and three duplicate tags against a working pipeline — while `web-container.json` stands as authoritative — ahead of live by the four Claudish pipelines `deploy-claudish.js` never applied, and behind it on the shared `gtes` event-settings variable and the `measurementId` every live tag already carries, both adopted into the spec rather than applied against live; **and divergent from it on consent, which is this deliverable's largest visitor-facing effect**: the spec has declared `analytics_storage: required` since Phase 1 and the container has carried `notNeeded` on every tag throughout, so the apply gates twenty-one of twenty-two tags and a visitor who declines analytics stops generating GA4 cookieless pings — the record conflict this resolves, and what it costs, are recorded in ARCHITECTURE's "Where the gate actually was"; because that gate makes a declining visitor's overlay legitimately empty, `buildRouting` and the timeline's empty state are corrected in the same deliverable so the overlay reports what happened rather than asserting delivery; built-in variables, folders and custom templates are declared outside the reconciler's ownership and exempt from both the drift report and `--allow-deletes`; then `infrastructure/gtm/reconcile.js` diffs the committed specs against the live Default Workspace on each container through a documented mapping between the specs' human-readable shapes (`firingTrigger` by name, `parameters` as a map) and the Tag Manager API v2 resources (`firingTriggerId`, `parameter[]`), applies adds and updates, takes deletes only behind `--allow-deletes`, defaults to dry run, and creates and publishes a container version only behind `--publish`, defaulting off, so dry run, apply and publish are three distinct steps; the web apply lands the four `claudish_*` pipelines as a consequence, and their arrival in `events_raw` is proven rather than assumed; the deploy identity is `gtm-reconciler@iampatterson.iam.gserviceaccount.com`, a member of GTM account `6346433751` by manual card and reached by impersonation locally and Workload Identity Federation in CI, never by a key; the four assertions in `tests/integration/web-container-spec.test.ts` that parse `deploy-phase6.js`'s source are re-homed onto the reconciler's spec-to-API mapping rather than deleted, and a `bridgeToGtagConsent` ↔ container `consentSettings` parity pin is added while that code is open; `deploy-phase6.js` and `deploy-claudish.js` are both retired once parity is reached `[deps: none]` ✅ 2026-09-08, session-2026-09-08-002, records `docs/verification/2026-09-08-gtm-container-census.md` and `2026-09-08-gtm-web-container-published.md` (census measured both live containers before any write and fired its own escalation; reconciler built red-first with dry run the default; web container published as version 9 and both containers report no drift; `deploy-phase6.js` and `deploy-claudish.js` retired, 981 lines, with seven assertions re-homed onto the spec; consent parity pin verified by inducing a failure. **Acceptance closed 2026-09-09**: all four `claudish_*` families queryable in `events_raw` from production traffic with `consent_analytics: true` — translate ×2, detected, share ×2, rate — against the zero rows the fourteen days before the publish returned. The consent gate is proven in the same run: a session with analytics denied fired 36 events and landed none)
- ✅ **[14.2]** Metabase load-balancer retirement onto the Terraform layer: confirm `infrastructure/terraform/metabase-lb.tf` describes the live forwarding rule, the URL map with the `/app/*`, `/api/*` and `/embed/*` split, both backend services and the managed certificate against a captured `terraform plan` no-op, rather than reimplementing that topology in a second writer against the provider boundary ARCHITECTURE records; give the residue neither Terraform nor that file covers a home — the IAP OAuth brand and client with their two Secret Manager versions stay a documented console procedure, the IAP service agent's `run.invoker` binding is declared, and the `roles/iap.httpsResourceAccessor` allowlist goes to a `docs/runbook/` procedure rather than declared configuration, because declaring it would make removing a name from a file revoke that person's access on the next apply and reverse the additive-only design `setup-iap.sh` documents; then retire `setup-domain.sh` and `setup-iap.sh`, recording in ARCHITECTURE that the Metabase load balancer is Terraform-owned and that the reconciler pattern stays reserved for GTM container configuration `[deps: none]` ✅ 2026-09-08, session-2026-09-08-002, record `docs/verification/2026-09-08-metabase-lb-terraform-owned.md` (terraform plan a no-op across all eight LB resources before any change, so the reconciler this deliverable originally called for would have been a second writer on one URL map; the IAP service agent binding imported — 1 imported, 0 added, 0 changed, 0 destroyed — with the additive `_iam_member` resource and a test forbidding the authoritative types; setup-domain.sh and setup-iap.sh deleted, 755 lines, with seven assertions re-homed onto the Terraform pin that already covered them more strictly; the OAuth brand, client, secret versions and the additive-only allowlist placed in `docs/runbook/metabase-access.md` with its read path rehearsed. **Found and recorded, not changed**: `allUsers` holds run.invoker on the metabase service — not an active exposure behind internal-only ingress, but independent of the IAP gate)
- ✅ **[14.3]** Reconcile workflow: `.github/workflows/infra-reconcile.yml` runs the GTM reconciler in dry-run mode on pull requests touching `infrastructure/gtm/**` and posts the diff as a comment, passing reconciler output through `env:` rather than `${{ }}` interpolation — the expression-injection sink `004f7a3` removed from the sibling workflow, pinned by a check over `.github/workflows/*.yml` — and runs the live apply on merge to `main` behind an `infra-production` environment with manual approval, authenticated through Workload Identity Federation with no service-account key in the repository; that environment is created with a required reviewer **before** `vars.GCP_WIF_PROVIDER` is set, because the same variable arms `.github/workflows/infra-terraform.yml`'s `terraform apply -auto-approve` on every push to `main` and a referenced-but-missing environment is created unprotected, and it is set only after every import in flight in this phase has landed and `terraform plan` is a captured no-op, so no unattended apply performs the import of a live service; each apply job's first step asserts its environment carries protection rules and stops loudly if it does not; the WIF pool, the provider with its repository-scoped attribute condition and the deployer service account are given an owner — declared in Terraform, or created by an operator script and recorded, stated on entry — with the deployer's roles recorded and justified rather than minimised; and `docs/runbook/` gains an entry for a reconciler or Terraform apply that damaged production, keeping `tests/unit/infra/runbook.test.ts` green. The Metabase path is not covered here: 14.2 leaves that topology to `infra-terraform.yml`'s existing `infrastructure/terraform/**` trigger `[deps: 14.1]` ✅ 2026-09-09, session-2026-09-08-002. `.github/workflows/infra-reconcile.yml` written with the dry-run comment reading its diff from `env:` rather than `${{ }}`, a preflight that refuses to apply when `infra-production` carries no protection rules, an explicit `--container=web`, and publish as a separate step. A generic check over `.github/workflows/*.yml` graduates the injection-sink lesson from a comment to a test and was verified by inducing the sink. `docs/runbook/infra-apply-damaged-production.md` covers the rollback. `docs/manual/task-2026-09-09-001.md` carries the five steps in the order that matters — the environment must exist WITH a reviewer before `vars.GCP_WIF_PROVIDER` is set, because that one variable also arms `infra-terraform.yml`'s auto-apply and GitHub creates a missing environment unprotected. **Gated** 2026-09-09: the platform review and `guv:reviewer` both ran over `c14e24f..HEAD`; their findings are fixed in `cbdb824` and the follow-up commit. **Acceptance closed 2026-09-09** on the merge of PR #75 (`80a2ff1`). Both `infra-reconcile` and `infra-terraform` triggered and both `apply (main)` jobs held at `status=waiting` on `env=infra-production` — nothing applied unattended. Ian approved `infra-reconcile`; its first step printed `infra-production carries 1 protection rule(s).`, the apply reported `no drift`, and the publish step created **container version 11, `ci: 80a2ff1 on 2026-09-09`**, now live (24 tags, 23 triggers, 63 variables). The whole path — WIF, the repository-scoped attribute condition, the deployer's GTM membership, the preflight, the apply and the publish — is exercised end to end by CI rather than a workstation. The merge also exposed an incomplete grant: the deployer held `secretmanager.secretAccessor` (payload) but not `secretmanager.viewer` (metadata), which `google_secret_manager_secret_version` reads first; added, after which the plan read "No changes." `infra-terraform` was then approved too and reported **"Apply complete! Resources: 0 added, 0 changed, 0 destroyed."** — the first CI apply of the Terraform root, and a true no-op.

**One clause was closed early and reopened.** This deliverable asks for the preflight on *each* apply job. `infra-terraform.yml` had none — it still carried the comment `# repo setting requires manual approval`, the exact assertion measured false on 2026-09-08 and the reason the preflight exists — and it ran once (run `34358564379`) without a guard. The existing check read only `infra-reconcile.yml`, so it could not see the asymmetry. Closed by generalising the check over every workflow deploying to `infra-production`, which immediately found another copy of the same false claim in that file's header comment. A later platform review found a **third**, in `infra-reconcile.yml`'s header, whose wording the check's regex did not match; swept in `cbdb824`. The guard is now on both jobs and the check is mutation-verified: deleting the step fails it, and so does moving the step after the apply it guards. **The `infra-terraform` guard then executed** on the merge of PR #76 (`55cfd12`, run `34362729377`): step 3 of 7, printing `infra-production carries 1 protection rule(s).` before auth, `init` or `apply` — after which the apply reported "No changes." Both apply jobs now carry the guard and both have run it in CI. The alignment review then found the guard counted protection rules of **any** type, so an environment carrying only a wait timer would report a reassuring count and the apply would proceed unreviewed — this phase's thesis turned on its own guard. It now selects `required_reviewers`, runs as the genuinely first step, and is byte-identical across both workflows, each property mutation-verified.

  **One clause closes with a named gap.** [14.3] asks that the WIF pool, "the provider **with its repository-scoped attribute condition**" and the deployer account be given an owner — declared in Terraform, or created by an operator script and recorded. What shipped is the third: `gcloud` commands in `docs/manual/task-2026-09-09-001.md` §2, recorded in `docs/verification/2026-09-09-wif-and-ci-armed.md`. A grep for `assertion.repository` or `workloadIdentityPools` across `*.tf` and `*.sh` returns nothing, so the one string stopping any repository on earth from minting a token for an identity this record itself calls close to `roles/editor` was declared nowhere and compared to live by nothing — the phase's thesis unapplied to its most consequential resource. Partly closed: UAT scenario 11 compares the live attribute condition against the committed expectation, so drift is now detected. **Carried as debt:** declaring the pool, provider and deployer in Terraform, so they have an owner rather than a transcript.
- ✅ **[14.4]** `web_vital` and `page_engagement` wiring: data layer variables for the seven parameters the two events carry — `metric_name`, `metric_value`, `metric_rating`, `metric_id`, `navigation_type`, `engagement_seconds`, `max_scroll_pct` — plus custom-event triggers and GA4 event tags in the web container spec, applied and published through the reconciler, so real-user Web Vitals and engagement rows land in `iampatterson_raw.events_raw` and the `web_vital` coverage chip on the Overview tab stops claiming a pipeline the container does not carry; the sGTM container needs no route change, and the deliverable confirms that rather than building it, its three destination tags all firing on `clientName - GA4`, a `type=always` trigger filtered on `{{Client Name}} contains GA4` — not the `All GA4 Events` trigger the committed spec, `deploy-claudish.js:8` and `pubsub-tag-template.js:16` name, which the 2026-09-08 census found does not exist `[deps: 14.1]` ✅ 2026-09-09, session-2026-09-08-002, record `docs/verification/2026-09-09-web-vitals-wiring.md` (published as version 10; 11 additions, nothing changed or removed; both tags gate on analytics_storage like every other GA4 event tag. Acceptance met with payloads intact: all five Core Web Vitals in `events_raw` with real values and ratings — LCP 128ms good, TTFB 30ms, FCP 128ms, CLS 0.00027, INP 40ms — and both engagement thresholds with the unbucketed max-scroll. The sGTM route needed no change, confirmed rather than built. **Blocked on a defect found while verifying**: schema.json had never been applied to the live table, so the first `page_engagement` landed with every parameter null; fixed by making `infrastructure/bigquery/setup.sh` reconcile, 50 to 77 columns)
- ✅ **[14.5]** Claudish adoption completed and the identity layer declared: import `google_cloud_run_v2_service.claudish_proxy` into `cloud-run.tf` with `client`, `client_version`, `build_config`, the rolling image and `template[0].containers[0].env` in `ignore_changes` — the `KILL_SWITCH` treatment `IMPORT_PLAN.md` specifies and calls the class of thing that plan exists to catch — adding `claudish_proxy` to the `SERVICES` and `SOURCE_DEPLOY` arrays in `tests/unit/infrastructure/terraform-cloud-run.test.ts` with a pin on the env exclusion, and a structural pin asserting every service directory under `infrastructure/cloud-run/` has a matching `google_cloud_run_v2_service` declaration, which runs offline and would have failed on 2026-09-03; declare the 13.4 runtime-account role grants and the proxy's `aiplatform.user` binding so the least-privilege posture is described rather than only applied, this root holding no IAM resources of any kind today; take the recorded decision on `IMPORT_PLAN.md`'s unused break-glass secret; correct ARCHITECTURE's 13.4 "Still open" paragraph, which names `roles/editor` and the Secret Manager and IAM halves but omits the service itself; and record what still depends on `roles/editor` on the default compute account as a finding and a recommendation, explicitly without revoking it `[deps: none]` ✅ 2026-09-09, session-2026-09-08-002, record `docs/verification/2026-09-09-claudish-proxy-adoption.md` — applied by Ian: **5 imported, 0 added, 0 changed, 0 destroyed**, and the following plan reports no differences. The proxy is unaffected (`/health` 200) and `KILL_SWITCH` still reads `off` in the live service, present in state and owned by nobody, which is what the env exclusion is for. A structural pin over `infrastructure/cloud-run/*/` would have caught the original miss on 2026-09-03. `roles/editor` measured: nothing runs as the default compute account, but Cloud Build does, so revoking it would break source deploys for three services — recommendation recorded, not acted on. Dataset-level grants and the break-glass secret decision named as outstanding. **Un-gated**: no review instruments run.
- ✅ **[14.6]** Session key resolved so the warehouse holds real visitors: `session_id` is reserved in gtag.js, which consumes it before the hit is built, so every real-browser row reaches `iampatterson_raw.events_raw` with it NULL while the generator — hand-building its query string against the same `/g/collect` endpoint — sets it explicitly; all twelve Dataform models keyed on that column and `stg_sessions` filtered `WHERE session_id IS NOT NULL`, so the session layer held 193,323 sessions of which zero were real and every mart and Metabase dashboard above it described the generator alone; resolve `COALESCE(iap_session_id, session_id)` at the staging boundary in the passthrough column, the dedup partition and the `event_id` hash, matching the precedence `pubsub-tag-template.js` already uses so the warehouse and the real-time overlay key a visitor identically; fix `is_synthetic`, which derived from `iap_source` that the real site also sends and therefore labelled every recovered visitor synthetic, by having the generator send its own `iap_synthetic` marker with its user agent as the fallback for rows already in the sixty-day raw window; and replace the `assert_stg_sessions` null check — which tested a predicate `stg_sessions` itself filters out and so could never fail — with one that asserts non-synthetic sessions exist, plus a generic check that no assertion tests a null its source model removes `[deps: none]` ✅ 2026-09-08, session-2026-09-08-002, commits `a6ff68b` and `e02674d` (resolved key measured against live: 12 distinct real sessions over two days where there were 0, generator sessions unchanged; both review instruments run, and both independently found the `is_synthetic` mislabel that would have shipped a permanently red assertion. **Open**: the marts carry the fix only after this merges, the sync action mirrors to the `dataform` branch and the scheduled run rebuilds; the RAG snapshot tables must be re-run after that; history before ~2026-07-10 is past the 60-day raw partition expiry and unrecoverable)

---
