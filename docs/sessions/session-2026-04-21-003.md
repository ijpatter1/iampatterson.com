# Session Handoff — 2026-04-21-003

**Phase:** 9F — Ecommerce Demo Native-Reveal Rebuild
**Date:** 2026-04-21
**Branch:** `phase/9f-ecommerce-native-reveal` (15 commits ahead of origin)

## Completed This Session

This session closed **all 15 UAT r1 items** (`docs/uat/phase-9f-uat-feedback.md`) and landed a two-pass dual-evaluator fix cycle on top. Phase 9F is now fully dev-complete + UAT-r1-closed.

### UAT r1 item responses (13 commits)

| UAT item | Block | Commit | Summary |
|---|---|---|---|
| 5 — footer duplication | E | `a893a35` | New `FooterSlot` client component suppresses site `<Footer />` on `/demo/ecommerce/*`. Site footer still renders everywhere else. 5 new tests. |
| — voice/style guide | — | `266a21a` | Added `docs/voice-and-style-guide.md` (user-supplied mid-session). Landed BEFORE the copy rework so every subsequent commit could reference it. |
| 1 — demos-section copy | A | `ef3d819` | Rewrote homepage demos-section. "Watch a purchase become a KPI" → "Shop a little. Watch the events land in BigQuery." Body drops "Instead of toggling an overlay" + "Tier 3 payoff"; names GTM, server-side GTM, BigQuery, Metabase. 5 regression tests. |
| 4, 7 — pointless fluff | D | `9d8f2a6` | Removed listing filter-chip row + product-detail palette swatch row. 2 regression pins. |
| 3, 10 — honest labels | C | `08836e5` | `resolveUtmMeta` returns `{ value, isLive }`; listing panel flags seed as "example · no utm in your url" when no URL param. Checkout "Courtney/Patterson" → "Jane/Rivera". 5 tests. |
| 2 — sub-nav margins | B | `7b249dc` | `EcomSubNav` wraps content in `mx-auto max-w-content px-6 md:px-10` to match prototype. 1 test. |
| 9 — abrupt toasts | G | `a54fae8` | `toast-enter` keyframe (180ms slide + fade, `motion-safe:`); desktop portal repositioned to `md:right-6 md:top-6 md:items-end`; default duration 2400 → 3200ms. 2 tests + 1 duration test updated. |
| — overbite correction | — | `807da6d` | User-flagged: chiweenies have **overbites**, not underbites. Fixed in listing hero + sub-nav + sub-nav docstring + REQUIREMENTS.md (with annotation that the prototype README got this wrong). Test updated. |
| 6 — live staging-layer | F1/3 | `6242654` | New shared `useSessionContext` hook (session_id from cookie + last-event facts from `useLiveEvents`); `stagingRowsForProduct` substitutes real session_id + event_timestamp; new `stitchOpsForSession` rewrites session_stitch detail with real session + event count. ProductDetail wires through. 13 new tests. |
| 8 — live data-quality | F2/3 | `77a0158` | `assertionsForCart` extended with `LiveCartContext`: volume_anomaly uses real add_to_cart-in-30s when stream data is present, session_join_integrity names real session_id, freshness shows real seconds-since-last-event. Header "source" line names latest event. 11 new tests. |
| 11, 13 — live warehouse-write | F3/3 | `38ed4cd` | `bqRowForCart` extended to substitute real session_id + event_timestamp + received_timestamp + consent flags. 8 new tests. |
| 14 — intermission pacing | H | `2f41b7b` | Duration 1900 → 4500ms (later corrected to 3200ms in Pass-1 fix). New `diagnosticLinesForConsent` branches Meta CAPI between SKIP/OK based on real consent; consent-check line reflects real analytics/marketing flags. 7 new tests + 3 existing updated. |
| 15 — dashboard not loading | I | `3596692` | Manual task card at `docs/manual/task-2026-04-21-006.md` — enumerates three failure paths (env missing / JWT bad / Metabase cold-start) with diagnosis steps + D9 ship-gate cold-start probe procedure. Not a code fix; deploy/env issue. |

### Pass-1 dual-evaluator fix pack (`fbc60d0`)

Both evaluators ran after the 13 commits. Tech returned 4.00/5 PASS WITH ISSUES (3 Important findings); Product returned 3.90/5 PASS (3 Majors + 5 Minors). All 5 Majors + 4 of the 5 Minors addressed in one fix pack:

1. **Tech M1** — data-quality volume_anomaly fallback no longer lies about stream counts; honest "cart holds N items" fallback phrasing.
2. **Tech M2** — `useSessionContext` dropped the useMemo, added 5s interval tick; `seconds_since_last_event` no longer freezes while idle. Cleanup on unmount. 1 regression test.
3. **Tech M3** — `stitchOpsForSession` requires `sid && count > 0` to avoid emitting "(0 events)" lie during the cookie-loaded-but-no-events-yet window.
4. **Prod M1** — `bqRowForCart` extended with pageLocation / pageReferrer / utmCampaign / utmIsLive / utmSource / channelClassified substitutions; CheckoutForm threads via `useSearchParams` + `window.location.href` + `document.referrer` + `classifyUtm`. When `utmIsLive: false`, readout header flags "utm_* values are example seeds."
5. **Prod M2** — geo_enrich stitch-op detail abstracted ("ip → US / CA / Los Angeles" → "ip → country · region · city (geoip)"), stopping the specific-location lie for out-of-LA visitors.
6. **Prod M3** — intermission 4500 → 3200ms; "press any key to skip" promoted from 10px muted to 11px amber so the affordance is discoverable.
7. **Prod m4** — EcomFooter "tier 1–3 measurement stack" jargon → "production measurement stack … click to dashboard."
8. **Prod m5** — cart empty-state "flow through the stack" → "the add_to_cart event lands in BigQuery before you get here."
9. **Prod m6** — checkout form got a muted "demo prefill — nothing ships from here, nothing is charged" note above the contact fieldset.
10. **Prod m8** — diagnostic intermission "attribution engine · last-click + shapley triggered" (decorative) → "dataform · marts rebuilding against the new row" (actual pipeline step, consistent with the cart DATAFORM sidebar tag).
11. Checkout page wraps `CheckoutForm` in `<Suspense fallback={null}>` — now required because `useSearchParams` was added to `CheckoutForm`.

### Pass-2 dual-evaluator fix pack (`4b73e64`)

Both evaluators ran again on the Pass-1 fix pack. Tech 4.75/5 PASS (0 Important, 3 Minors); Product 4.03/5 PASS (0 Majors, 4 Minors — with explicit "Pass-3 not warranted" verdict). 4 actionable Minors addressed:

1. **Tech m1** — volume_anomaly FAIL/OK copy consistency ("add_to_cart events" vs "add_to_cart").
2. **Tech m2** — explicit regression test for `addToCartInLast30s: 0` boundary (pins that `typeof === 'number'` keeps 0 in the live branch; a future `if (liveCount)` bug would fail the suite).
3. **Prod m1 follow-up** — `deviceCategory` added to `LiveCheckoutContext`; CheckoutForm derives mobile/tablet/desktop from UA. Readout example-seed note extended to name `client_id` + `geo_country` alongside `utm_*` when utm is example; when utm is live, note surfaces "client_id / geo_country are example seeds; the remaining visitor-scoped columns reflect your real session."
4. **Prod m4** — visible sidebar tags renamed to remove internal tier jargon:
   - `UNDER · TIER 2` → `UNDER · STAGING LAYER` (product detail)
   - `UNDER · TIER 2 · DATAFORM` → `UNDER · DATAFORM ASSERTIONS` (cart)
   - `UNDER · TIER 2 · BQ` → `UNDER · BIGQUERY WRITE` (checkout)
   - `TIER 3 · DASHBOARDS` → `DASHBOARDS · LIVE` (confirmation embed)
   - `WHAT JUST HAPPENED · TIER 2 + 3` → `WHAT JUST HAPPENED` (confirmation journey)
   Dashboard-payoff test updated to pin the new tag + assert "TIER 3" is gone.

## In Progress

Nothing. Every deliverable committed; working tree clean.

## Blocked

Nothing on the code side. The joint 9E+9F release cut remains blocked on the Metabase dashboard-load diagnosis (manual task `docs/manual/task-2026-04-21-006.md`) — must resolve before shipping so the Tier 3 payoff actually lands.

## Issues & Technical Debt

Three Minors intentionally deferred from Pass-2 evaluation (both reviewers explicit that Pass-3 isn't warranted):

1. **Tech Minor — interval cleanup regression test** (self-identified, minor). Current `useSessionContext` 5s `setInterval` / `clearInterval` pair works, but no test pins the cleanup. A regression (e.g. accidentally dropping the effect's return function) wouldn't be caught by the current suite. One-line test: unmount the hook, advance fake timers past the next tick, assert no state update fired.
2. **Product Minor — intermission multi-pass skip** (product-reviewer, minor). 3.2s intermission fires on every checkout submit with no "seen it" short-circuit. A visitor exploring the demo multiple times sits through the full sequence each time. `sessionStorage` flag could auto-skip or abbreviate subsequent passes. Feature add; rolls into Phase 10 polish.
3. **Product Minor — Suspense skeleton on checkout** (product-reviewer, minor). `checkout/page.tsx` wraps CheckoutForm in `<Suspense fallback={null}>` to preserve static generation after adding `useSearchParams`. On slow connections, the viewport briefly renders empty white space between the site nav and demo sub-nav. A layout-preserving skeleton (`h-96` placeholders for form + sidebar) would preserve perceived quality. Polish pass.

Two carry-overs from prior sessions still outstanding:

4. **Playwright E2E execution** — scaffold at `tests/e2e/ecommerce-sessionpulse.spec.ts` ready; requires `npm run dev` + `npx playwright install` + `E2E_ENABLED=1 npx playwright test` in a browser-capable environment. Not blocking dev-complete.
5. **Metabase cold-start probe** — binary ship-gate for release cut. Full diagnosis runbook at `docs/manual/task-2026-04-21-006.md`.

## Evaluator Results

**Pass 1:**
- Technical: 4.00/5 PASS WITH ISSUES (3 Important — all fixed in `fbc60d0`)
- Product: 3.90/5 PASS (3 Majors + 5 Minors — all Majors + 4/5 Minors fixed in `fbc60d0`)

**Pass 2 (after `fbc60d0`):**
- Technical: 4.75/5 PASS (0 Critical · 0 Important · 3 Minors)
- Product: 4.03/5 PASS (0 Critical · 0 Majors · 4 Minors — reviewer explicit "Pass-3 not warranted")
  - Vision alignment 4.5/5
  - User experience 3.75/5
  - Content quality 4.25/5
  - Feature depth 3.5/5

**Post Pass-2 (after `4b73e64`):** 4 Minors fixed (honesty + consistency items); 3 Minors deferred to handoff (feature/polish adds — see Issues & Technical Debt).

## Product Review Results

Covered in the Evaluator Results block above — product-reviewer ran both passes alongside the tech evaluator. Final verdict: 4.03/5 PASS with no Pass-3 recommended.

## Test State

- **Total:** 1068 (up from 1000 at session start — **+68 net**)
- **Passing:** 1068
- **Failing:** 0
- **Skipped:** 0
- **New test files:** 3 (`footer-slot.test.tsx`, `useSessionContext.test.tsx`, + deletions offset by coverage adds in existing files)
- **Lint:** Clean

## Build State

- `npm run build` clean throughout the session (evaluator Pass-1 noted a sandbox-specific `PageNotFoundError` that did not reproduce locally; after the Suspense boundary was added in `fbc60d0` the build emits `○ (Static)` for `/demo/ecommerce/checkout` as expected)
- TypeScript: strict mode, no `any` types introduced
- Route distribution unchanged from session-002: `/demo/ecommerce` (Static), `/demo/ecommerce/[productId]` (SSG, 6 paths), `/demo/ecommerce/cart` (Static), `/demo/ecommerce/checkout` (Static), `/demo/ecommerce/confirmation` (Dynamic — request-time JWT signing)

## Next Steps

1. **Push branch to origin.** 15 unpushed commits on `phase/9f-ecommerce-native-reveal`. Safety preservation; no merge implication. — small
2. **Resolve Metabase dashboard-load.** Run `docs/manual/task-2026-04-21-006.md` runbook end-to-end. Path A (env vars missing) is the most likely cause on local dev. Record cold-start probe timings (<10s first paint / <15s interactive are binary ship-gate invariants). — small (diagnose) to medium (if Cloud Run config needs tuning)
3. **Formal Phase 9F UAT r2 plan.** UAT r1 closed this session; a structured UAT r2 script — pinning every UAT r1 response as pass/fail + covering the new live-sidebar invariants — is the natural next artifact. Pattern: follow `docs/uat/phase-9e-uat.sh` shape. Produce via `/handoff` Step 8 (skipped this session per user direction) or separately via product-reviewer. — medium
4. **Joint 9E+9F release cut.** Merge `phase/9f-ecommerce-native-reveal` to main once UAT r2 passes + Metabase cold-start probe records within thresholds. Both phases have been authored as separate blocks but release as one branch cut per the 9E release-coupling. — medium
5. **Minor debt fixes** (optional, low priority; see Issues & Technical Debt): interval-cleanup regression test; intermission sessionStorage skip; checkout Suspense skeleton. — small each

## Session Notes

### Pattern: TDD-first with backward-compat shims

Every live-sidebar wiring commit followed the same shape: extend the lib signature with optional `live?` context, thread through the readout with optional prop, then flip callers. Result: each commit was backward-compatible — the full suite kept passing between each change — which meant I could split the work across three commits (F1/F2/F3) without the suite going red in the middle.

### Pattern: "example" framing as a reusable honesty primitive

The UAT r1 item 3 fix introduced `resolveUtmMeta` returning `{ value, isLive }` and a readout-level "example · no utm in your url" badge. Pass-1 review identified the same dishonesty pattern on the warehouse-write BQ row; the fix reused the same pattern (utmIsLive gate + readout-header example-seed note). Pass-2 review identified the same concern on three more BQ columns; the fix extended the same seed-note to name them. Now there's an explicit reusable pattern: when a demo surface shows values that MAY be seeds, surface the seed framing in the panel header rather than pretending the value is live. Any future sidebar that presents "this is what's in the pipeline" should follow this shape.

### Pattern: Subagent-driven re-evaluation

Running the evaluator + product-reviewer in parallel after a batch of commits turned up two passes of findings that I would have missed (particularly the `useSessionContext` useMemo freeze — invisible in the sample-tests because they don't run real time forward). Each pass's fix pack was a single commit with a long message naming every finding and how it was addressed. Made the history easy to audit.

### Decision: skip Step 8 UAT plan generation this session

The `/handoff` invocation passed `skip the evals`. I interpreted this as "skip all subagent invocations for speed," which includes Step 8's product-reviewer UAT-plan call. Next session should produce a formal UAT r2 script + capture structured pass/fail results against the UAT r1 responses this session landed.

### Decision: three Minors deferred to handoff

Both Pass-2 reviewers explicitly said "Pass-3 not warranted." The 3 remaining Minors are feature/polish adds that change behaviour beyond the scope of "fix the UAT feedback" — they're the right shape for Phase 10 polish or opportunistic cleanup, not blocking the release cut.

### CLAUDE.md freshness

The **Current Phase** header line was updated this session to reflect "UAT r1 response complete + dual-evaluator passes." No other CLAUDE.md sections needed changes — tech stack, directory structure, coding standards, references all accurate as-is.

### Branch state

- Branch: `phase/9f-ecommerce-native-reveal` at `4b73e64`
- Commits this session: 15 (`a893a35` → `4b73e64`)
- **Unpushed** — 15 commits ahead of `origin/phase/9f-ecommerce-native-reveal`. Next session should push early to preserve.
- Working tree clean.

---

**Next session starting point:** `/start-phase 9F` → read this handoff → push branch → resolve Metabase dashboard-load task card → generate UAT r2 plan → run UAT r2 → cut joint 9E+9F release.
