# Session Handoff, 2026-03-29-012

**Phase:** 6, Three Demo Front-Ends
**Date:** 2026-03-29

## Completed This Session

### Demo Event Schema Extension (`250842c`)
- 8 new event types in `schema.ts`: ProductViewEvent, AddToCartEvent, BeginCheckoutEvent, PurchaseEvent, PlanSelectEvent, TrialSignupEvent, FormCompleteEvent, LeadQualifyEvent
- 8 corresponding tracking functions in `track.ts` matching data generator contracts
- 11 tests covering all event types and base field inclusion

### Shared Demo Layout & Navigation (`250842c`)
- `DemoNav` component with links to all three demos + back to main site, active route highlighting
- Demo landing page at `/demo` with cards for all three demos (content from CONTENT_GUIDE.md)
- `Demo layout.tsx` wraps all demo routes with `DemoNav`
- Header updated with Demos dropdown (desktop + mobile)
- Sitemap updated with demo routes
- 9 tests for DemoNav, demo landing, and header dropdown

### E-Commerce Demo, The Tuna Shop (`e27f286`)
- Product data module: 6 products from content guide with `getProduct()` and `getRelatedProducts()`
- Cart context: `useReducer`-based `CartProvider` with add/remove/update/clear operations
- Product listing page at `/demo/ecommerce` (6 product cards with links)
- Product detail pages at `/demo/ecommerce/[productId]`, fires `product_view` on mount, `add_to_cart` on button click
- Cart page at `/demo/ecommerce/cart`, quantity adjustment, remove, empty state
- Checkout page at `/demo/ecommerce/checkout`, fires `begin_checkout` once (useEffect+ref guard), `purchase` on submit
- Order confirmation at `/demo/ecommerce/confirmation`, shows order ID, total, flip-the-card prompt
- 29 tests (products, cart context, all page components)

### Subscription Demo, Tuna Subscription Box (`d98d2d7`)
- Plan data module: 3 plans (The Pup $19.99, The Good Boy $34.99, The Big Tuna $49.99)
- Plan cards with `plan_select` events on CTA click, Most Popular badge
- Trial signup form with `trial_signup` event, 50% off first box messaging
- Account dashboard with Active (Trial) status, plan details, Upgrade/Downgrade/Cancel buttons (fire `click_cta` events)
- 12 tests

### Lead Gen Demo, Tuna Partnerships (`2d6bb82`)
- Landing page with partnership highlights, 4 offering cards from content guide
- Partnership inquiry form with multi-select partnership types (checkboxes), budget range dropdown
- Form fires `form_start` (once via ref), `form_field_focus` per field, `form_complete` + `lead_qualify` on submit
- Lead qualification: high/medium/low tiers matching data generator contract
- Thank you page with flip-the-card prompt
- 11 tests

### Evaluator Fixes (`a9c0845`)
- Fixed `begin_checkout` re-fire on every render (moved to useEffect with ref guard)
- Fixed qualification tiers from hot/warm/cold to high/medium/low (matches generator)
- Changed partnership type from single-select to multi-select checkboxes (matches content guide)
- Added click_cta events to subscription dashboard management buttons

## In Progress

Nothing, all front-end deliverables complete.

## Blocked

Nothing.

## Issues & Technical Debt

### Remaining Phase 6 deliverables (deferred):
1. **Web GTM container update**, need to add demo event tags (product_view, add_to_cart, purchase, trial_signup, plan_select, form_complete, lead_qualify) to `infrastructure/gtm/web-container.json`
2. **sGTM container update**, need demo event triggers, GA4 forwarding tags, BigQuery write tags, simulated Meta CAPI and Google Ads Enhanced Conversions tags in `infrastructure/gtm/server-container.json`

### From evaluator (minor, non-blocking):
3. **Demo introduction sections not implemented**, Content guide defines tier-by-tier "what you'll see when you flip the card" descriptions. Demos jump straight to content instead. Low priority.
4. **`getRelatedProducts` is category-unaware**, returns first N products after filtering, not category-matched. Cosmetic.
5. **Checkout form fields have no `required` attributes**, form submits even if fields are cleared. Minor UX issue.
6. **Frontend schema missing subscription_renewal/subscription_churn types**, server-generated lifecycle events not in frontend DataLayerEvent union. No functional impact.
7. **`PurchaseEvent.products` is `string` (JSON) on frontend vs `Array` on generator**, may need alignment in Dataform staging layer.

### Carried forward from Phase 5:
8. **Ad platform data not in BigQuery**, `ad_platform_raw` table exists but generator doesn't insert records
9. **Subscription/leadgen backfill not yet run**, only ecommerce has test data
10. **Dataform not connected to GitHub**, needs GCP console setup

## Evaluator Results

- **Score:** 3.00/5.0, PASS WITH ISSUES
- **Critical issues:** 3 found, all fixed (begin_checkout re-fire, qualification tier mismatch, single vs multi-select)
- **Important issues:** 4 found, 2 fixed (dashboard buttons, PHASE_STATUS update), 2 deferred (GTM containers, subscription_renewal/churn types)
- **Minor issues:** 3 remaining (intro sections, related products, form validation)

## Test State

- **Total:** 369 passing, 0 failing, 42 suites
- **New this session:** 72 tests across 7 test files
- Build: clean
- Lint: clean

## Next Steps

1. **Update GTM container specs**, add demo event tags and triggers to web and server container JSON files
2. **Add demo intro sections**, tier-by-tier descriptions from content guide (optional, cosmetic)
3. **Phase 7, BI Layer & Demo Dashboards**, after GTM container updates are complete
4. **Run full 3-model backfill**, subscription and leadgen models still need historical data
5. **Connect Dataform to GitHub**, register repository in GCP Dataform console
