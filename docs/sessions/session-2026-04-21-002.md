# Session Handoff — 2026-04-21-002

**Phase:** 9F — Ecommerce Demo Native-Reveal Rebuild
**Date:** 2026-04-21
**Branch:** `phase/9f-ecommerce-native-reveal` (cut from `phase/9e-nav-overlay-pivot` HEAD at start of session)

## Completed This Session

This session took Phase 9F from a bare-stub `deliverables TBD` block to **dev-complete**: doc pass + 12 deliverables + prototype sub-nav adoption + final PHASE_STATUS update. 17 commits on the 9F branch.

### Doc pass (commit `136c303`)

Replaced the stub Phase 9F sections in `docs/REQUIREMENTS.md`, `docs/ARCHITECTURE.md`, `docs/PHASE_STATUS.md`, and `docs/CLAUDE.md` with the full 12-deliverable specification. Reconciles the canonical `docs/UX_PIVOT_SPEC.md` §3.5 against the hi-fi reference implementation at `docs/input_artifacts/design_handoff_ecommerce/`. The reconciliation subsection in REQUIREMENTS records each spec-vs-prototype decision explicitly so future sessions don't relitigate. Notable resolutions:

- Routing: Next.js native, not prototype hash routing.
- Listing toast cascade: adopt prototype's 3-toast session-boot sequence (session_start → taxonomy_classified → view_item_list), not spec's single toast.
- Toasts on cart/checkout/confirmation: adopt prototype's "toast + dominant pattern" compositions over spec's table-only dominant pattern — preserves event visibility parity.
- Full-page diagnostic placement: adopt prototype's on-checkout-submit → /confirmation (spec internal inconsistency on placement resolved).
- Tuna Shop palette: demo-scoped via `data-demo="ecommerce"`. UX_PIVOT_SPEC §1.4 "no editorial surface adopts amber as primary" invariant preserved (shop primary is terracotta, not amber).
- Brand voice rules (lowercase / no em dashes / no rule-of-three / first-person plural / no-kill rescues) verbatim from prototype README.
- 6-SKU catalog verbatim from prototype (replaces Phase 6's placeholder catalog).
- Confirmation embed shape locked: one full-dashboard Metabase embed at `/embed/dashboard/:jwt`, not six individual question embeds (user-confirmed).
- Cold-start ship-gate upgraded from soft caveat to binary release blocker.

### D1–D4 — Four pattern primitives

| Deliverable | Commit | Tests |
|---|---|---|
| D1 Event toast (Pattern 1) | `97bfc37` | +17 |
| D2 Live sidebar (Pattern 2) | `202a8ab` | +17 |
| D3 Inline diagnostic (Pattern 3) | `718741c` | +7 |
| D4 Full-page diagnostic moment (Pattern 4) | `b774d43` | +12 |

All four primitives land under `src/components/demo/reveal/`. Shared: warm Tuna terminal palette (`#0D0B09` bg / `#EAD9BC` ink / `#F3C769` amber — distinct from site-wide `#FFA400` phosphor), `prefers-reduced-motion` branching, portal-based rendering where needed (toast portal at `document.body`, full-page diagnostic portal at body), explicit `data-*` attributes for testability, `aria-live="polite"` / `role="dialog"` / `role="status"` per pattern.

Also added z-index tokens to `tailwind.config.ts`: `sidebar: 20`, `toast: 45`, `full-page-diagnostic: 49`, `overlay: 50`.

### D12a — Brand palette scope (commit `184e966`)

`data-demo="ecommerce"` on the ecommerce layout wrapper. CSS variables in `src/styles/globals.css` scope the Tuna Shop palette inside the demo subtree: `--accent` flips from persimmon to terracotta (`#C4703A`), `--shop-cream` / `--shop-cream-2` / `--shop-amber` / `--shop-warm-brown`, plus the `--term-*` palette for reveal-pattern surfaces. Shop primary is terracotta, preserving §1.4 invariant.

### Catalog adoption (commit `79dd5cc`)

`src/lib/demo/products.ts` replaced with the prototype's 6 SKUs verbatim: `tuna-plush-classic` ($26), `tuna-calendar-2026` ($14), `colin-plush` ($16), `tuna-plush-pi` ($24), `tuna-cameo` ($40), `tuna-combo` ($32). Added `blurb` / `tag` / `palette[3]` / `imageLabel` fields. Updated downstream test fixtures.

### D5–D9 — Per-page rebuilds

Each deliverable rewrote one route's surface against the four primitives + palette + catalog.

| Route | Deliverable | Commit | Key moves |
|---|---|---|---|
| `/demo/ecommerce` | D5 | `28f0688` | 3-toast cascade + editorial hero + your-utm panel + 3-col palette-tile grid |
| `/demo/ecommerce/[productId]` | D6 | `b66696d` | `product_view` toast + staging-layer LiveSidebar + 2-col layout + palette swatches |
| `/demo/ecommerce/cart` | D7 | `5f0f1df` | `view_cart` toast + data-quality LiveSidebar + localStorage persistence (`iampatterson.tunashop.cart.v1`) + cross-tab sync |
| `/demo/ecommerce/checkout` | D8 | `b73fd41` | `begin_checkout` toast + warehouse-write LiveSidebar (real `iampatterson_raw.events_raw`) + full-page diagnostic moment on submit |
| `/demo/ecommerce/confirmation` | D9 | `5a2afe3` | `purchase` toast + 6-step inline diagnostic + full-dashboard Metabase embed (`/embed/dashboard/:jwt` × 1) |

Each per-page rebuild includes a new test file under `tests/unit/components/demo/ecommerce/`. The old combined `tests/unit/demo/ecommerce-pages.test.tsx` was progressively emptied and eventually deleted (all coverage migrated to the per-page test files).

New libraries under `src/lib/demo/reveal/` carrying the salvaged pre-9E `*Underside` content as pure data:
- `campaign-taxonomy.ts` — 7-entry UTM seed map
- `staging-layer.ts` — 6 field casts + 4 stitch ops
- `data-quality.ts` — 6 assertions + volume-anomaly branching
- `warehouse-write.ts` — 21-column BQ row preview + 7-line diagnostic sequence

D9 also added `signDashboardEmbedUrl` + `mintConfirmationDashboardUrl` to `src/lib/metabase/embed.ts` (preserves 9B signing conventions; swaps resource from `question` to `dashboard`). Container widened to `max-w-[1200px]` at ≥1024px. Mobile (<768px) shows IAP-gated "View full dashboard → (Google SSO required — internal BI)" deep-link as honest production affordance. Lead paragraph with `$total` interpolation + zombie-state fallback closes 9B follow-up #5.

### D10 — Component removal + signer cleanup (commit `12cf7a1`)

Deleted `Tier3Embeds` + `LiveEmbedFrame` + the question-level signer helpers (`signEmbedUrl`, `mintConfirmationEmbedUrls`, `ConfirmationEmbedUrls`, `EmbedUrlParams`). Dropped the `description` alias on `Product` (no callers after D5/D6). `parseEmbedConfig` + its 6 tests preserved — still validates the env contract shape.

### Sub-nav adoption (commit `536bcd3`, user direction)

User requested the prototype's sub-nav pattern after D9. New `EcomSubNav` component renders `shop · cart` wayfinding below the site chrome on every `/demo/ecommerce/*` route, with live cart count badge + `aria-current="page"` state + accessible nav landmark. Distinct from the site's session-scoped SessionPulse nav.

### D11 — SessionPulse reachability (commit `5017376`)

Two-pass coverage:
1. Jest funnel integration test (`tests/integration/phase9f-funnel-integration.test.tsx`) drives the SessionStateProvider via window.dataLayer events and verifies `demo_progress.ecommerce.stages_reached` + percentage invariants through the full product_view → add_to_cart → begin_checkout → purchase sequence. Runs in `npm test`.
2. Playwright E2E scaffold (`tests/e2e/ecommerce-sessionpulse.spec.ts`) with full browser-level coverage — requires running dev server + installed browsers, executes via `npx playwright test`, **not** part of `npm test`. Staged for when a ready environment exists.

Also fixed a Jest/Playwright spec-file collision by adding `testPathIgnorePatterns: ['/tests/e2e/']` to `jest.config.ts`.

### D12b — Palette-token harmony (commit `1dde49d`)

5 dashboard components migrated from raw `neutral-*` to the editorial `ink / paper / rule` scale: `analytics-dashboard.tsx`, `kpi-card.tsx`, `chart-card.tsx`, `data-table.tsx`, `funnel-chart.tsx`. Addresses 9B follow-up #3. No `neutral-*` classes remain in `src/components/demo/` or `src/app/demo/` trees.

## Test State

- **Total:** 990 (up from 863 at session start — **+127 net**)
- **Passing:** 990
- **Failing:** 0
- **Skipped:** 0
- **New test files:** 11
- **Deleted test files:** 2 (obsolete combined `ecommerce-pages.test.tsx` + `tier3-embeds.test.tsx`)

## Build State

- `npm run build` clean throughout the session
- Route distribution: `/demo/ecommerce` (Static), `/demo/ecommerce/[productId]` (SSG, 6 paths), `/demo/ecommerce/cart` (Static), `/demo/ecommerce/checkout` (Static), `/demo/ecommerce/confirmation` (Dynamic — request-time JWT signing)
- Homepage `/` still Static (unchanged by 9F)

## In Progress

Nothing. Every deliverable committed; working tree clean.

## Blocked

No deliverables blocked. Two pending-env items carry to the release cut:

1. **Playwright E2E execution** — scaffold ready. Requires `npm run dev` + `npx playwright install` + `npx playwright test`. Not blocking dev-complete.

2. **Metabase cold-start probe** (D9 ship-gate) — requires deployed environment with `MB_EMBEDDING_SECRET_KEY` + `METABASE_EMBED_CONFIG` Vercel env vars. User confirmed they applied `--no-cpu-throttling` to the Metabase Cloud Run service earlier in the session. Probe execution + timing record happens at joint 9E+9F release deploy, not before.

## Evaluator Results

Dual evaluators running at session end (tech + product). Results to be pasted in when they complete.

## Next Steps

1. **Review dual-evaluator findings** — fix critical + major items per standard close-out pattern.
2. **Run Playwright E2E** when a dev-server-capable environment is available — should pass as-written.
3. **Deploy to Vercel preview** — confirm `MB_EMBEDDING_SECRET_KEY` + `METABASE_EMBED_CONFIG` env vars set, then run the Metabase cold-start probe (fresh tab after ≥15 min idle) and record first-paint / interactive timings in the release handoff. Ship-gate invariant: <10s first paint / <15s interactive.
4. **Joint 9E+9F release cut** — both phases now dev-complete. Merge to main when preview + cold-start probe validate.

## Session Notes

### Reconciliation table (documented in REQUIREMENTS 9F block for posterity)

The hi-fi prototype was authored after UX_PIVOT_SPEC §3.5 and made concrete design decisions the spec left open. Where spec and prototype diverged, this session's reconciliations:

1. Routing: Next.js native (spec-aligned; prototype's hash routing was a static-HTML constraint).
2. Listing toast cascade: 3-toast sequence (prototype), not lone toast (spec's thinner framing).
3. Toasts on cart/checkout/confirmation: adopt in addition to spec's dominant pattern.
4. Full-page placement: on-submit → /confirmation (prototype), resolving spec's internal inconsistency.
5. Tuna Shop palette: demo-scoped via `data-demo`, terminal surfaces use warm amber `#F3C769` not site-wide `#FFA400`.
6. Catalog: 6 SKUs verbatim from prototype (prototype resolves — Phase 6 placeholders were generic).
7. Cart shape: **kept pre-9F API** (`addItem/updateQuantity/removeItem/clearCart` with `{product_id, product_name, product_price, quantity}`). Storage key adopted from prototype (`iampatterson.tunashop.cart.v1`) but shape rename (`{id,qty,price,name}`) deliberately skipped to avoid cross-cutting consumer churn — documented in module JSDoc as a deliberate divergence.
8. Confirmation embed: single full-dashboard (user-confirmed).
9. Brand voice rules: verbatim.
10. Motion timings: verbatim.
11. "Returning soon" footnote: dropped (UAT F8 carry-forward).

### Pattern established: doc-pass before deliverables

Opening the session with a full doc pass (replacing all four top-level docs' Phase 9F stubs with fleshed deliverables + reconciliation subsection + architecture detail) lets every subsequent per-deliverable commit reference a clear source of truth. The April 19 prior doc pass (commits `88e4a28` / `477371c` / `6d485d9` on the old `phase/9f-ecommerce-native-reveal` branch) was used as starting material and updated for post-UAT vocabulary + prototype-locked decisions.

### Pattern established: progressive obsolescence of combined test files

The pre-9F `tests/unit/demo/ecommerce-pages.test.tsx` combined file tested ProductListing / ProductDetail / CartView / CheckoutForm / OrderConfirmation in one fixture. Each per-page rebuild (D5 → D6 → D7 → D8 → D9) dropped its block from the combined file + introduced a new per-page test file under `tests/unit/components/demo/ecommerce/`. By D9 the combined file was empty; D9 deleted it. This kept each commit's test delta scoped to that deliverable without a dedicated test-refactor commit.

### Decision: cart-context keeps pre-9F API

The doc spec called for rewriting cart-context wholesale to the prototype's shape (`{id,qty,price,name}` with `add/setQty/remove/clear` verbs). I assessed that as churn without visitor-visible benefit: the field-rename touches every consumer (listing-view, product-detail, checkout-form, order-confirmation, tracking helpers, existing tests) without changing what visitors experience. Compromise: adopted the storage key + localStorage persistence + cross-tab sync (visible behavior changes) while keeping the API names stable. Documented in module JSDoc.

### Decision: per-page tests over monolithic integration tests

Jest tests at the per-page component level + one integration test for the end-to-end funnel progression (D11). Playwright E2E ready to run but deferred because the sandbox lacks browser infrastructure. This keeps the test suite fast (~6.5s for 990 tests) while preserving behavior coverage.

### Branch state

- Branch: `phase/9f-ecommerce-native-reveal` at `8f032c4`
- Commits this session: 17 (`136c303` doc pass → `8f032c4` PHASE_STATUS completion)
- Diverged from `origin/phase/9f-ecommerce-native-reveal`: 17 commits ahead. The remote branch from session 027's earlier doc-first pass was discarded; this branch was reset to `phase/9e-nav-overlay-pivot` HEAD at session start, so on push it'll force-update (user flagged they'd log off — force-push requires their ack).
- Working tree clean.

---

**Next session starting point:** `/start-phase 9F` → read this handoff + dual-evaluator results → address any critical/major findings → coordinate joint 9E+9F release cut.
